# ─── Django core ─────────────────────────────────────────────────────────────
# Generate with: python -c "from django.utils.crypto import get_random_string; print(get_random_string(50))"
SECRET_KEY=your-50-char-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# ─── Database ────────────────────────────────────────────────────────────────
DB_NAME=klikk_db
DB_USER=klikk_user
DB_PASSWORD=klikk_pass
DB_HOST=localhost
DB_PORT=5432

# ─── Testing ─────────────────────────────────────────────────────────────────
USE_SQLITE_FOR_TESTS=True

# ─── AI / LLM ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=your-anthropic-api-key
ANTHROPIC_WEB_FETCH_ENABLED=false

# ─── PDF generation (Gotenberg) ──────────────────────────────────────────────
GOTENBERG_URL=http://localhost:3000

# ─── Google ──────────────────────────────────────────────────────────────────
GOOGLE_OAUTH_CLIENT_ID=463351671420-7hh3aln6nc6s5vi6ds9huka21cphp9h1.apps.googleusercontent.com
GOOGLE_MAPS_API_KEY=AIzaSyAKdTBybb8R0xC-NldeUsjgRve7kDs5RZY

# ─── E-signing URLs ──────────────────────────────────────────────────────────
SIGNING_PUBLIC_APP_BASE_URL=http://localhost:5173
ESIGNING_WEBHOOK_PUBLIC_URL=http://localhost:8000

# ─── Email (Gmail SMTP) ──────────────────────────────────────────────────────
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=michael.c.dippenaar@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=Klikk <michael.c.dippenaar@gmail.com>

# ─── Optional RAG / vector store ─────────────────────────────────────────────
# CONTRACT_DOCUMENTS_ROOT=/absolute/path/to/documents
# RAG_CHROMA_PATH=/absolute/path/to/rag_chroma
# RAG_QUERY_CHUNKS=8
# RAG_PDF_MAX_PAGES=120

# ─── Optional Twilio (SMS/OTP) ───────────────────────────────────────────────
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_SMS_FROM=
