# Notifications Test Module

This module currently has **no test coverage**.

## What needs testing
- Push notification dispatch (Firebase Admin SDK)
- Email template rendering
- Notification model CRUD
- Token validation (PushToken model in accounts app)

## Priority
Medium — add tests before shipping any notification feature changes.
