name: klikk_tenant
description: Klikk Tenant App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  country_code_picker: ^3.0.0
  pin_code_fields: ^8.0.0
  http: ^1.2.0
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
