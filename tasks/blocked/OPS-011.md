---
id: OPS-011
stream: ops
title: "Add website_web image build and push to staging CI/CD pipeline"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: blocked
assigned_to: null
depends_on: []
asana_gid: "1214181292747447"
created: 2026-04-22
updated: 2026-04-22T20:30:00
---

## Goal
Wire the Astro marketing site (`website_web`) into the staging CI/CD pipeline so it is built, pushed to GHCR, and deployed from a pinned SHA tag — matching the pattern used for `backend`, `admin_web`, and `agent_app_web`.

## Acceptance criteria
- [x] Add `image: ${WEBSITE_IMAGE:-ghcr.io/michaelcdippenaar/klikk-website:latest}` to `website_web` in `deploy/docker-compose.staging.yml`
- [x] Add a "Build & push website" step to `.github/workflows/staging-deploy.yml` using `./website/Dockerfile`
- [x] Export `WEBSITE_IMAGE` in the SSH deploy block alongside the other three image env-vars
- [ ] Staging deploy runs end-to-end without the website service lagging behind `main`

## Files likely touched
- `deploy/docker-compose.staging.yml`
- `.github/workflows/staging-deploy.yml`
- `website/Dockerfile` (may need to exist / be stable first)

## Test plan
**Manual:**
- Trigger staging deploy workflow after a commit that changes the website
- Verify `docker ps` on staging shows the newly tagged `klikk-website` image

**Automated:**
- GitHub Actions workflow completes without errors for all four services

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-22 — Promoted from discovery `2026-04-22-website-web-not-in-cicd.md` found during OPS-001 review. Depends on `website/Dockerfile` being stable; implement once the Astro site ships real content.

2026-04-22 — Implemented by rentals-implementer. Three changes made:
1. `deploy/docker-compose.staging.yml`: added `image: ${WEBSITE_IMAGE:-ghcr.io/michaelcdippenaar/klikk-website:latest}` to the `website_web` service.
2. `.github/workflows/staging-deploy.yml`: added "Build & push website" step in the `build-and-push` job, using `context: .` (repo root) with `file: ./website/Dockerfile` to match the Dockerfile's `COPY website/` and `COPY content/` pattern. `BUILD_MODE=staging` passed as build-arg.
3. `.github/workflows/staging-deploy.yml`: exported `WEBSITE_IMAGE` in the SSH deploy block, and added `website_web` to the `docker compose pull` command.
The `website/Dockerfile` already exists and is stable (nginx serving Astro static output). The fourth acceptance criterion (end-to-end staging run) requires a live pipeline trigger — left for tester to verify.

2026-04-22 — Review passed (rentals-reviewer). Checked commit b0690b6.
AC1: `image: ${WEBSITE_IMAGE:-...}` correctly added to `website_web` in `deploy/docker-compose.staging.yml` (line 93), consistent with the same pattern on `backend`, `admin_web`, and `agent_app_web`.
AC2: "Build & push website" step added to `.github/workflows/staging-deploy.yml` using `context: .` (repo root) + `file: ./website/Dockerfile`. Context width is intentional and correct — the Dockerfile does `COPY content/ /content/` which requires the repo root. `BUILD_MODE=staging` build-arg matches the admin and agent-app pattern.
AC3: `WEBSITE_IMAGE` exported in the SSH deploy block; `website_web` added to `docker compose pull` command — both consistent with the other three services.
AC4: Live end-to-end pipeline run deferred to tester per task plan.
Security pass: CI/CD config only. No auth endpoints, no PII, no raw SQL, no secrets introduced. No regressions identified.

2026-04-22 — Test run (rentals-tester).
- Static checks (code review): PASS — AC1, AC2, AC3 all confirmed in files:
  - `deploy/docker-compose.staging.yml` line 93: `image: ${WEBSITE_IMAGE:-ghcr.io/michaelcdippenaar/klikk-website:latest}` present.
  - `.github/workflows/staging-deploy.yml`: "Build & push website" step present at line 82, using `file: ./website/Dockerfile`, `BUILD_MODE=staging` build-arg.
  - `WEBSITE_IMAGE` exported at line 144; `website_web` added to `docker compose pull` at line 146.
- Manual test (live staging deploy trigger + `docker ps` verification): BLOCKED — requires SSH access to staging server and a live GitHub Actions run. Cannot be executed from this environment. The test plan requires human execution against a live staging pipeline.
- Decision: blocked pending live pipeline verification (AC4).
