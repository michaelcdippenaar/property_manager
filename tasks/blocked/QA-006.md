---
id: QA-006
stream: qa
title: "UI visual regression + design-token audit across admin SPA"
feature: ""
lifecycle_stage: null
priority: P1
effort: M
v1_phase: "1.0"
status: blocked
asana_gid: "1214177048185609"
assigned_to: implementer
depends_on: []
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Verify that every view in the admin SPA uses the Klikk design tokens consistently (Navy, Accent, fonts, spacing scale) and snapshot the current UI as a baseline for visual-regression tests going forward.

## Acceptance criteria
- [ ] Token audit report: every hardcoded colour hex not in `src/tokens/*` flagged and replaced
- [ ] Typography: every non-token font-size/weight flagged
- [ ] Spacing: every magic margin/padding normalised to token scale
- [ ] Storybook (or `/components` preview) for atoms + molecules + key organisms
- [ ] Chromatic / Percy / Playwright snapshot baseline captured per view on staging viewport
- [ ] Tie in to `klikk-design-standard` and `klikk-design-frontend-taste` skills

## Files likely touched
- `admin/src/tokens/*`
- `admin/src/assets/styles/*`
- Every `admin/src/views/**/*.vue` (targeted token replacement)
- `docs/qa/ui-audit-2026-04.md` (new)

## Test plan
**Automated:**
- Snapshot test: `npm run test:visual` green
- Token lint: ESLint rule flagging raw hex values fails the build

## Handoff notes

### 2026-04-22 — implementer → blocked (wrong effort estimate; missing infrastructure)

**Reason blocked:** Task is labelled `effort: M` but is actually L or XL. Three blockers prevent implementation:

1. **No tokens directory.** `admin/src/tokens/` does not exist. Acceptance criterion 1 says "replace hardcoded hex not in `src/tokens/*`" but there is nowhere to put them yet. Creating a token system from scratch is a separate task.

2. **No Storybook.** Acceptance criterion 4 requires Storybook (or a `/components` preview route). `@storybook/*` is absent from `admin/package.json`. Installing and configuring Storybook for a Vue 3 + Vite + Tailwind project, then writing stories for every atom/molecule/organism, is L-effort on its own.

3. **No visual-regression tooling.** Acceptance criterion 5 requires Chromatic, Percy, or `npm run test:visual` (Playwright snapshots). Neither Chromatic nor Percy is installed, and the `test:visual` npm script does not exist. Setting up a snapshot baseline across every view on staging is a separate pipeline task.

**Questions for PM — please split into three child tasks:**
- `QA-006a` — Create `admin/src/tokens/` CSS custom-property file and replace all hardcoded hex values across views (M effort — token audit + replacement pass).
- `QA-006b` — Scaffold Storybook for admin SPA; write stories for atoms/molecules/key organisms (M effort — tooling setup + story authoring).
- `QA-006c` — Add `npm run test:visual` Playwright snapshot baseline; integrate with CI (M effort — snapshot tooling + baseline capture).

Each child task has clear, bounded scope and fits M effort. Retiring QA-006 as the parent/umbrella once all three are created.
