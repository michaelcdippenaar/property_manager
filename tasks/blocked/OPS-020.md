---
id: OPS-020
stream: ops
title: "Remove plaintext staging password from DEVOPS.md and harden SSH auth"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: blocked
assigned_to: implementer
depends_on: []
asana_gid: "1214200629201038"
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Remove the inline staging server SSH password from the committed runbook and disable password-based SSH authentication on the staging server to close the credential leak.

## Acceptance criteria
- [x] Inline password comments removed from `deploy/DEVOPS.md` (lines 19 and 276)
- [ ] Staging server SSH password rotated (MC performs this server-side action)
- [ ] `PasswordAuthentication no` set in `/etc/ssh/sshd_config` on the staging server — key-based auth only
- [ ] MC's personal SSH public key added to `~/.ssh/authorized_keys` on the staging server (alongside the deploy key from OPS-012)
- [x] `grep -n "password:" deploy/DEVOPS.md` returns zero matches

## Files likely touched
- `deploy/DEVOPS.md`
- Server-side: `/etc/ssh/sshd_config` (human action required)

## Test plan
**Manual:**
- `ssh mc@102.135.240.222` with old password — expect rejection
- `ssh -i ~/.ssh/id_ed25519 mc@102.135.240.222` — expect success

## Handoff notes
Promoted from discovery `2026-04-22-devops-runbook-plaintext-password.md` (found during OPS-012). Staging server is now publicly reachable; credential is directly exploitable.

2026-04-23 (implementer): This task is a near-duplicate of OPS-017 (same title, same goal). AC1 and AC5 are already satisfied: `grep -n "password:" deploy/DEVOPS.md` returns zero matches (confirmed). The inline password comments were removed in commit `bb6dad9` (OPS-017 implementation). AC3 runbook steps are already in DEVOPS.md at section "5a. Harden SSH on the staging server (run once by MC)" — sed commands to set `PasswordAuthentication no`, reload sshd safely, and rotate the OS password are all documented. ACs 2, 3, 4 are server-side manual actions that require MC to SSH into `102.135.240.222` directly (or via console) and run the runbook. These cannot be executed by an implementer agent. BLOCKED: PM should confirm whether MC has run the OPS-017 runbook on the server yet. If yes, this task is done and should be closed. If no, MC needs to execute the runbook in deploy/DEVOPS.md section 5a before this can close.
