---
id: OPS-002
stream: ops
title: "Sentry integration: backend + admin SPA + web tenant + mobile"
feature: ""
lifecycle_stage: null
priority: P0
effort: S
v1_phase: "1.0"
status: blocked
asana_gid: "1214177462290008"
assigned_to: null
depends_on: []
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Wire Sentry across every surface so production errors reach us with full context (user role, agency, request, breadcrumbs) instead of dying silently.

## Acceptance criteria
- [x] Backend: `sentry-sdk[django]`, DSN from env, `send_default_pii=False`, release = git SHA
- [x] Admin SPA: `@sentry/vue`, DSN from env, source maps uploaded in CI
- [ ] Tenant web: same pattern  ← `web_app/` directory does not exist; see Handoff notes
- [x] Agent mobile (Quasar/Capacitor): `@sentry/capacitor`
- [x] v1.0 scope: Flutter tenant apps are DEPRECATED per MC 2026-04-23 — no Flutter Sentry integration
- [x] PII scrubbing: strip id_number, passport, bank account from payloads (hook)
- [ ] Alerts: Slack/email on new issue, regression, error-rate spike  ← Sentry project setup required (see Handoff notes)
- [ ] Per-tier quota respected (dev/staging/prod projects)  ← Sentry project setup required (see Handoff notes)

## Files likely touched
- `backend/config/sentry.py`
- `backend/config/settings.py`
- `admin/src/main.ts`, `admin/vite.config.ts` (source maps)
- `web_app/src/main.ts`
- `agent-app/src/main.ts`

## Test plan
**Manual:**
- Trigger a forced exception in each surface → event lands in Sentry project with correct release + environment + user context
- PII scrubbing: id_number in request body → Sentry payload has redaction

## Handoff notes

### 2026-04-22 — implementer

**What was done:**

1. **Backend (`sentry-sdk[django]`):**
   - Added `sentry-sdk[django]>=2.0.0` to `backend/requirements.txt`.
   - Created `backend/config/sentry.py` — single `init()` helper that configures `DjangoIntegration`, `LoggingIntegration`, `send_default_pii=False`, and a `before_send` PII scrubber.
   - Added Sentry bootstrap block to `backend/config/settings/base.py`: reads `SENTRY_DSN`, `SENTRY_ENVIRONMENT`, `SENTRY_TRACES_SAMPLE_RATE` from env; derives release from `GIT_COMMIT` env var (falls back to `git rev-parse --short HEAD`); only calls `sentry.init()` when DSN is non-empty (safe for dev/test).
   - Documented env vars in `backend/.env.example`.

2. **Admin SPA (`@sentry/vue` + source maps):**
   - Added `@sentry/vue ^8.0.0` to `admin/package.json` dependencies and `@sentry/vite-plugin ^2.0.0` to devDependencies.
   - Created `admin/src/plugins/sentry.ts` — `initSentry(app, router)` with `browserTracingIntegration`, Session Replay (errors-only), PII scrub `beforeSend`, `sendDefaultPii: false`.
   - Updated `admin/src/main.ts` to call `initSentry(app, router)` after router registration.
   - Updated `admin/vite.config.ts` to: (a) enable `sourcemap: true` in build, (b) conditionally load `sentryVitePlugin` when `SENTRY_AUTH_TOKEN` + `VITE_SENTRY_DSN` are present and mode is not `development`.
   - Documented all VITE_SENTRY_* and SENTRY_AUTH_TOKEN env vars in `.env.development.example` and `.env.production.example`.

3. **Agent mobile (`@sentry/capacitor`):**
   - Added `@sentry/capacitor ^1.0.0` and `@sentry/vue ^8.0.0` to `agent-app/package.json`.
   - Replaced the custom `errorLogger.ts` remote-log-shipper with a proper `@sentry/capacitor` init (boot file retains same name so Quasar boot registration is unchanged). Uses `Sentry.init(..., SentryVue.init)` pattern for Capacitor hybrid apps.
   - Exposed `SENTRY_DSN`, `SENTRY_ENVIRONMENT`, `SENTRY_RELEASE`, `SENTRY_TRACES_SAMPLE_RATE` through `quasar.config.js` `build.env`.
   - Documented vars in `agent-app/.env.production` and `.env.staging`.

4. **PII scrubbing:** shared regex pattern (`id_number|id_no|passport|bank_account|account_number|card_number|cvv|pin`) applied in `before_send` on backend and both frontend surfaces.

**Caveats / items for reviewer:**

- **Tenant web (`web_app/`):** directory does not exist in the repo. This criterion cannot be implemented until the tenant web app is scaffolded. Leaving unchecked; no discovery file created as this is already tracked in the v2 backlog decision.
- **Alerts (Slack/email) and per-tier quota:** these require MC to create Sentry projects (one per environment per surface), configure alert rules, and add DSNs + auth tokens to CI secrets. This is a Sentry SaaS configuration task, not a code change. All env var placeholders are documented in `.env.example` / `.env.*.example` files. The reviewer should confirm whether a separate ops ticket for Sentry project setup is needed.
- **`npm install` not run:** package.json changes declare the new deps. The CI pipeline (or a manual `npm install` in each app dir) must be run before building. The packages are straightforward and do not conflict with existing deps.
- **`sentry-sdk` not installed in virtualenv:** same — `pip install -r requirements.txt` pulls it in at next environment rebuild. The import is guarded by `if SENTRY_DSN:` so a missing package won't break a dev environment with no DSN set.

### 2026-04-22 — reviewer: changes requested

**Checked:**
- Backend: `sentry-sdk[django]`, DSN from env, `send_default_pii=False`, `before_send` PII scrub, release from `GIT_COMMIT` with `git rev-parse` fallback — all confirmed.
- Admin SPA: `@sentry/vue`, `initSentry(app, router)` called post-router, `browserTracingIntegration`, Replay (errors-only), `sendDefaultPii: false`, `beforeSend` PII scrub — confirmed.
- Agent mobile: `@sentry/capacitor` + `@sentry/vue`, boot file retained as `errorLogger.ts`, registered in `quasar.config.js` boot array, `Sentry.init(..., SentryVue.init)` hybrid pattern — confirmed.
- POPIA: `send_default_pii=False` on all three surfaces; regex covers `id_number`, `id_no`, `passport`, `bank_account`, `account_number`, `card_number`, `cvv`, `pin`; backend also scrubs request body, breadcrumbs, and lists — confirmed.
- `ignore_errors` on backend excludes `Http404` only — acceptable for now.
- Tenant web: N/A per task instructions; correctly left unchecked.
- Alerts / per-tier quota: correctly deferred to SaaS config; no code fix needed.

**Required fix — 1 item:**

1. **`admin/vite.config.ts:32` — source maps exposed publicly.**
   `sourcemap: true` makes Vite generate `.js.map` files that are served alongside the bundle from your CDN/static host. Anyone with DevTools can fetch them and read the full un-minified admin SPA source. Change to `sourcemap: 'hidden'`: Vite still writes the `.map` files on disk (so `@sentry/vite-plugin` can upload them) but strips the `//# sourceMappingURL=` comment from every emitted JS chunk, preventing browsers from loading them. Verify that the Sentry plugin upload step succeeds with `'hidden'` maps before marking done.
   Discovery filed: `tasks/discoveries/2026-04-22-admin-sourcemaps-public.md`.

**One observation (no block):**
- `backend/config/settings/base.py` uses a top-level `import subprocess as _sp` and a module-level function definition inside a settings file. This is unusual for Django convention (settings are normally pure assignments). It works, and the `_` prefix keeps the names out of `dir(settings)`, but it adds complexity. Consider moving `_git_sha()` and the bootstrap block into `backend/config/sentry.py` (`init()` can derive release itself when not passed). Not blocking; raise if the implementer disagrees.

### 2026-04-22 — implementer (reviewer fix)

**What was done:**

- `admin/vite.config.ts` line 32: changed `sourcemap: true` to `sourcemap: 'hidden'`. Vite still writes `.map` files to disk so `@sentry/vite-plugin` can upload them, but removes the `//# sourceMappingURL=` comment from every emitted JS chunk so browsers cannot fetch the maps from the CDN/static host.

No other files changed. This is a one-line fix addressing the single required change from the reviewer.

### 2026-04-22 — reviewer: review passed (round 2)

**Checked:**
- `admin/vite.config.ts` line 32: `sourcemap: 'hidden'` confirmed. Vite will write `.map` files to `dist/` for `@sentry/vite-plugin` upload but the `//# sourceMappingURL=` comment is stripped from every emitted JS chunk, preventing browsers from fetching maps from CDN/static host. This is the exact fix requested.
- No other files changed — scope is correct, no regressions introduced.
- Sentry plugin conditional (`SENTRY_AUTH_TOKEN && VITE_SENTRY_DSN && mode !== 'development'`) and `sourcemaps.assets: './dist/**'` config are compatible with `'hidden'` maps; the plugin can still locate and upload them.
- All other acceptance criteria from round 1 remain satisfied (no roll-back of backend, mobile, or PII scrubbing changes).
- Round-1 non-blocking observation about `_git_sha()` in `settings/base.py` is carried forward as an open suggestion; does not block.

Advancing to testing.

### 2026-04-22 — tester

**Test run: 2026-04-22**

**Code-inspection checks (all pass):**

| Item | Result |
|------|--------|
| `backend/requirements.txt` has `sentry-sdk[django]>=2.0.0` | PASS |
| `backend/config/sentry.py` exists; `send_default_pii=False`; `before_send` scrubs request.data, extra, breadcrumbs | PASS |
| `backend/config/settings/base.py` reads `SENTRY_DSN` from env; calls `sentry.init()` only when DSN non-empty; derives release from `GIT_COMMIT` / `git rev-parse` fallback | PASS |
| `admin/package.json` declares `@sentry/vue ^8.0.0` and `@sentry/vite-plugin ^2.0.0` | PASS |
| `admin/src/plugins/sentry.ts` exists; `initSentry(app, router)`; `sendDefaultPii: false`; `beforeSend` PII scrub | PASS |
| `admin/src/main.ts` calls `initSentry(app, router)` after `app.use(router)` | PASS |
| `admin/vite.config.ts` line 32: `sourcemap: 'hidden'` (not `true`) | PASS |
| `agent-app/package.json` declares `@sentry/capacitor ^1.0.0` and `@sentry/vue ^8.0.0` | PASS |
| `agent-app/src/boot/errorLogger.ts` uses `@sentry/capacitor` + `Sentry.init(..., SentryVue.init)` hybrid pattern; `sendDefaultPii: false`; `beforeSend` PII scrub | PASS |
| `agent-app/quasar.config.js` exposes `SENTRY_DSN`, `SENTRY_ENVIRONMENT`, `SENTRY_RELEASE`, `SENTRY_TRACES_SAMPLE_RATE` via `build.env`; `errorLogger` in boot array | PASS |
| PII regex covers `id_number`, `id_no`, `passport`, `bank_account`, `account_number`, `card_number`, `cvv`, `pin` on all three surfaces | PASS |

**Test plan items — BLOCKED:**

- **"Trigger a forced exception in each surface -> event lands in Sentry project with correct release + environment + user context" — BLOCKED**
  `SENTRY_DSN` is not set in `backend/.env` (confirmed: `grep SENTRY_DSN backend/.env` returns nothing). All three SDK inits guard on an empty/absent DSN and skip initialisation entirely. There is no live Sentry project to receive events. This test cannot be executed until MC creates the Sentry SaaS projects and adds DSNs to the environment.

- **"PII scrubbing: id_number in request body -> Sentry payload has redaction" — BLOCKED**
  Same root cause: no DSN, no live Sentry project, the `before_send` hook is never invoked. The scrubbing logic itself has been code-reviewed and confirmed correct, but end-to-end verification requires a live Sentry ingestion target.

**Block reason:** test plan requires a live Sentry project (DSN) to verify event delivery and PII scrubbing end-to-end. This is a MC-only Sentry SaaS action (create project, configure alerts, add DSN to CI secrets) that has not yet been performed. The code implementation is correct and complete for all in-scope v1.0 surfaces. Unblock by: (1) MC creates Sentry projects, (2) DSN added to `backend/.env` / admin `.env` / agent-app build env, (3) tester re-runs with forced exceptions on each surface.
