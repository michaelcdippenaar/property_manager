---
id: OPS-017
stream: ops
title: "Remove plaintext staging password from DEVOPS.md and harden SSH auth"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: blocked
assigned_to: null
depends_on: []
asana_gid: "1214195157722044"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Remove the committed plaintext SSH password from `deploy/DEVOPS.md`, rotate the staging server credential, and disable password-based SSH authentication.

## Acceptance criteria
- [x] Inline `# password: pass` comments removed from all SSH examples in `DEVOPS.md` (lines 19 and 276)
- [ ] Staging server SSH password rotated (server-side action by MC)
- [ ] `PasswordAuthentication no` set in `/etc/ssh/sshd_config` on staging — key-based auth only
- [ ] MC's personal SSH key added to `~/.ssh/authorized_keys` on staging (if not already present)
- [ ] `sshd` reloaded after config change; confirm `ssh -o PasswordAuthentication=no mc@102.135.240.222` works with key

## Files likely touched
- `deploy/DEVOPS.md` (lines 19, 276)
- Staging server `/etc/ssh/sshd_config` (server-side)

## Test plan
**Manual:**
- Attempt `ssh -o PubkeyAuthentication=no mc@102.135.240.222` — confirm connection refused
- `ssh mc@102.135.240.222` with key — confirm succeeds

**Automated:**
- n/a (server-side hardening)

## Handoff notes
2026-04-22: Promoted from discovery `2026-04-22-devops-runbook-plaintext-password.md` (found during OPS-012). Staging server is now publicly reachable — credential is directly exploitable.

2026-04-22 (implementer): The `# password: pass` comments were already removed from DEVOPS.md in commit `43b2194` (RNT-SEC-009). Acceptance criterion 1 is done. Added a dedicated "Harden SSH on the staging server (run once by MC)" runbook section to `deploy/DEVOPS.md` with step-by-step `sed` commands for disabling PasswordAuthentication, reloading sshd safely, and rotating the OS password. Criteria 2-5 are server-side manual actions that MC must execute from the staging console. The test plan steps (confirm key works, confirm password refused) are embedded as comments in the new runbook section. Reviewer: confirm the runbook steps are correct and complete, then MC runs them on the server during the testing phase.

2026-04-22 (reviewer): Review passed. Confirmed `grep "password: pass" deploy/DEVOPS.md` returns zero matches — AC1 satisfied. The new runbook section at deploy/DEVOPS.md lines 229-265 is technically correct: `sed` regex handles both commented and uncommented `PasswordAuthentication` lines; `systemctl reload ssh` (not restart) preserves existing sessions; key presence is checked before disabling password auth; verify step is included before reload. No security regressions. Criteria 2-5 are server-side manual actions requiring MC console access — correctly deferred to testing phase. No code changes; no auth/POPIA surface.

2026-04-22 (tester): Test run — BLOCKED. AC1 verified: `grep "password: pass" deploy/DEVOPS.md` returns zero matches (PASS). Test plan steps 1 and 2 require live SSH access to staging server `mc@102.135.240.222` — tester policy prohibits staging/server access. AC2-5 remain unverified and require MC to execute the runbook in `deploy/DEVOPS.md` lines 229-265 directly on the staging server, then confirm password-auth is refused and key-auth succeeds.
