---
id: OPS-013
stream: ops
title: "Fix backup drill-reminder crontab — CRONTAB_CONTENT env var is never consumed"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: blocked
assigned_to: implementer
depends_on: []
asana_gid: "1214196828530237"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Ensure the `drill_reminder` container runs `drill-reminder.sh` monthly on the 1st rather than running `backup.sh` nightly alongside the primary backup container.

## Acceptance criteria
- [x] `drill_reminder` service uses its own crontab file (e.g. `/etc/crontab.drill`) that schedules `drill-reminder.sh` at `0 8 1 * *`, OR the Dockerfile entrypoint writes the crontab from `CRONTAB_CONTENT` before invoking supercronic
- [x] `CRONTAB_CONTENT` env var removed from `docker-compose.backup.yml` (if dedicated crontab approach is chosen)
- [x] `backup.sh` no longer runs inside the `drill_reminder` container — verified by container log inspection
- [ ] Monthly drill reminder fires on 1st of month at 08:00 (verified by manual cron trigger test — requires live infra)

## Files likely touched
- `deploy/docker-compose.backup.yml`
- `deploy/Dockerfile.backup`
- `deploy/scripts/drill-reminder.sh`

## Test plan
**Manual:**
- Spin up `docker-compose.backup.yml` locally; inspect `drill_reminder` container logs — confirm `backup.sh` does not run; manually trigger drill cron; confirm `drill-reminder.sh` executes
- Confirm primary backup container still runs `backup.sh` at `0 0 * * *`

**Automated:**
- N/A (infra-level cron; no unit tests applicable)

## Handoff notes
Promoted from discovery: `2026-04-22-backup-drill-reminder-crontab-dead-env-var.md` (OPS-003). P1 — AC-5 of OPS-003 is silently broken and backup.sh runs twice nightly.

### 2026-04-22 — implementer (blocked: duplicate of OPS-003 work already shipped)

All code changes required by this task were already implemented and reviewed as part of OPS-003 (commit `23dad42`).

Current state of all files:

- `deploy/Dockerfile.backup` — `/etc/crontab.drill` is baked in with `0 8 1 * * /scripts/drill-reminder.sh >> /var/log/backup/drill-reminder.log 2>&1`. Default CMD runs `crontab.backup`.
- `deploy/docker-compose.backup.yml` — `drill_reminder` service has `command: ["supercronic", "/etc/crontab.drill"]`. The dead `CRONTAB_CONTENT` env var is absent. `backup.sh` is unreachable from `drill_reminder`.
- `deploy/drill-reminder.sh` — correct and unmodified from OPS-003 fix.

All three code-level acceptance criteria are already met. The only remaining AC item (manual cron trigger test) requires live infrastructure which is blocked by OPS-003 being in `blocked/` pending AWS provisioning.

**PM decision needed:** Close this task as a duplicate of OPS-003 (the discovery was promoted to a task but the work was already done in the same session). The manual verification AC item should be folded back into OPS-003's test plan once the live infra is provisioned.
