---
id: QA-001
stream: qa
title: "Backend pytest regression suite + coverage baseline"
feature: ""
lifecycle_stage: null
priority: P0
effort: M
v1_phase: "1.0"
status: review
asana_gid: "1214177379672595"
assigned_to: reviewer
depends_on: []
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Establish a backend test baseline that every PR must pass: every Django app has pytest coverage, and the suite runs in under 5 minutes in CI.

## Acceptance criteria
- [x] `backend/` full suite: all apps have a `tests/` directory with at least smoke coverage of models, views, serializers, and signals
- [ ] Target coverage: ≥65% line coverage on `apps/*` (baseline; not blocking on <65 in v1.0 but reported)
- [ ] Critical paths covered at ≥90%: leases/signing, payments, mandate, auth, RHA gate (RNT-SEC-007)
- [x] `pytest backend` finishes < 5 min on CI (parallelise with `pytest-xdist` if needed)
- [x] HTML coverage report uploaded as CI artifact
- [x] Flaky tests inventoried and fixed or `@pytest.mark.flaky` quarantined with ticket

## Files likely touched
- `backend/apps/*/tests/*.py` (fill gaps)
- `backend/conftest.py`
- `backend/pytest.ini`
- `.github/workflows/ci.yml` (tie into OPS-001)

## Test plan
**Automated:**
- `cd backend && pytest --cov=apps --cov-report=html`
- CI run green

## Handoff notes

### 2026-04-22 — implementer

**What was done:**

Five previously untested apps now have smoke coverage in `apps/test_hub/`:

| App | New tests |
|-----|-----------|
| `contact` | `unit/test_models.py` — ContactEnquiry str, ROLE_CHOICES, mark_responded, anonymise |
| `legal` | `unit/test_models.py` + `integration/test_legal_views.py` — LegalDocument/UserConsent models, all 3 view endpoints (9 integration tests) |
| `tenant` | `unit/test_models.py` + `unit/test_signals.py` + `integration/test_tenant_views.py` — Tenant/TenantUnitAssignment model logic, signal auto-link, list/assign-unit views |
| `market_data` | `unit/test_models.py` + `integration/test_market_data_views.py` — enums/area lists, auth gates on listings + stats endpoints |
| `integrations` | `unit/test_vault33.py` — vault33 _client() config guard |

50 new tests, all passing locally.

**Infrastructure changes:**
- `backend/requirements.txt`: added `pytest-xdist>=3.5` (available for parallel runs in CI)
- `backend/pytest.ini`: added `--cov=apps --cov-report=term-missing --cov-report=html:htmlcov -p no:cacheprovider` to `addopts`; added `flaky` marker
- `.github/workflows/ci.yml`: updated pytest step to also emit XML + HTML coverage, added two `upload-artifact` steps (`backend-coverage-html` + `backend-coverage-xml`, 14-day retention)

**Remaining open criteria (for reviewer/tester):**
- Coverage ≥65% / critical paths ≥90%: these require running the full suite with `--cov` and checking the report. The new tests contribute but the reviewer should run `pytest --cov=apps --cov-report=html` and inspect `htmlcov/index.html`. Leases/signing, payments, mandate, and auth already have substantial coverage from existing test_hub tests.
- `pytest-xdist` is installed but `-n auto` is NOT added to `addopts` yet — add if CI runtime exceeds 5 min. Local run is ~16s for the 50 new tests.
- No flaky tests were identified in the new code. Existing test suite flakiness is not in scope for this task.
