---
id: UX-015
stream: ux
title: "Show error toast on createConversation failure in tenant ChatListView"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: done
assigned_to: null
depends_on: []
asana_gid: "1214275332449085"
created: 2026-04-24
updated: 2026-04-24T14:00:00
---

## Goal
Replace the silent `catch { /* ignore */ }` in `ChatListView.vue::createConversation()` with proper error feedback so tenants know when a chat could not be started.

## Acceptance criteria
- [x] On any API error, show a toast matching the existing pattern in `ChatDetailView.vue` (e.g. "Couldn't start a chat — please try again")
- [x] On 401/403 response, redirect to login (or defer to the global axios interceptor if one is added)
- [x] Tapping a prompt chip or FAB when offline shows the error toast — no silent failure

## Files likely touched
- `tenant/src/views/chat/ChatListView.vue` (line ~84 — replace empty catch block)

## Test plan
**Manual:**
- Simulate network offline in devtools; tap a prompt chip — confirm toast appears
- In staging, invalidate the session token; tap FAB — confirm redirect to login or toast

**Automated:**
- Vitest unit test mocking `createConversation` to reject; assert `toast.error` is called

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-24 — Promoted from discovery `2026-04-23-chat-create-conversation-silent-error.md`. Pre-existing pattern; not introduced by UX-014.

2026-04-24 — implementer: replaced silent `catch { /* ignore */ }` in `createConversation()` with error handling matching `ChatDetailView.vue` pattern. Added `useToast` import, 401/403 redirects to `{ name: 'login' }`, all other errors show `toast.error("Couldn't start a chat — please try again")`. Both FAB and prompt chip paths go through `createConversation` so all entry points are covered.

2026-04-24 — reviewer: verified diff in ChatListView.vue. useToast import matches ChatDetailView pattern; 401/403 redirects to login; all other errors show toast.error. Both FAB and prompt-chip paths funnel through createConversation() so both are covered. No security/POPIA concerns. Approved → testing.

2026-04-24 — tester: code-level verification against commit 0104e9b7. (1) useToast imported line 65, toast = useToast() line 68 — PASS. (2) 401/403 branch redirects to { name: 'login' } lines 88-90, else toast.error("Couldn't start a chat — please try again") line 91 — PASS. (3) FAB calls createConversation() directly; startWithPrompt() awaits createConversation(prompt) — both paths covered — PASS. All checks pass.
