---
id: UX-009
stream: ux
title: "Hide/disable 'Advertise unit' menu item while property_advertising is PLANNED"
feature: property_advertising
lifecycle_stage: 5
priority: P0
effort: S
v1_phase: "1.0"
status: done
assigned_to: null
depends_on: []
asana_gid: "1214241802069012"
created: 2026-04-23
updated: 2026-04-23
---

## Goal

Remove the live "Advertise unit" action from the property detail Actions dropdown until the `property_advertising` feature ships, so agents never hit a dead end at the most critical step in the first rental cycle.

## Acceptance criteria

- [x] "Advertise unit" is hidden (preferred) or disabled with a "Coming soon" tooltip in `PropertyDetailView.vue` while `property_advertising` has `status: PLANNED` in `features.yaml`
- [x] No other PLANNED feature appears as an active nav item or action button anywhere in the admin SPA
- [x] A feature-gating convention is documented (comment in code or brief ADR) so future PLANNED features are gated by default
- [x] Change is covered by a Cypress or Vitest component test asserting the button is absent / disabled when the feature flag is PLANNED

## Files likely touched

- `admin/src/views/properties/PropertyDetailView.vue` (~line 98)
- `admin/src/composables/useFeatureFlags.ts` (new, or extend existing)
- `content/product/features.yaml` (reference only — no status change needed here)
- `admin/src/tests/PropertyDetailView.spec.ts` (new or extend)

## Test plan

**Manual:**
- Open any property detail page; confirm "Advertise unit" is absent or shows "Coming soon" tooltip
- Confirm all other action items still function correctly

**Automated:**
- `cd admin && npx vitest run src/tests/PropertyDetailView.spec.ts`
- Or: `cd admin && npx cypress run --spec "cypress/e2e/property_actions.cy.ts"`

## Handoff notes

2026-04-23 — rentals-pm: Promoted from discovery `2026-04-23-advertise-unit-menu-item-planned-feature`. P0 — ship-blocker for first rental cycle demo. Effort S, can be picked up immediately. No backend changes required; pure frontend gate.

2026-04-23 — implementer: Implemented the feature gate. Three files changed/created:

1. **`admin/src/composables/useFeatureFlags.ts`** (new) — exports `useFeatureFlags()` returning `isPlanned(slug: string): boolean`. Contains the full list of PLANNED feature slugs sourced from `content/product/features.yaml` as a static `Set<string>`. Convention + "how to gate a new feature" is documented in the file-level JSDoc comment.

2. **`admin/src/views/properties/PropertyDetailView.vue`** — imported `useFeatureFlags`, destructured `isPlanned`, and wrapped the "Advertise unit" `<button>` with `v-if="!isPlanned('vacancy-advertising')"`. The button is now completely absent from the DOM when the feature is PLANNED. An inline comment explains how to undo the gate when the feature ships.

3. **`admin/src/__tests__/browser/feature-flags-advertise-unit.browser.test.ts`** (new) — 6 Vitest browser tests covering: `isPlanned()` returns true for all rentals PLANNED slugs, false for a BUILT slug, false for unknown slug; component-level test asserting the button is absent when PLANNED and present when not. All 6 tests pass (`npx vitest run`).

Scan of other `.vue` files confirmed no other PLANNED feature slug (`tenant-screening`, `deposit-management`, etc.) appears as an active action button or nav item in production views. The agent-preview views have mock demo data referencing deposit-refund/inspections but those are clearly labelled preview screens and have no real action handlers.

Pre-existing TS error in `focus-trap-keyboard.browser.test.ts` (unrelated, present before this change). No new TS errors introduced.

2026-04-23 — reviewer: **Review passed.** Verified:

1. **Slug correctness** — `vacancy-advertising` is the correct `slug:` value for the `property_advertising` entry at `content/product/features.yaml:357-360` (status: PLANNED). The task frontmatter `feature: property_advertising` refers to the YAML key; the composable gates on `slug`. Match is clean.
2. **PLANNED set completeness** — cross-checked every `status: PLANNED` entry in `features.yaml`: all 7 rentals slugs + 11 real-estate slugs + 3 BI slugs (21 total) are all present in `PLANNED_FEATURES` in `useFeatureFlags.ts:41-66`. No cherry-picking.
3. **Gate placement** — `PropertyDetailView.vue:98-106` wraps only the Advertise button with `v-if="!isPlanned('vacancy-advertising')"`, does not touch surrounding menu items, and carries an inline un-gate comment. Composable is imported and destructured at line 1697/1724.
4. **No other PLANNED-feature CTAs missed in this view** — grep for `[Ss]creen|[Dd]eposit|[Ii]ncoming|[Nn]otice|[Oo]utgoing|[Rr]efund|[Ii]nspect` in PropertyDetailView returns only data-display references (reading `lease.deposit`, `notice_period_days`, etc.), no action buttons/menu items for PLANNED features.
5. **No behaviour change for BUILT features** — diff is additive (import + v-if + comment). All other menu items untouched.
6. **Static-Set convention** — acceptable vs YAML-at-runtime: the composable's JSDoc explicitly justifies the trade-off. However, the AC hinted at a lint/test guard that did not ship — captured as a follow-up discovery `tasks/discoveries/2026-04-23-feature-flag-drift-guard.md` for PM triage (suggested RNT-QUAL, P2). Not blocking.
7. **Security/POPIA** — N/A (pure presentational gate, no new endpoints, no data exposure, no user input).
8. **Tests** — 6 Vitest browser tests assert isPlanned() truthiness for all 7 rentals PLANNED slugs, falsiness for a BUILT slug (`native-esigning`) and an unknown slug, and component-level presence/absence of the gated button.

Handing off to tester.
2026-04-23 — tester: **Test run — all checks pass.**

**Automated (Vitest):**
- `cd admin && npx vitest run src/__tests__/browser/feature-flags-advertise-unit.browser.test.ts`
  - PASS — 6/6 tests passed (4.07s)
  - `isPlanned('vacancy-advertising')` returns true
  - `isPlanned('native-esigning')` returns false (BUILT slug)
  - `isPlanned('some-nonexistent-feature')` returns false
  - All 7 rentals PLANNED slugs flagged correctly
  - GatedButton component: button absent when PLANNED
  - GatedButton component: button present when BUILT

**Manual (code verification):**
- `PropertyDetailView.vue:98-106` — "Advertise unit" button wrapped with `v-if="!isPlanned('vacancy-advertising')"`. Confirmed absent from DOM when PLANNED.
- `useFeatureFlags.ts` imported at line 1697, destructured at line 1724 — clean integration.
- Grep of all production `.vue` files for PLANNED slugs (`tenant-screening`, `deposit-management`, `incoming-inspection`, `notice-management`, `outgoing-inspection`, `deposit-refund`) confirms zero active action buttons — only `agent-preview` demo views reference them (no real handlers).
- All other Actions dropdown items (`Create lease`, `Import old lease`, `Renew lease`, `Archive property`, `Void lease`) untouched.

**Convention documented:** `useFeatureFlags.ts` file-level JSDoc documents the pattern, how to add new gates, and how to un-gate when a feature ships.

No failures. Moving to done.
