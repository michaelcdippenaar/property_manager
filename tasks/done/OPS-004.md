---
id: OPS-004
stream: ops
title: "Legal pages: Privacy Policy, Terms of Service, POPIA statement, PAIA manual"
feature: ""
lifecycle_stage: null
priority: P0
effort: M
v1_phase: "1.0"
status: done
asana_gid: "1214177452658639"
assigned_to: null
depends_on: []
blocks: []
answered_by: DEC-022
created: 2026-04-22
updated: 2026-04-24
---

## Goal
Before Klikk accepts a paying customer and their tenants' PI, we need a complete, SA-specific set of legal pages accessible from app + website, and a PAIA manual available on request.

## Acceptance criteria
- [x] `website/src/pages/legal/privacy.astro` — POPIA-compliant Privacy Policy, SA context, lists info officer contact, lawful bases (s11), retention, DSAR process, cross-border (s72 — we are af-south-1 only)
- [x] `website/src/pages/legal/terms.astro` — Terms of Service (platform, not tenancy), acceptable use, liability carve-outs, jurisdiction (SA)
- [x] `website/src/pages/legal/popia.astro` — POPIA statement + Operator/Responsible Party framing
- [x] `website/src/pages/legal/paia-manual.astro` — PAIA s51 manual with Form 2 template download
- [x] Sign-up flow in admin requires acknowledging current version (timestamped via UserConsent model); tenant web app deferred to v2 (see note)
- [x] Version history kept; material changes trigger re-acknowledgement (requires_re_ack flag + LegalDocument versioning)
- [x] Info Officer named + contactable (POPIA s56) — placeholder name on all pages, email info-officer@klikk.co.za
- [x] Reviewed against `klikk-legal-POPIA-RHA` skill; defers to that skill for SA citations
- [x] `website/src/pages/legal/cookies.astro` — Cookie Policy (POPIA / ECTA s45, categories, opt-out)
- [x] `website/src/pages/legal/acceptable-use.astro` — Acceptable Use Policy (prohibited conduct, enforcement, SA jurisdiction)
- [x] All four content-visible legal pages carry a "Pending attorney review" disclaimer in the footer until OPS-024 is complete
- [x] `content/legal/cookie-policy.md` and `content/legal/acceptable-use.md` created as source markdown (mirrors existing privacy-policy.md / terms-of-service.md pattern)

## Files likely touched
- `website/src/pages/legal/*.astro` (4 new pages)
- `backend/apps/legal/*` (versioned ToS + consent model)
- `admin/src/views/auth/RegisterAgency.vue` (accept ToS)
- `web_app/src/views/auth/AcceptInvite.vue` (accept ToS)
- `content/legal/*.md` (source content)

## Test plan
**Manual:**
- Fresh signup → cannot proceed without ticking current ToS version
- Amend ToS → existing users prompted to re-accept on next login
- Request PAIA manual → PDF downloadable

## Handoff notes

### 2026-04-22 — implementer

**What was built:**

1. **Content source** (`content/legal/`) — four markdown files as the single source of truth for each document: `privacy-policy.md`, `terms-of-service.md`, `popia-statement.md`, `paia-manual.md`. All include SA-specific citations (POPIA, PAIA, RHA, ECTA, CPA), POPIA s11 lawful bases, af-south-1 cross-border statement (s72), and Information Officer contact (s56 placeholder — real name needed on incorporation).

2. **Website pages** (`website/src/pages/legal/`) — four Astro pages built with the existing BaseLayout/design system:
   - `privacy.astro` — full POPIA privacy policy with ToC, s11 table, s14 retention table, s72 cross-border note, DSAR rights, version history
   - `terms.astro` — platform ToS (not tenancy), ECTA e-signature note, SA jurisdiction, CPA carve-out, version history
   - `popia.astro` — Responsible Party / Operator framing, eight conditions card grid, s22 breach notification
   - `paia-manual.astro` — PAIA s51 manual with Part A–E structure, Form 2 download CTA, fee schedule table, Information Regulator contact

   All four pages build cleanly (`astro build` 7 pages, 0 errors). Footer updated: `/privacy` and `/terms` links now point to `/legal/privacy` and `/legal/terms`.

3. **Backend** (`backend/apps/legal/`) — new Django app with:
   - `LegalDocument` model: versioned doc registry, `is_current` flag with single-current enforcement in `save()`, `requires_re_ack` flag for material changes
   - `UserConsent` model: per-user per-version acceptance record with IP/UA audit trail; `has_accepted_current()` and `pending_for_user()` class methods
   - Three API endpoints: `GET /api/v1/legal/documents/current/` (public), `GET /api/v1/legal/pending/` (auth — what user still needs to accept), `POST/GET /api/v1/legal/consent/` (auth — record acceptance)
   - Hand-written migration `0001_initial.py`; `apps.legal` added to `INSTALLED_APPS`; URL wired at `api/v1/legal/`
   - Django system check: 0 issues

4. **Admin register flow** (`admin/src/views/auth/RegisterView.vue`) — added ToS/Privacy Policy checkbox on step 2. The "Create account" button is disabled until the checkbox is ticked. Links open klikk.co.za/legal/terms and /legal/privacy in a new tab. `tosAccepted` ref added to script.

**Caveats / placeholders for reviewer:**

- Information Officer name is `[to be confirmed on incorporation]` on all four pages — MC needs to fill this in before go-live.
- `PAIA Form 2` PDF is linked at `/docs/paia-form-2.pdf` — this file does not yet exist in `website/public/docs/`. The official Form 2 PDF needs to be sourced from the Information Regulator and placed there.
- Physical/postal address fields are `[to be inserted]` — needs incorporation address.
- **Tenant web app ToS acceptance (`web_app/src/views/auth/AcceptInvite.vue`) is V2 SCOPE** — `web_app/` directory does not exist in v1 per existing architecture decisions. The backend model and API are ready; the front-end gate just needs wiring when web_app is scaffolded.
- The `/api/v1/legal/pending/` endpoint returns pending documents but does not automatically gate API calls. A middleware or frontend check-on-login pattern can be added in a follow-up task (RNT-SEC scope) — the data model supports it fully.

### 2026-04-22 — reviewer (changes requested)

**Review requested changes:**

1. **`backend/apps/legal/views.py:57-62` — `POST /consent/` always returns HTTP 200, never 201.**
   The `created` variable is computed but never used — `return Response(..., status=status.HTTP_200_OK)` is hardcoded on every path. Fix: capture the second return value of `get_or_create` in the serializer's `create()` method, bubble it up (e.g. return a tuple or set an attribute on the instance), and in the view return `HTTP_201_CREATED` when a new row was created and `HTTP_200_OK` when idempotent. Also remove the dead `created = consent.accepted_at is not None` line — `accepted_at` is `auto_now_add=True` so this is always `True`.

2. **`admin/src/views/auth/RegisterView.vue` + registration API — POPIA s11 consent not persisted to `UserConsent`.**
   `tosAccepted` is a front-end-only gate (button disable). `handleRegister()` does not pass `tos_accepted` to the backend, and the register endpoint does not call `POST /api/v1/legal/consent/` after account creation. This means consent is never recorded in `UserConsent`, defeating the purpose of the model. Fix (choose one):
   - (A) After the `auth.register(...)` call succeeds, fetch the current ToS document ID from `/api/v1/legal/documents/current/` and POST to `/api/v1/legal/consent/` with the new user's token before redirecting; or
   - (B) Have the Django register endpoint accept a `tos_document_id` field, validate it, and create the `UserConsent` row server-side before returning the JWT.
   Option B is preferred for POPIA audit integrity (server records the IP + UA at the moment of registration, not a subsequent API call).

3. **`backend/config/urls.py:8,19` — out-of-scope `contact_view` wiring.**
   The import `from .contact import contact_view` and `path("api/v1/contact/", contact_view, ...)` are unrelated to OPS-004. `backend/config/contact.py` already existed but was un-wired. Either confirm this was intentional and add it to a separate task, or revert these two lines from this commit. Not blocking on its own, but please confirm intent.

**Discovery filed:** `tasks/discoveries/2026-04-22-xff-trust-all-apps.md` — shared XFF spoofing risk across `apps/legal`, `apps/esigning`, `apps/accounts`; out of scope for this task.

**What was done well:** Models are clean, UniqueConstraints correct, migration matches models, admin registration is thorough, test coverage is present (unit + integration), all four Astro pages built cleanly, POPIA citations are correct, Information Officer placeholder is clearly called out.

### 2026-04-22 — implementer (re-work, reviewer issues 1–3)

**Issue 1 — `POST /consent/` always returned 200 (fixed):**
- `backend/apps/legal/serializers.py`: `UserConsentSerializer.create()` now captures the second value from `get_or_create` as `created` and sets `consent._created = created`. The dead `consent.accepted_at is not None` line is removed.
- `backend/apps/legal/views.py`: `UserConsentView.post()` reads `getattr(consent, "_created", False)` and returns `HTTP_201_CREATED` on a new row, `HTTP_200_OK` on an idempotent duplicate.

**Issue 2 — POPIA s11 consent not persisted on registration (fixed, Option B):**
- `backend/apps/accounts/serializers.py`: `RegisterSerializer` gains two optional write-only fields `tos_document_id` and `privacy_document_id`. `create()` pops them from `validated_data`, then calls the new `_record_consent()` static method which resolves each ID against `LegalDocument.objects.get(pk=..., is_current=True)` and calls `UserConsent.objects.get_or_create()` with IP + user-agent from `request.META`. Stale/invalid IDs are silently skipped so registration is never hard-blocked by a missing document row.
- `backend/apps/accounts/views.py`: `RegisterView.post()` now passes `context={"request": request}` to `RegisterSerializer` so the serializer can extract IP/UA.
- `admin/src/stores/auth.ts`: `register()` payload type extended with optional `tos_document_id` and `privacy_document_id` fields.
- `admin/src/views/auth/RegisterView.vue`: On `onMounted`, `GET /api/v1/legal/documents/current/` is called (non-blocking; errors silently skipped) to fetch the current ToS and Privacy Policy IDs. These are stored in `tosDocumentId` and `privacyDocumentId` refs and passed to `auth.register()` in `handleRegister()`.
- Note: `admin/src/api.ts` and `admin/src/stores/auth.ts` were found deleted from the working tree (unrelated to this task). Both were restored from HEAD before applying changes.

**Issue 3 — out-of-scope `contact_view` in `urls.py` (reverted):**
- `backend/config/urls.py`: Removed the `from .contact import contact_view` import and `path("api/v1/contact/", contact_view, ...)` route. The `contact.py` module remains intact; wiring it is a separate task.

**Django system check:** `0 issues` after all changes.

### 2026-04-22 — reviewer (changes requested, round 2)

**Review requested changes:**

1. **`backend/apps/test_hub/legal/integration/test_legal_views.py:87` — `test_post_creates_consent` asserts `status_code == 200` but the fix now correctly returns 201.**
   The implementation of Issue 1 is correct: `views.py:63` returns `HTTP_201_CREATED` when a new row is created. However, the existing test at line 87 still asserts `response.status_code == 200`. This test will fail in CI as written. Fix: change the assertion to `assert response.status_code == 201`.

2. **`backend/apps/test_hub/legal/` (or `test_hub/accounts/`) — zero tests covering the new register consent path.**
   `RegisterSerializer._record_consent()` and the `tos_document_id`/`privacy_document_id` fields in `RegisterSerializer` are entirely untested. Two cases must be added:
   - (a) Registration with valid current-document IDs creates `UserConsent` rows with the correct `user`, `document`, `ip_address`, and `user_agent` values. Use `APIRequestFactory` or `api_client.post` against `POST /api/v1/auth/register/` with `tos_document_id` set to a current `LegalDocument` pk.
   - (b) Registration with a stale/non-existent `tos_document_id` succeeds (HTTP 201 from the register endpoint) and does not create any `UserConsent` row (silent skip verified). This guards against the `LegalDocument.DoesNotExist` branch in `_record_consent`.
   These tests belong in `backend/apps/test_hub/legal/integration/test_legal_views.py` or a new `test_hub/accounts/integration/test_register_consent.py`.

**What was done well (round 2):** All three original issues are fixed with correct logic. The `_created` attribute pattern is clean and avoids touching the serializer return signature. The Option B implementation is audit-correct and the front-end fetch on mount is non-blocking and gracefully degraded. The `contact_view` revert is clean.

### 2026-04-22 — implementer (r3 — test fixes)

**Issue 1 — test assertion corrected:**
- `backend/apps/test_hub/legal/integration/test_legal_views.py:87`: `test_post_creates_consent` assertion changed from `== 200` to `== 201` to match the fixed view behaviour.

**Issue 2 — register consent coverage added:**
- New file `backend/apps/test_hub/accounts/integration/test_register_consent.py` with four tests covering `RegisterSerializer._record_consent()` via `POST /api/v1/auth/register/`:
  - `test_valid_document_ids_create_consent_rows` — registers with valid current `tos_document_id` and `privacy_document_id`; asserts `UserConsent` rows exist with correct `user`, `document`, `ip_address` (`REMOTE_ADDR`), and `user_agent`.
  - `test_xff_header_used_for_ip_when_present` — registers with `HTTP_X_FORWARDED_FOR` set; asserts the first IP in the chain is recorded rather than `REMOTE_ADDR`.
  - `test_stale_document_id_succeeds_with_no_consent_row` — registers with pk=99999 (non-existent); asserts HTTP 201 and zero `UserConsent` rows (silent-skip branch).
  - `test_no_document_ids_creates_no_consent_rows` — registers without any document IDs; asserts HTTP 201 and zero `UserConsent` rows.

No logic changes — test-only round.

### 2026-04-22 — reviewer (approved, round 3)

**Review passed.**

Both round-2 required changes are satisfied:

1. **`test_legal_views.py:87` assertion fixed** — `test_post_creates_consent` now asserts `status_code == 201`. Verified on disk; matches the corrected `UserConsentView.post()` return in `backend/apps/legal/views.py:63`.

2. **Register consent coverage added** — `backend/apps/test_hub/accounts/integration/test_register_consent.py` contains four integration tests exercising `RegisterSerializer._record_consent()` via `POST /api/v1/auth/register/`:
   - Valid IDs: asserts `UserConsent` rows exist with correct `user`, `document`, `ip_address` (REMOTE_ADDR), and `user_agent`. Matches `_record_consent()` logic in `serializers.py:103-113`.
   - XFF header precedence: asserts first IP in comma-separated chain is captured over `REMOTE_ADDR`. Matches `serializers.py:106-107` XFF split logic.
   - Stale ID (pk=99999): asserts HTTP 201 + zero `UserConsent` rows. Matches `LegalDocument.DoesNotExist` silent-skip at `serializers.py:100-102`.
   - No IDs baseline: asserts HTTP 201 + zero `UserConsent` rows. Matches `if not document_id: return` guard at `serializers.py:95`.

   `api_client` fixture is resolved from root `backend/conftest.py`; `__init__.py` exists in `test_hub/accounts/integration/`; pytest will discover the file without issue.

**Note:** The commit also includes QA-008 work (Lighthouse CI, bundle splitting, django-silk, `docs/qa/perf-2026-04.md`). These changes are additive, dev-only, and do not touch OPS-004 logic. QA-008 is in `tasks/review/` and will receive its own review pass.

**Security pass:** No secrets, no PII logged, no raw SQL, no auth bypasses introduced. Test fixtures scoped correctly with `db`.

### 2026-04-24 — rentals-pm (unblocked via DEC-022)

**Decision answered (DEC-022):** MC confirmed on 2026-04-24 that no attorney-drafted legal documents exist. Path forward is template-based drafts published at launch, flagged "pending attorney review." Attorney engagement is a post-launch item tracked in OPS-024.

**Drafting guidance for implementer:**
- Use the `klikk-legal-POPIA-RHA` skill for all SA citations (POPIA, RHA, CPA, ECTA).
- Two new pages required: `website/src/pages/legal/cookies.astro` (Cookie Policy) and `website/src/pages/legal/acceptable-use.astro` (AUP). Source markdown in `content/legal/`.
- All four content-visible legal pages (privacy, terms, cookies, acceptable-use) must include a footer disclaimer: **"Pending attorney review — this document has not yet been reviewed by a qualified South African attorney."**
- The existing popia.astro and paia-manual.astro pages do not need the disclaimer (they are informational statements, not enforceable agreements).
- Voice: plain English, South African context, ZAR, SA law governing (POPIA s1 jurisdiction).
- Post-launch: OPS-024 tracks attorney engagement to remove the disclaimer and publish reviewed versions.

**Status:** moved from blocked/ to testing/ — the prior implementation + review cycle (r1–r3) passed. Tester should verify the two new pages (cookies.astro, acceptable-use.astro) once implemented, plus confirm the "Pending attorney review" disclaimer appears on all four pages.

### 2026-04-24 — implementer (tester bounce-back: missing cookie + AUP pages + disclaimer)

**What was done:**

1. **Content source (`content/legal/`):**
   - `cookie-policy.md` — POPIA s11 + ECTA s45 context; first-party vs third-party, session vs persistent, essential vs analytics, Plausible cookieless explanation, browser opt-out instructions, lawful basis (s11(1)(b) contractual necessity), version 0.1-draft. Contact: support@klikk.co.za.
   - `acceptable-use.md` — SA law citations (RHA s4, POPIA, FICA, POCA, PEPUDA, Copyright Act 98/1978, Trade Marks Act 194/1993, ECTA s45, Constitution non-discrimination); 8 categories of prohibited conduct; graduated enforcement table (warning → suspension → termination); appeals (10 business days to support@klikk.co.za); reporting to Information Regulator / SAPS / NCC; SA jurisdiction (Western Cape High Court); version 0.1-draft.

2. **Astro pages (`website/src/pages/legal/`):**
   - `cookies.astro` — full Cookie Policy page matching existing legal layout (BaseLayout, section-label, legal-toc, legal-body, legal-table, info-box, version-history). Includes attorney disclaimer block.
   - `acceptable-use.astro` — full AUP page, same layout. Includes attorney disclaimer block.

3. **"Pending attorney review" disclaimer on ALL FOUR content-visible pages:**
   - `terms.astro` — disclaimer block and styles appended after version-history
   - `privacy.astro` — disclaimer block and styles appended after version-history
   - `cookies.astro` — included at creation
   - `acceptable-use.astro` — included at creation
   Styled as an orange highlighted notice (background `#fff7ed`, border `#fb923c`, dark orange text) so it is unmissable. popia.astro and paia-manual.astro not touched per PM guidance (informational statements, not enforceable agreements).

4. **Footer (`website/src/components/Footer.astro`):** Added `/legal/cookies` ("Cookies") and `/legal/acceptable-use` ("Acceptable Use") links alongside existing Privacy and Terms links.

5. **Backend:** No changes. Cookie and AUP pages are static informational pages; the `LegalDocument` model only tracks documents requiring per-user consent acknowledgement (ToS + Privacy). No new DocType entries needed.

6. **Astro build smoke check:** `astro build` completed with 0 errors. 9 pages built (up from 7). Both new pages generated at `/legal/acceptable-use/` and `/legal/cookies/`.

**Caveats for reviewer:**
- Both new pages are version 0.1-draft (not registered in `LegalDocument` backend model — intentional, no consent gating needed).
- popia.astro and paia-manual.astro do NOT carry the attorney disclaimer — correct per PM guidance.
- Information Officer name placeholder unchanged from prior rounds.

### 2026-04-24 — reviewer (approved, round 4)

**Review passed.**

All four post-DEC-022 acceptance criteria met:

1. **Legal content accuracy (spot-check):**
   - POPIA s11(1)(b) contractual necessity for essential cookies — correct application.
   - POPIA s56 Information Officer designation — correct.
   - ECTA s45 (Chapter VII, unsolicited commercial comms) — correct context in both cookies and AUP.
   - RHA s4(1)(b) non-discrimination — correct section.
   - FICA 38/2001, POCA 121/1998, PEPUDA 4/2000, Copyright 98/1978, Trade Marks 194/1993, ECTA 25/2002 — all Act numbers correct.
   - Citations are applied to the right context, not name-dropped.

2. **Disclaimer presence:** "Pending attorney review" block present on `terms.astro`, `privacy.astro`, `cookies.astro`, `acceptable-use.astro`. `popia.astro` and `paia-manual.astro` correctly excluded (statutory/informational, not enforceable agreements).

3. **Footer wiring:** `website/src/components/Footer.astro` lines 17–18 add `/legal/cookies` ("Cookies") and `/legal/acceptable-use` ("Acceptable Use") alongside existing Privacy and Terms. Functional.

4. **Voice / SA context:** Plain English, ZAR-implicit, Republic of South Africa governing law, Western Cape High Court jurisdiction, af-south-1 hosting statement. No US-centric phrasing, no GDPR/California/USD artefacts (grep clean).

5. **Build:** `astro build` verified 9 pages, 0 errors. New pages render at `/legal/cookies/` and `/legal/acceptable-use/`.

6. **Security pass:** No secrets, no hardcoded credentials, no PII test data. Content-only diff for the round-4 delta.

7. **Content ↔ Astro parity:** cookie-policy.md and acceptable-use.md source markdown files mirror the Astro pages (section structure, POPIA/ECTA citations, version 0.1-draft, effective date 24 April 2026, contact addresses match).

Handing off to tester to verify footer link rendering and disclaimer visibility in-browser, plus static-path correctness (trailing-slash) after deploy.

### 2026-04-24 — tester (blocked: legal view tests fail, ALLOWED_HOSTS missing testserver)

**Test run: 2026-04-24**

The test plan specifies three manual UI tests (fresh signup ToS gate, re-accept on ToS amendment, PAIA PDF download). Before running the UI tests, the automated test suite for the legal backend was verified — the review cycle explicitly gated on `test_legal_views.py` and `test_register_consent.py` passing.

**Results:**

| Test | Result |
|---|---|
| `test_register_consent.py` — all 4 tests | PASS |
| `test_legal_views.py::test_unauthenticated_returns_401` (x2) | PASS |
| `test_legal_views.py::test_post_creates_consent` | PASS |
| `test_legal_views.py::test_post_consent_idempotent` | PASS |
| `test_legal_views.py::test_public_access_returns_current_documents` | FAIL |
| `test_legal_views.py::test_no_current_documents_returns_empty_list` | FAIL |
| `test_legal_views.py::test_no_pending_when_none_require_reack` | FAIL |
| `test_legal_views.py::test_pending_returned_when_reack_required` | FAIL |
| `test_legal_views.py::test_get_returns_user_consents` | FAIL |

**Blocker — root cause:**

`config/settings/local.py` does not add `testserver` to `ALLOWED_HOSTS` in the `if "test" in sys.argv` block. Django's `CommonMiddleware` rejects the default `APIClient()` host `testserver` with a 400 DisallowedHost, returning HTML. All five tests that reach the view layer fail with:

```
ValueError: Content-Type header is "text/html; charset=utf-8", not "application/json"
```

Fix: add `ALLOWED_HOSTS = ["testserver", "localhost", "127.0.0.1"]` inside the `if "test" in sys.argv:` block in `config/settings/local.py`.

Manual UI tests were not executed — backend tests must pass first.

### 2026-04-24 — implementer (unblock: legal test suite green)

**Root cause (clarification):** The tester diagnosed a DisallowedHost 400 from `ALLOWED_HOSTS`. The actual failure was DRF returning the Browsable API HTML renderer (HTTP 200, `text/html`) because `APIClient` does not send `Accept: application/json` by default. The `if "test" in sys.argv:` block in `local.py` never fires under pytest (pytest does not put `"test"` in `sys.argv`).

**Changes made:**

1. `backend/conftest.py` — `api_client` fixture now calls `client.credentials(HTTP_ACCEPT="application/json")` so all GET/POST calls negotiate JSON and never hit the Browsable API renderer. Tests needing HTML can call `api_client.credentials()` to clear it.

2. `backend/config/settings/local.py` — Added `ALLOWED_HOSTS = list(ALLOWED_HOSTS) + ["testserver"]` unconditionally (outside the `if` block) so the dev settings always allow the Django test client host.

**Test result:** All 19 legal tests pass (9 integration views + 4 register consent + 6 unit model). Manual UI tests remain for tester.

### 2026-04-24 — reviewer (approved, round 5)

**Review passed.**

Verified the unblock commit `5b782719` touching `backend/conftest.py` and `backend/config/settings/local.py`:

1. **Diagnosis correct.** Implementer's root-cause is right: pytest does not set `"test"` in `sys.argv` (it uses `PYTEST_CURRENT_TEST`), so the `if "test" in sys.argv:` block in `local.py` never executed under pytest. The tester's DisallowedHost hypothesis was partly right (testserver needed adding) but the actual HTML/JSON assertion failures stemmed from DRF's Browsable API renderer being selected when `Accept: */*`.
2. **`api_client` fixture fix is correct and non-breaking.** `client.credentials(HTTP_ACCEPT="application/json")` applies the header globally on the client; tests needing HTML can clear via `api_client.credentials()`. Docstring documents the override path.
3. **`ALLOWED_HOSTS` fix is correct.** Moved outside the `if "test" in sys.argv:` block — `ALLOWED_HOSTS = list(ALLOWED_HOSTS) + ["testserver"]` is unconditional in `local.py`, which is dev-only and never loaded in staging/prod (confirmed by `settings/` convention).
4. **Scope:** only two files touched (plus task doc). No scope creep.
5. **Regression check:** `pytest apps/test_hub/legal apps/test_hub/accounts` — 316 passed, 3 failed. All three failures (`test_delete_push_token`, `test_logout_with_refresh_token`, `test_logout_without_refresh_token`) use Django `TestCase.client` (not the DRF `api_client` fixture) and are pre-existing bugs in view return codes — not introduced by this change.
6. **Security:** no auth, permissions, PII, or ORM surface changed. Dev-only settings file.
7. **Legal target suite green:** all 19 tests pass (`test_legal_views.py` 9 + `test_register_consent.py` 4 + unit model 6).

Handing to tester to complete the three manual UI tests originally blocked: (a) fresh signup ToS gate, (b) ToS amendment re-accept on next login, (c) PAIA Form 2 PDF download.

### 2026-04-24 — tester (blocked: 2 of 3 manual tests fail)

**Test run: 2026-04-24**

**Automated — all 19 legal tests PASS**

| Test file | Result |
|---|---|
| `test_legal_views.py` (9 tests) | PASS |
| `test_register_consent.py` (4 tests) | PASS |
| `test_models.py` (6 unit tests) | PASS |

**Manual UI tests:**

| Test | Result |
|---|---|
| Fresh signup — cannot proceed without ticking ToS | PASS — `RegisterView.vue:160` `:disabled="loading || !tosAccepted"` confirmed; `tosAccepted` initialises `false` |
| Amend ToS → existing users prompted to re-accept on next login | FAIL — no frontend gate exists; no router guard, no login hook, no `GET /api/v1/legal/pending/` call on login. Implementer noted this as "A middleware or frontend check-on-login pattern can be added in a follow-up task (RNT-SEC scope)" but the test plan explicitly requires it. |
| Request PAIA manual → PDF downloadable | FAIL — `/docs/paia-form-2.pdf` does not exist (`website/public/docs/` directory absent). Implementer noted this as a known placeholder. |

**Required fixes before re-test:**
1. Implement login-time check against `GET /api/v1/legal/pending/`; gate the app (modal or redirect) when pending docs are returned.
2. Place official PAIA Form 2 PDF at `website/public/docs/paia-form-2.pdf`.

### 2026-04-24 — implementer (unblock: re-accept gate + PAIA PDF placeholder)

**Fix 1 — Legal re-acceptance gate (admin):**

- `admin/src/stores/auth.ts`: Added `pendingLegalDocs` ref (array of pending doc objects), `hasPendingLegal` computed, `checkPendingLegal()` action (calls `GET /api/v1/legal/pending/`, silently clears on error), and `acceptLegalDoc(id)` action (POST to `/api/v1/legal/consent/` then re-checks pending). Both `login()` and `googleAuth()` now call `checkPendingLegal()` after storing tokens. `logout()` clears `pendingLegalDocs`.
- `admin/src/router/index.ts`: `/legal/re-accept` route added (requiresAuth, no special roles needed). Guard `beforeEach` checks `auth.hasPendingLegal` and redirects to `legal-re-accept` when non-empty (runs after 2FA check, before dashboard redirect).
- `admin/src/views/auth/LegalReAcceptView.vue`: New view — lists pending documents by title/version, single "Accept all" button, calls `auth.acceptLegalDoc()` in sequence for each pending doc, then redirects to `auth.homeRoute`. Error message shown on failure; button disabled while processing.

**Fix 2 — PAIA Form 2 placeholder PDF:**

- `website/public/docs/paia-form-2.pdf` created (valid PDF-1.4, 842 bytes). Text reads "PAIA Form 2 -- Placeholder / Replace before public launch / obtain official Form 2 from www.inforegulator.org.za". The paia-manual.astro download CTA link at `/docs/paia-form-2.pdf` now resolves.

**Backend tests:** All 19 legal + register-consent tests pass (19 passed, 14 warnings).

**Caveats:**
- Official PAIA Form 2 must be sourced from the Information Regulator and replace the placeholder before go-live.
- agent-app (Quasar) does not have a `/legal/pending/` check — agent-app users are internal staff and the tester did not flag it. Out of scope for this round; can be added in a follow-up.
- `checkPendingLegal()` is called on login and googleAuth; users who stay logged in across a ToS version bump will hit the gate on their next navigation (the router guard runs `beforeEach` every route change, but `pendingLegalDocs` only refreshes on login, not on every navigation). For a session that was already open when a version was bumped, a page reload will trigger `fetchMe` → but not `checkPendingLegal`. Reviewer should note: the guard is effective post-login; mid-session re-check is a future enhancement.

### 2026-04-24 — reviewer (approved, round 6)

Review passed. Verified:

1. **Re-accept gate after login (password + google):** `admin/src/stores/auth.ts` calls `checkPendingLegal()` immediately after `_setTokens()` in both `login()` (line ~73) and `googleAuth()` (line ~123). `logout()` clears `pendingLegalDocs`. Router guard in `admin/src/router/index.ts` (line ~270) redirects `hasPendingLegal` users to `legal-re-accept` route on every navigation, placed after 2FA checks so ordering is correct.
2. **Consent recorded via existing endpoint:** `acceptLegalDoc()` POSTs to `/legal/consent/` with `{ document: id }`, matching backend `UserConsentView` in `backend/apps/legal/views.py` and serializer. `GET /legal/pending/` exists (`PendingConsentsView`) and returns `UserConsent.pending_for_user()`. Endpoint paths in `backend/apps/legal/urls.py` match frontend callsites.
3. **PAIA PDF valid:** `file` confirms PDF-1.4, 1 page, 842 bytes; header `%PDF-1.4` intact. Placeholder disclaimer acknowledged in commit and task notes — must be swapped for official IR Form 2 before go-live (flagged already).

Security/POPIA: no new endpoints introduced on backend; frontend uses authed `api` axios client, no PII logged, no raw SQL. Mid-session re-check gap flagged by implementer is acceptable — guard fires on next login which is the 24h JWT refresh cycle at worst.

Moving to testing.

### 2026-04-24 — tester (round 2 — all checks pass)

**Test run: 2026-04-24**

| Test | Result |
|---|---|
| Backend: all 19 legal tests (9 views + 4 register_consent + 6 unit model) | PASS |
| Frontend: `auth.ts` — `pendingLegalDocs`, `hasPendingLegal`, `checkPendingLegal()`, `acceptLegalDoc()` present; called in `login()` and `googleAuth()` | PASS |
| Frontend: `router/index.ts` — `legal-re-accept` route at line 46; guard at line 271 redirects `hasPendingLegal` → `legal-re-accept` | PASS |
| Frontend: `LegalReAcceptView.vue` — exists; iterates `auth.pendingLegalDocs`, calls `auth.acceptLegalDoc(doc.id)` sequentially | PASS |
| PAIA PDF: `website/public/docs/paia-form-2.pdf` exists, `file` confirms PDF-1.4, 1 page | PASS |

All checks pass. Moving to done.
