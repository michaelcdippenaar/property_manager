---
id: QA-019
stream: rentals
title: "tremly-mcp PLAN.md: update stale esigning_public_link contract reference"
feature: klikk_mcp
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: done
assigned_to: null
depends_on: []
asana_gid: "1214235790283012"
created: 2026-04-23
updated: 2026-04-23
---

## Goal
Update `services/tremly-mcp/PLAN.md` to reflect the new `esigning_public_link` tool contract (`{id, signer_role}`) introduced in QA-017, and do a pass for any other stale esigning contract references.

## Acceptance criteria
- [x] `services/tremly-mcp/PLAN.md` line 153: `esigning_public_link` row updated from `{id: number, submitter_id: number}` to `{id: number, signer_role: string}`
- [x] Full scan of PLAN.md for any other esigning tool references that still mention `submitter_id` — update or remove them
- [x] No other esigning contract documentation in `services/tremly-mcp/` references the old schema

## Files likely touched
- `services/tremly-mcp/PLAN.md`

## Test plan
**Manual:**
- Read `PLAN.md` line 153 and confirm the schema matches the live code in `services/tremly-mcp/lib/tools/esigning.mjs`

**Automated:**
- `grep -n "submitter_id" services/tremly-mcp/PLAN.md` — expect 0 results after fix

## Handoff notes
2026-04-23 — Promoted from discovery `2026-04-23-tremly-mcp-plan-md-stale-submitter-id-reference.md` (found by rentals-reviewer during QA-017). Trivial doc-only fix; low effort.

2026-04-23 — implementer: Updated `services/tremly-mcp/PLAN.md` line 153 — changed `esigning_public_link` input schema from `{id: number, submitter_id: number}` to `{id: number, signer_role: string}` to match the live Zod schema in `services/tremly-mcp/lib/tools/esigning.mjs`. Full scan confirmed no other `submitter_id` occurrences anywhere in `services/tremly-mcp/`. No other esigning contract documentation in that directory referenced the old schema.

2026-04-23 — reviewer: Review passed. Verified (1) PLAN.md line 153 now shows `{id: number, signer_role: string}` matching the live Zod schema in `services/tremly-mcp/lib/tools/esigning.mjs:73` (`{ id: z.coerce.number(), signer_role: z.string()... }`); (2) grep for `submitter_id` across `services/tremly-mcp/` returns zero matches; (3) no other stale esigning contract references in that directory. Doc-only change, no code/security impact. Commit clean aside from an unrelated discovery file carried along from UX-009 — harmless.

### Test run 2026-04-23

**Manual — PLAN.md line 153 vs live Zod schema:**
- PLAN.md line 153: `| esigning_public_link | Create public link | {id: number, signer_role: string} |` — PASS
- Live code at `services/tremly-mcp/lib/tools/esigning.mjs:73`: `{ id: z.coerce.number(), signer_role: z.string()... }` — schemas match — PASS

**Automated — grep for `submitter_id` in PLAN.md:**
- `grep -n "submitter_id" services/tremly-mcp/PLAN.md` — 0 results — PASS

All checks pass.
