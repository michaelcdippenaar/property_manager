---
id: OPS-029
stream: ops
title: "Fix .gitignore: exclude coverage artefacts, env files; add agent commit hygiene guidance"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: null
created: 2026-04-24
updated: 2026-04-24
---

## Goal
Ensure coverage artefacts and env files never land in commits again, and add guardrails / guidance so implementer agents scope their `git add` to task-relevant files only.

## Acceptance criteria
- [ ] `.gitignore` gains entries for `.coverage`, `backend/.coverage`, `coverage.xml`, `admin/.env.production`, `admin/.env.staging`, `backend/.env.production`, `backend/.env.staging`
- [ ] Audit whether any env files already committed contain real secrets; rotate any exposed values immediately
- [ ] Add a `docs/dev/commit-hygiene.md` (or note in CONTRIBUTING.md) instructing agents/devs to use explicit `git add <path>` not `git add -A`
- [ ] Verify the new gitignore entries remove the currently tracked artefacts from the index (`git rm --cached`)

## Files likely touched
- `.gitignore`
- `backend/.gitignore` (if present)
- `admin/.gitignore` (if present)
- `docs/dev/commit-hygiene.md` (new)

## Test plan
**Manual:**
- Run `git status` after adding gitignore entries — confirm `.coverage`, `coverage.xml` and `*.env.production/staging` no longer appear as tracked changes
- Run `git log --oneline --all -- admin/.env.production` to confirm if secrets were ever committed

**Automated:**
- None required beyond manual verification

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-24 — Promoted from discovery `2026-04-23-gtm-012-commit-included-unrelated-tracked-file-noise.md`. GTM-012 commit `de59eea9` bundled 16 files; only 2 were in scope. Coverage artefacts + env files appeared alongside unrelated app-code edits.

2026-04-24 — Review passed. Verified: (1) .gitignore entries added for all six artefacts + bonus backend/admin coverage.xml; (2) diff shows all six tracked files removed cleanly; (3) spot-checked removed backend/.env.production + .env.staging content — only non-secret config (hostnames, SES endpoint, SSM param paths, bucket names), with every real secret (SECRET_KEY, DB_PASSWORD, ANTHROPIC_API_KEY, EMAIL_HOST_USER/PASSWORD, VOLT_OWNER_API_KEY) explicitly deferred to .env.secrets via comments — audit claim is accurate, no rotation needed; (4) docs/dev/commit-hygiene.md is clear and actionable. git status clean. → tester.
