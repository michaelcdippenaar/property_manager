---
id: RNT-020
stream: rentals
title: "AI in-app onboarding/guide agent (agent + owner portals)"
feature: "ai_guide_agent"
lifecycle_stage: 0
priority: P1
effort: L
v1_phase: "1.0"
status: done
assigned_to: null
depends_on: []
asana_gid: "1214227840268616"
created: 2026-04-23
updated: 2026-04-23
---

## Goal

Embed a natural-language guide agent in the agent and owner portals that can navigate users to the right page, highlight the correct CTA, and optionally perform the action on their behalf — reducing time-to-first-value for new users and cutting support load.

## Acceptance criteria

- [x] Chat widget (`AIGuide.vue`) mounted in `AppLayout.vue` for both agent and owner portals; widget is collapsible and non-blocking.
- [x] User can type a natural-language intent (e.g. "I want to create a property") and the agent responds with navigation and/or highlights the relevant UI element.
- [x] Two modes supported: **Walk-through** (highlight next click, user acts) and **Do-it-for-me** (agent pre-fills / submits forms via allowlisted RPA actions).
- [ ] Backend intent mapper (`backend/apps/ai/guide_views.py`) receives user message, determines intent, and returns a structured action payload (route, element selector, pre-fill data, confirmation required flag). **DEFERRED — frontend uses mock; see discovery `tasks/discoveries/2026-04-23-ai-guide-backend-endpoint.md`**
- [ ] Tool-use loop: backend agent has an allowlisted set of actions (create property, list leases, go to dashboard, view maintenance, etc.) — no action outside the allowlist can be triggered. **DEFERRED — backend not built per sprint constraint**
- [x] Actions are permission-scoped: agent-portal users see agent-role tools only; owner-portal users see owner-role tools only. (Implemented in mock route maps; will be enforced server-side when backend ships.)
- [ ] Telemetry: each guide interaction (intent, action taken, completion/abandonment) is logged to a `GuideInteraction` model for UX analytics. **DEFERRED — requires backend**
- [x] Anthropic API key injected via env var (`ANTHROPIC_API_KEY`); never hardcoded. (Frontend never touches ANTHROPIC_API_KEY; backend will read it from env when built.)
- [x] `useAIGuide.ts` composable exposes `{ sendMessage, highlight, clearHighlight, isOpen, toggle }` to the rest of the frontend.
- [x] `aiGuide` Pinia store holds conversation history, highlight state, and router-navigation state.
- [ ] Unit tests for intent mapper (mock Anthropic responses); E2E smoke test for walk-through mode. **DEFERRED — backend tests deferred; FE unit tests left for QA stream**
- [x] Feature flag (`ENABLE_AI_GUIDE` env var, default `false`) so the widget can be toggled without a deploy.

## Files likely touched

- `admin/src/components/AIGuide.vue` (new — chat widget + highlight overlay)
- `admin/src/composables/useAIGuide.ts` (new)
- `admin/src/stores/aiGuide.ts` (new — conversation, highlight, router state)
- `admin/src/components/AppLayout.vue` (mount AIGuide widget)
- `backend/apps/ai/__init__.py` (new or extend existing)
- `backend/apps/ai/guide_views.py` (new — intent mapper, tool-use handler)
- `backend/apps/ai/guide_tools.py` (new — allowlisted action definitions)
- `backend/apps/ai/models.py` (new `GuideInteraction` model)
- `backend/apps/ai/migrations/` (new migration)
- `backend/config/urls.py` (route `/api/v1/ai/guide/`)
- `backend/config/settings/base.py` (ANTHROPIC_API_KEY, ENABLE_AI_GUIDE)
- `backend/apps/ai/tests/test_guide.py` (new)

## Test plan

**Manual:**
1. Open agent portal → click guide widget → type "create a new property" → confirm agent navigates to the correct page and highlights the "Add Property" button.
2. Switch to walk-through mode → confirm step-by-step highlights appear in sequence.
3. Switch to do-it-for-me mode → confirm agent pre-fills the property creation form.
4. Log in as owner → confirm only owner-scoped tools are available in the guide.
5. Disable `ENABLE_AI_GUIDE` → confirm widget is not rendered.

**Automated:**
- `cd admin && npx vue-tsc --noEmit` (type-check)
- `cd admin && npm run build` (Vite production build, flag unset)
- Verify `VITE_ENABLE_AI_GUIDE=false` suppresses widget render; `=true` renders floating button.

> Note: Backend pytest suite deferred per discovery 2026-04-23-ai-guide-backend-endpoint.md — re-add to test plan when backend endpoint task ships.

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-23 rentals-pm: Task authored. Feature flag defaults off — ship behind flag, enable for beta users first.

2026-04-23 rentals-implementer: Frontend-only implementation per sprint constraint (no backend/ changes). Four new files shipped in `admin/src/`:
- `stores/aiGuide.ts` — Pinia store with conversation history, highlight state, mode (walkthrough/do-it-for-me), pending action, and an in-process mock intent mapper that handles ~7 common intents via regex until the real backend endpoint ships.
- `composables/useAIGuide.ts` — composable that wires the store to vue-router for navigation execution; exposes `sendMessage, highlight, clearHighlight, isOpen, toggle, executeAction, setMode`.
- `components/AIGuide.vue` — self-contained chat widget rendered via `<Teleport to="body">`. Includes: collapsible floating button (Navy/Accent tokens), slide-in panel (22rem, max 600px tall), typing-dot loader, highlight pulse overlay (CSS `requestAnimationFrame` loop tracks target element), walkthrough action card with "Go" button, do-it-for-me action card with Confirm/Cancel, quick-prompt chips on empty state, Escape key to close, route-change clears highlight, focus trap on open.
- `admin/.env.development` — `VITE_ENABLE_AI_GUIDE=false` documented.

`AppLayout.vue` updated: imports `AIGuide` + `AI_GUIDE_ENABLED`; renders `<AIGuide portal-role="agent" />` unconditionally (renders nothing when flag is false); falls back to the original static Sparkles FAB when flag is off.
`OwnerLayout.vue` updated: same pattern with `portal-role="owner"`.

Vite build passes cleanly (`✓ built in 27.27s`), no TS errors in new files.

Backend AC (guide_views, guide_tools, GuideInteraction model, pytest suite) deferred — see discovery `tasks/discoveries/2026-04-23-ai-guide-backend-endpoint.md`. The swap from mock to real API is a one-line change in `stores/aiGuide.ts` (comment marked "When backend ships, swap this for:").

Reviewer note: to smoke-test locally, set `VITE_ENABLE_AI_GUIDE=true` in `admin/.env.development.local`, start Vite dev server, open any agent or owner page, and click the accent-pink floating button bottom-right.


2026-04-23 rentals-reviewer: Review passed. Checked: (a) FE-only diff confirmed via git show --stat — zero backend/ changes; (b) Navy (#2B2D6E) default FAB + Accent (#FF3D7F) open state matches design tokens; (c) Teleport isolation (x3: highlight overlay, FAB, panel) prevents layout interference; (d) VITE_ENABLE_AI_GUIDE gate cleanly short-circuits render and falls back to original static FAB in AppLayout; (e) route-change highlight cleanup wired via watch on route.path + rAF cancelled on unmount; (f) walkthrough "Go" button + do-it-for-me Confirm/Cancel cards rendered conditionally by mode; (g) role-scoped mock route maps (agent/owner) and portalRole prop threading; (h) Escape to close, focus on open, quick-prompt chips, typing-dot loader. Security: no secrets in FE, no PII logged, ANTHROPIC_API_KEY never referenced frontend-side, mock intent mapper is pure regex (no injection surface). Backend ACs correctly deferred with discovery filed at tasks/discoveries/2026-04-23-ai-guide-backend-endpoint.md. Vite build reported clean by implementer.

2026-04-23 rentals-tester: Test run blocked — pytest test file missing (backend deferred but still in plan).

Results:

1. `npm run type-check` — PASS (no such script; ran `npx vue-tsc --noEmit` as equivalent — exit 0, clean)
2. `npm run lint` — N/A (no lint script in package.json, no eslint config found; not a regression introduced by this PR)
3. `npm run build` (VITE_ENABLE_AI_GUIDE unset) — PASS (vue-tsc + vite build clean, `built in 8.67s`, zero TS errors, AIGuide and AppLayout bundles emitted correctly)
4. Claude Preview UI smoke test — NOT EXECUTED (mcp__Claude_Preview__ tools not available in this agent session; see note below)
5. `cd backend && pytest apps/ai/tests/test_guide.py -v` — FAIL

Failure output verbatim:
```
ERROR: file or directory not found: apps/ai/tests/test_guide.py
collected 0 items
```

Additional verification performed (source-level, in lieu of UI preview):
- Dev server started with `VITE_ENABLE_AI_GUIDE=true`; Vite env confirmed `"VITE_ENABLE_AI_GUIDE": "true"` baked into served modules. `AI_GUIDE_ENABLED` evaluates to `true` at runtime.
- AIGuide.vue `v-if="enabled"` guard confirmed (line 3); `enabled = AI_GUIDE_ENABLED` (line 243).
- Mock intent mapper in `stores/aiGuide.ts` handles "create a new property" → navigates `/properties`, highlights `[data-guide="add-property"]` — matches walk-through test step 1.
- Production build (flag unset=false): AppLayout bundle contains Sparkles import (fallback FAB) and no `ai-guide` class string — dead-code elimination confirmed; fallback FAB correctly present.
- OwnerLayout has `<AIGuide portal-role="owner" />` but no explicit fallback FAB (no Sparkles FAB existed in owner portal before, so no regression).

Blocking reason: The test plan's automated item `pytest apps/ai/tests/test_guide.py -v` fails because the test file was never created — backend was deferred per sprint constraint but the test plan was not updated to remove or skip this step. The PM/implementer must either create the test file (even a stub with `pytest.mark.skip`) or remove the pytest step from the test plan before re-testing.

2026-04-23 rentals-pm: Unblocked by PM 2026-04-23: test plan corrected to match frontend-only scope. Backend deferral was already documented in the discovery file but the test plan was not updated at implementation time. Pytest line removed; frontend checks (type-check, build, feature-flag behaviour) retained. Moving to testing.

2026-04-23 rentals-tester (re-run): All frontend-only checks pass.

Results:

1. `npx vue-tsc --noEmit` — PASS (exit 0, zero TypeScript errors across all new files including AIGuide.vue, useAIGuide.ts, stores/aiGuide.ts)
2. `npm run build` (VITE_ENABLE_AI_GUIDE unset → false) — PASS (`✓ built in 9.75s`, zero errors; AIGuide-*.js and AIGuide-*.css chunks emitted correctly; sparkles FAB chunk present in AppLayout bundle)
3. VITE_ENABLE_AI_GUIDE toggle verification — PASS (source-level):
   - `useAIGuide.ts` line 12: `export const AI_GUIDE_ENABLED = import.meta.env.VITE_ENABLE_AI_GUIDE === 'true'`
   - `AIGuide.vue` line 3: `<template v-if="enabled">` where `enabled = AI_GUIDE_ENABLED` — entire widget (FAB, panel, highlight overlay) gated behind this boolean
   - `AppLayout.vue` line 350: `v-if="!aiGuideEnabled"` on fallback Sparkles RouterLink — static FAB shown when flag is off, hidden when flag is on (AIGuide widget takes over)
   - `OwnerLayout.vue`: same pattern with `portal-role="owner"`
   - When flag is false/unset: AIGuide.vue renders nothing; static Sparkles FAB shown in AppLayout
   - When flag is true: AIGuide.vue renders full widget; static Sparkles FAB suppressed

All three automated items from the corrected test plan pass. Backend pytest item correctly removed from plan per PM unblock note.
