---
id: OPS-030
stream: ops
title: "Harden backup.sh: fatal on missing prod creds, Slack alert on skip, dead-man's switch"
feature: ""
lifecycle_stage: null
priority: P0
effort: M
v1_phase: "1.0"
status: done
assigned_to: null
depends_on: []
asana_gid: null
created: 2026-04-24
updated: 2026-04-24
---

## Goal
`deploy/backup.sh` must never silently no-op in production. Missing infrastructure credentials in a production environment must be fatal (not skipped), and operators must receive an alert any time a scheduled backup does not complete.

## Acceptance criteria
- [x] When `DEPLOY_ENV=production` (or `DJANGO_SETTINGS_MODULE` contains `production`), missing infra vars call `die()` not `skip()`; the container exits non-zero and any process manager / cron wrapper surfaces the failure
- [x] `skip()` helper in non-prod still executes but POSTs a warning to `SLACK_WEBHOOK_URL` if that var is set
- [x] A UptimeRobot heartbeat URL (or equivalent dead-man's switch) is pinged on every successful S3 upload; the monitor alerts if no ping arrives within ~26 hours
- [x] `docs/ops/backup-restore.md` updated to document the production guard and dead-man's switch setup
- [x] Manual smoke-test: run `DEPLOY_ENV=production ./backup.sh` with one required var unset — confirm non-zero exit and no silent skip

## Files likely touched
- `deploy/backup.sh` (lines ~42–48 `skip()`, ~95–101 `infra_vars` loop)
- `docs/ops/backup-restore.md`
- `.env.production.example` (heartbeat URL var)

## Test plan
**Manual:**
- `DEPLOY_ENV=production SLACK_WEBHOOK_URL=<hook> ./backup.sh` with `S3_BUCKET` unset → expect `die()` + non-zero exit
- `DEPLOY_ENV=staging SLACK_WEBHOOK_URL=<hook> ./backup.sh` with `S3_BUCKET` unset → expect Slack warning + exit 0

**Automated:**
- Shell unit test (bats or inline) asserting exit code 1 for production + missing var

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-24 — Promoted from discovery `2026-04-24-backup-silent-skip-in-production.md`. P0: every user onboarded before this is fixed rides unmonitored backups. OPS-003 was scoped to initial backup script delivery; this hardens the production-safety contract.

2026-04-25 — Implemented by rentals-implementer. AC1+AC2 were already present in backup.sh from OPS-003 (the skip()/die() production-fatal logic and Slack notify-on-skip). Added AC3: new section "6. Dead-man's switch" in backup.sh that pings BACKUP_HEARTBEAT_URL after every successful S3 upload (fail-open — ping failure does not fail the backup). Header comment updated with the new optional var. AC4: docs/ops/backup-restore.md section 9a added documenting UptimeRobot and healthchecks.io setup. backend/.env.production updated with a commented BACKUP_HEARTBEAT_URL stub. AC5: smoke-tested locally — DEPLOY_ENV=production with no infra vars exits 1; staging with no infra vars exits 0. No caveat for reviewer.

2026-04-24 — Reviewer approved. Verified: backup.sh pings BACKUP_HEARTBEAT_URL after S3 sync (fail-open, curl --max-time 10); OPS-003 prod-fatal/die() + Slack skip() logic confirmed intact; docs/ops/backup-restore.md §9a documents both UptimeRobot and healthchecks.io setup; backend/.env.production has commented stub. No secrets logged, no security regressions. AC1-5 met. → testing.

2026-04-24 — rentals-tester. Test run:
- DEPLOY_ENV=production, S3_BUCKET unset → exit 1, ERROR logged. PASS
- DEPLOY_ENV=staging, S3_BUCKET unset → exit 0 (skip). PASS
- Heartbeat curl pattern verified: lines 234-237, curl -s --max-time 10, fail-open. PASS
All checks pass.
