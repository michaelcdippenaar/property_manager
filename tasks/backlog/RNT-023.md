---
id: RNT-023
stream: rentals
title: "Fix lease signing flow flash-and-dismiss when launched via URL query params"
feature: "e-signing"
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
asana_gid: "1214278195765176"
created: 2026-04-25
updated: 2026-04-25
---

## Goal
Ensure that navigating to `/leases?expand=124&sign=1` reliably opens and holds the signing modal for lease 124, without flashing and self-dismissing.

## Repro
- URL: `http://localhost:5173/leases?expand=124&sign=1`
- Expected: leases list opens, lease 124 expands, signing modal auto-launches and persists.
- Actual: page flashes — modal appears for a single frame then dismisses. No backend 500 (backend log silent).

## Root cause candidates
1. `?sign=1` is consumed on mount but a route guard or watcher re-navigates and strips the param, unmounting the modal.
2. Modal opens before lease data has loaded (`leases[124]` is undefined); the open-handler bails out and closes itself.
3. `expand=124` watcher and `sign=1` watcher conflict — one stomps the other.
4. Pinia store action triggers a `router.replace` that removes `sign=1`.

## Acceptance criteria
- [ ] Locally reproduce the flash-dismiss; capture browser console output and Vue devtools component state at the moment of dismissal.
- [ ] Identify the root cause with evidence (console trace, watcher log, or router hook capture) — not theory.
- [ ] Fix so that `/leases?expand=124&sign=1` reliably opens the signing modal for the target lease.
- [ ] The modal persists until the user explicitly dismisses it or the signing flow completes normally.
- [ ] Add a Vitest regression test that simulates the URL-driven flow (mount LeasesView with `?expand=124&sign=1`, assert modal is visible after lease data resolves).
- [ ] Verify: hard-refresh the URL — modal opens. Click outside — modal closes cleanly with no console errors. Re-open URL — works again.

## Files likely touched
- `admin/src/views/leases/LeasesView.vue` (or `LeasesListView.vue`)
- `admin/src/components/leases/SignLeaseModal.vue` (or equivalent signing modal component)
- `admin/src/router/index.ts` (if a route guard is stripping params)
- `admin/src/stores/leases.ts`

## Test plan
**Manual:**
1. Start admin dev server (`npm run dev` in `admin/`).
2. Navigate to `http://localhost:5173/leases?expand=124&sign=1`.
3. Confirm signing modal opens and remains open.
4. Click outside modal — confirm it closes cleanly, no console errors.
5. Re-navigate to the same URL — confirm modal opens again.

**Automated:**
- Vitest: mount `LeasesView` with mocked router params `{ expand: '124', sign: '1' }`, mock Pinia leases store returning lease 124, assert `SignLeaseModal` is rendered and `visible` prop is `true` after `nextTick`.

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)
