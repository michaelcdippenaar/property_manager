---
id: QA-015
stream: qa
title: "Fix BaseModal: closable=false must guard backdrop click and Escape key"
feature: ""
lifecycle_stage: null
priority: P3
effort: S
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
asana_gid: "1214197071642707"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Ensure `BaseModal` with `:closable="false"` cannot be dismissed via backdrop click or the Escape key, matching the intent of the hidden X button.

## Acceptance criteria
- [ ] Backdrop `@click` in `BaseModal.vue` is gated on the `closable` prop (skip emit when `closable` is false)
- [ ] `useFocusTrap` accepts an optional `onEscape` callback; passing `null` disables Escape dismissal
- [ ] `BaseModal` passes `null` to `onEscape` when `closable` is `false`
- [ ] Existing modals with default `closable` (true) are unaffected — backdrop and Escape still close them
- [ ] Unit/component test covers the `:closable="false"` case for both backdrop click and Escape keydown

## Files likely touched
- `admin/src/components/BaseModal.vue`
- `admin/src/composables/useFocusTrap.ts`

## Test plan
**Manual:**
- Open any modal rendered with `:closable="false"` in the admin app
- Click the backdrop — modal must stay open
- Press Escape — modal must stay open
- Confirm that a regular (closable) modal still closes on backdrop click and Escape

**Automated:**
- Vue Test Utils component test: mount `BaseModal` with `closable=false`, trigger backdrop click and Escape, assert `close` event is not emitted

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-22 — Promoted from discovery `2026-04-22-basmodal-closable-prop-escape-backdrop-not-guarded.md` found during QA-011 review. The fix requires a design decision on whether `closable` gates Escape and backdrop independently — the discovery author recommends a single `closable` flag controlling both via a composable callback pattern.
