---
id: OPS-011
stream: ops
title: "Add website_web image build and push to staging CI/CD pipeline"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
asana_gid: "1214181292747447"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Wire the Astro marketing site (`website_web`) into the staging CI/CD pipeline so it is built, pushed to GHCR, and deployed from a pinned SHA tag — matching the pattern used for `backend`, `admin_web`, and `agent_app_web`.

## Acceptance criteria
- [ ] Add `image: ${WEBSITE_IMAGE:-ghcr.io/michaelcdippenaar/klikk-website:latest}` to `website_web` in `deploy/docker-compose.staging.yml`
- [ ] Add a "Build & push website" step to `.github/workflows/staging-deploy.yml` using `./website/Dockerfile`
- [ ] Export `WEBSITE_IMAGE` in the SSH deploy block alongside the other three image env-vars
- [ ] Staging deploy runs end-to-end without the website service lagging behind `main`

## Files likely touched
- `deploy/docker-compose.staging.yml`
- `.github/workflows/staging-deploy.yml`
- `website/Dockerfile` (may need to exist / be stable first)

## Test plan
**Manual:**
- Trigger staging deploy workflow after a commit that changes the website
- Verify `docker ps` on staging shows the newly tagged `klikk-website` image

**Automated:**
- GitHub Actions workflow completes without errors for all four services

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-22 — Promoted from discovery `2026-04-22-website-web-not-in-cicd.md` found during OPS-001 review. Depends on `website/Dockerfile` being stable; implement once the Astro site ships real content.
