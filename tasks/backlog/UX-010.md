---
id: UX-010
stream: ux
title: "Tenant invite-accept screen missing — new tenant cannot set password"
feature: ""
lifecycle_stage: 4
priority: P0
effort: M
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
blocks: [MIL-001]
asana_gid: "1214237361697811"
created: 2026-04-23
updated: 2026-04-23
---

## Goal
Build the `/invite/:token` route and view in the tenant web app so new tenants can accept their invite, set a password, and reach the portal without agent intervention.

## Acceptance criteria
- [ ] Route `/invite/:token` added to `tenant/src/router/index.ts`
- [ ] GET: pre-fills email (read-only) from backend token lookup; prompts for full name + password
- [ ] POST: calls `UserInviteAcceptView`, logs the user in on success, redirects to welcome/home screen
- [ ] Expired or cancelled invite shows a clear error message with agent contact prompt
- [ ] Invalid/unknown token returns a 404-style error screen (not a blank login page)
- [ ] At least one integration test covering the happy path and the expired-token path

## Files likely touched
- `tenant/src/router/index.ts` — add `/invite/:token` route
- `tenant/src/views/auth/InviteAcceptView.vue` — new view (create)
- `backend/apps/accounts/views.py` — `UserInviteAcceptView` (GET + POST, line ~381)
- `content/emails/invite_tenant.md` — verify `invite_url` destination matches new route

## Test plan
**Manual:**
- Send a real invite to a test tenant email; click link; confirm InviteAcceptView loads with email pre-filled
- Fill name + password → submit → confirm redirect to home screen
- Use an expired token (modify DB expiry) → confirm error state
- Use a non-existent token → confirm 404 error state

**Automated:**
- `cd backend && pytest apps/accounts/tests/test_invite_accept.py`
- E2E: `mcp__tremly-e2e__tenant_invite_accept_flow`

## Handoff notes
2026-04-23 rentals-pm: Promoted from discovery `2026-04-23-tenant-no-invite-accept-screen.md` (found during UX-002 audit). P0 ship-blocker — blocks MIL-001 (tenant golden path E2E QA-003 cannot pass without this route). Assign to implementer immediately.
