---
id: OPS-018
stream: ops
title: "Add MJML compile script to prevent Django template blocks being overwritten"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
asana_gid: "1214195192114625"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Add a `compile_email_templates.sh` script (or Makefile target) that re-inserts Django template conditional blocks (`{% if cta_url %}`, `{% if note %}`) after running `npx mjml`, so recompiling `base.mjml` does not silently break email rendering.

## Acceptance criteria
- [ ] `scripts/compile_email_templates.sh` (or `Makefile` target) runs `npx mjml base.mjml -o compiled/base.html` then re-patches the conditional rows
- [ ] Script is documented in `README` / `CONTRIBUTING` — "always use the script, not raw `npx mjml`"
- [ ] Running the script produces a `compiled/base.html` that passes the existing email rendering smoke test (CTA button only renders when `cta_url` is provided)

## Files likely touched
- `scripts/compile_email_templates.sh` (new)
- `Makefile` (optional target)
- `backend/apps/notifications/email_templates/base.mjml`
- `backend/apps/notifications/email_templates/compiled/base.html`

## Test plan
**Manual:**
- Modify a branding detail in `base.mjml`, run the script, confirm compiled output retains `{% if cta_url %}` blocks and CTA renders conditionally

**Automated:**
- Existing email send smoke tests pass after recompile

## Handoff notes
2026-04-22: Promoted from discovery `2026-04-22-mjml-build-script.md` (found during OPS-005). The hand-patched conditional blocks will be silently overwritten on any future `npx mjml` run.
