---
id: OPS-012
stream: ops
title: "Make staging server reachable from GitHub Actions (implement chosen tunnel/host)"
feature: ""
lifecycle_stage: null
priority: P1
effort: M
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: [DEC-015]
asana_gid: "1214181228191237"
created: 2026-04-22
updated: 2026-04-24
---

## Goal
Implement whichever connectivity approach MC selects in DEC-015 so the GitHub Actions staging deploy workflow can SSH into the staging server without timeout.

## Acceptance criteria
- [x] Staging server reachable from a public/tunnelled address (per DEC-015 decision) — confirmed: `102.135.240.222` / `klikk.co.za` is publicly reachable
- [ ] `STAGING_HOST` GitHub secret updated to the reachable address — **MC action required**: set `STAGING_HOST=102.135.240.222` in GitHub → Settings → Secrets (see DEVOPS.md §5a)
- [ ] SSH access locked to GitHub Actions IP range or a dedicated deploy key — **MC action required**: generate + install Ed25519 deploy key per DEVOPS.md §5a instructions
- [x] `staging-deploy.yml` `ssh-keyscan` step completes without timeout — explicit `Add staging host to known_hosts` step added to workflow
- [ ] Full staging deploy workflow runs end-to-end successfully — requires secrets to be set first, then MC triggers `workflow_dispatch`

## Files likely touched
- `.github/workflows/staging-deploy.yml` (update host references)
- `deploy/DEVOPS.md` (update staging IP/hostname)
- Server-side: cloudflared / Tailscale / router config (out-of-repo)

## Test plan
**Manual:**
- Trigger the staging-deploy workflow from a push to `main`
- Verify all services are restarted on the staging host and the deploy log shows no timeout

**Automated:**
- GitHub Actions workflow completes with green status on all steps

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-22 — Blocked on DEC-015. Created from discovery `2026-04-22-staging-private-lan-deploy.md`. Do not implement until MC chooses connectivity option.

2026-04-22 — Implemented. DEC-015 resolved: staging server is publicly reachable at `102.135.240.222` (klikk.co.za). No Cloudflare Tunnel or Tailscale needed.

Changes made:
1. `.github/workflows/staging-deploy.yml` — workflow already uses `workflow_dispatch` (manual trigger only, satisfying MC's "no auto-deploy" requirement). Added explicit `Add staging host to known_hosts` step (ssh-keyscan preflight). Added a comment block at the top of the `deploy` job listing all required GitHub secrets with their correct values.
2. `deploy/DEVOPS.md` — updated staging IP from `192.168.1.235` to `102.135.240.222` throughout (environment table and all SSH command examples). Added new §5a "GitHub Actions Secrets (staging deploy)" documenting the 5 required secrets and step-by-step deploy key generation instructions.

Two ACs still require MC's manual action before the workflow can run end-to-end:
- Set GitHub secrets: `STAGING_HOST`, `STAGING_SSH_USER`, `STAGING_SSH_KEY`, `STAGING_DOMAIN`, `GHCR_PAT` — instructions in DEVOPS.md §5a
- Generate and install the Ed25519 deploy key on the server (instructions in DEVOPS.md §5a)
Once those are set, trigger the workflow via `workflow_dispatch` to validate end-to-end.

2026-04-22 — Review passed. Checked: all `192.168.1.235` → `102.135.240.222` replacements are complete and consistent throughout DEVOPS.md (7 occurrences). Workflow `ssh-keyscan` step is correctly placed before `appleboy/ssh-action`, uses `-H` flag to hash the hostname, and sets correct file permissions (700/600). The action will pick up `~/.ssh/known_hosts` populated by the preceding step (no conflicting `known_hosts` param passed to the action). DEVOPS.md §5a deploy key instructions are complete and accurate; `from="X.X.X.X"` hardening note is present. Two remaining open ACs are correctly identified as MC manual actions — not implementable in-repo. Security pass: no new endpoints, no PII, no raw SQL, workflow-only change. Discovery filed: `tasks/discoveries/2026-04-22-devops-runbook-plaintext-password.md` — pre-existing `# password: pass` comment in SSH examples is now a higher-risk issue given the server is publicly reachable; separate task recommended to rotate credentials and disable password auth.

2026-04-22 — Test run (rentals-tester). BLOCKED — test plan requires human execution. Both remaining open ACs depend on MC manually setting GitHub secrets (STAGING_HOST, STAGING_SSH_USER, STAGING_SSH_KEY, STAGING_DOMAIN, GHCR_PAT) and installing an Ed25519 deploy key on the server before the workflow_dispatch trigger can be attempted. No automated test path exists; neither pytest nor MCP E2E tools can interact with GitHub Actions secrets or SSH into the staging server. Test plan cannot be executed by the automated tester.

### 2026-04-24 — rentals-pm: recycled to backlog (dep DEC-015 landed)

DEC-015 is in `done/` (staging server publicly reachable at 102.135.240.222 confirmed). Code implementation and review are complete. Remaining open ACs are all MC manual actions: set 5 GitHub secrets and install the Ed25519 deploy key per DEVOPS.md §5a, then trigger `workflow_dispatch`. Task returned to backlog for MC to execute those manual steps.
