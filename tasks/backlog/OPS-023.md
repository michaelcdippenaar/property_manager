---
id: OPS-023
stream: ops
title: "Verify seen_welcome_at migration applied on staging and document migrate as required setup step"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: backlog
assigned_to: null
depends_on: []
asana_gid: "1214227849755929"
created: 2026-04-23
updated: 2026-04-23
---

## Goal

Confirm the `accounts_user.seen_welcome_at` migration is applied on staging (it was manually applied to dev on 2026-04-23) and add a `migrate` step to the README / Makefile so no tester or developer hits the `ProgrammingError: column accounts_user.seen_welcome_at does not exist` login-blocker again.

## Acceptance criteria
- [ ] `python manage.py showmigrations accounts` shows the `seen_welcome_at` migration as applied on staging
- [ ] `POST /api/v1/auth/login/` returns 200 on staging with valid credentials (no 500 ProgrammingError)
- [ ] `README.md` or `Makefile` explicitly lists `python manage.py migrate` as a required step in the local dev setup section
- [ ] CI/CD pipeline runs `manage.py migrate` before any integration test run (or confirm it already does)

## Files likely touched
- `README.md` (or `docs/setup.md` if it exists)
- `Makefile` (if present)
- `backend/apps/accounts/migrations/` — verify migration file exists and is not missing

## Test plan
**Manual:**
- SSH/exec into staging container: `python manage.py showmigrations accounts` — all migrations should be `[X]`
- Hit `POST /api/v1/auth/login/` with valid credentials on staging — expect 200

**Automated:**
- `cd backend && python manage.py migrate --check` — exits 0 if no unapplied migrations

## Handoff notes
(2026-04-23 rentals-pm) Promoted from discovery `2026-04-23-accounts-missing-seen-welcome-at-migration.md`. Dev DB migration was manually applied by MC. Staging verification and README update still outstanding.
