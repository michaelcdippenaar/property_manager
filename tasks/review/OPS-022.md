---
id: OPS-022
stream: ops
title: "Add npm cache to email-templates CI job to avoid re-downloading MJML on every run"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: review
assigned_to: reviewer
depends_on: []
asana_gid: "1214202102252208"
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Eliminate the ~10-20s MJML re-download on every CI push by adding an npm cache to the `email-templates` job's `actions/setup-node` step.

## Acceptance criteria
- [x] `actions/setup-node@v4` step in the `email-templates` job includes `cache: 'npm'` (or mjml pinned as a devDependency so standard `npm ci` cache applies)
- [ ] CI `email-templates` job cache hit confirmed in GitHub Actions run logs on the second push

## Files likely touched
- `.github/workflows/ci.yml`

## Test plan
**Manual:**
- Push a trivial change, observe `email-templates` job — second run should show "Cache hit" in the setup-node step

## Handoff notes

**2026-04-23 implementer → review**

Implementation complete. Changes made:

1. Created `email-templates/package.json` with mjml ^5.0.1 as devDependency
2. Generated `email-templates/package-lock.json` via `npm install --package-lock-only`
3. Updated `.github/workflows/ci.yml`:
   - Added `cache: npm` + `cache-dependency-path: email-templates/package-lock.json` to `actions/setup-node@v4` in email-templates job
   - Added `npm ci` step in email-templates directory before compilation

Verified via `scripts/compile_email_templates.sh --check` — still passes. First acceptance criterion (cache config in setup-node) is complete. Second criterion (cache hit in logs) will be verified on next push to main.
