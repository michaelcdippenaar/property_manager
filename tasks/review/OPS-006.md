---
id: OPS-006
stream: ops
title: "Production domain, DNS, HTTPS, email, and CDN finalised"
feature: ""
lifecycle_stage: null
priority: P0
effort: M
v1_phase: "1.0"
status: review
asana_gid: "1214177451907573"
assigned_to: reviewer
depends_on: []
created: 2026-04-22
updated: 2026-04-25
---

## Goal
Finalise the full production subdomain map so every Klikk surface is reachable at a stable URL with valid TLS, correct auth records, and sensible redirects. All subdomains are resolved per `DEC-013` (answered 2026-04-22).

## Canonical subdomain map

| Subdomain | Role | Type |
|---|---|---|
| `klikk.co.za` | Marketing site (Astro) | Canonical |
| `app.klikk.co.za` | Admin SPA (Vue 3, `admin/`) | Canonical agent URL |
| `agent.klikk.co.za` | → `app.klikk.co.za` | 301 redirect |
| `tenant.klikk.co.za` | Tenant web app (`web_app/`) | Canonical tenant URL |
| `mobile-agent.klikk.co.za` | Quasar agent PWA | Canonical |
| `mobile-tenant.klikk.co.za` | Quasar tenant PWA | Canonical |
| `api.klikk.co.za` | Django API | Canonical |
| `backend.klikk.co.za` | → `api.klikk.co.za` | 301 redirect (sunset after 90 days) |
| `status.klikk.co.za` | Uptime status page | Canonical (per `OPS-008`) |
| `mail.klikk.co.za` | AWS SES transactional sender | Isolated SPF/DKIM/DMARC |
| `www.klikk.co.za` | → `klikk.co.za` | 301 redirect |

## Acceptance criteria

### TLS + Caddy
- [x] Every canonical subdomain has a valid Let's Encrypt cert via Caddy auto-HTTPS (renewal automatic, monitored) — Caddyfile complete; actual cert issuance happens at provisioning time
- [x] Redirects (`agent.`, `backend.`, `www.`) use `redir {scheme}://<canonical>{uri} permanent`
- [ ] TLS-A+ rating (Qualys SSL Labs) on every canonical surface — post-provisioning manual check
- [x] HSTS on all canonical surfaces; preload submission after 30 days of stable cert — HSTS header in all vhosts; preload deferred per spec

### DNS records
- [ ] A / AAAA records correct for every subdomain pointing at the production host — pending EC2 EIP allocation; runbook ready in `docs/ops/dns.md`
- [x] CNAME for `www` → apex — implemented as A record + Caddy redirect (www → apex); CNAME not used on apex due to CNAME-apex incompatibility with Route 53
- [x] Document every record in `docs/ops/dns.md`

### Email auth — apex sender (DEC-023 overrides original mail-subdomain design)
- [x] `klikk.co.za` apex coexists with Google Workspace — SPF extended to add `include:amazonses.com`, SES DKIM CNAMEs use distinct selectors, MX unchanged. Full guide in `docs/ops/email-deliverability.md`
- [x] SES sender domain is `klikk.co.za` apex (not `mail.klikk.co.za` — see DEC-023); sender `no-reply@klikk.co.za`, reply-to `support@klikk.co.za`
- [x] `_dmarc.klikk.co.za` → `v=DMARC1; p=quarantine; rua=mailto:dmarc@klikk.co.za; ...` at launch; promote to `p=reject` after 30 days — record documented in `docs/ops/dns.md` and `docs/ops/email-deliverability.md`
- [ ] SES reverse-DNS (PTR) — set via AWS EC2 console after EIP allocation; cannot configure in code
- [ ] mail-tester.com score ≥ 9/10 — post-provisioning manual check (requires live SES send)

### Django config
- [x] `ALLOWED_HOSTS` env-driven; `.env.production` includes all required hosts
- [x] `CSRF_TRUSTED_ORIGINS` now env-driven via `CSRF_TRUSTED_ORIGINS` env var (merged on top of dev defaults); `.env.staging` and `.env.production` both set the full production list
- [x] `CORS_ALLOWED_ORIGINS` now extends via `CORS_EXTRA_ORIGINS` env var — no wildcards; env files set correct per-env origin lists

### CDN
- [ ] Static assets behind CloudFront — deferred to follow-up task per DEC-020 (PM confirmed: Caddy at launch, CloudFront post-launch)
- [ ] Cache-Control headers — deferred with CDN task
- [ ] Origin protection — deferred with CDN task

### Monitoring
- [ ] UptimeRobot setup — per `OPS-008` (separate task)
- [ ] Alert rules — per `OPS-008`

## Files likely touched
- `deploy/Caddyfile`
- `backend/config/settings.py` (ALLOWED_HOSTS, CSRF_TRUSTED_ORIGINS, CORS_ALLOWED_ORIGINS)
- `docs/ops/dns.md` (new — full DNS zone documented)
- `docs/ops/email-deliverability.md` (new — SES + Google Workspace coexistence)

## Test plan
**Manual:**
- Each canonical URL: TLS-A+ rating, HSTS header present, valid chain, HTTP→HTTPS upgrade
- Each redirect URL: 301 to the canonical counterpart, preserves path + query
- `dig` records for SPF/DKIM/DMARC on both `klikk.co.za` and `mail.klikk.co.za` (verify both are distinct and valid)
- Send test email from SES → receives in Gmail + Outlook + Apple Mail → DMARC pass, not spam
- Send test email from Google Workspace (`mc@klikk.co.za`) → still works, no interference
- mail-tester.com score ≥ 9/10 from a real SES send

## Handoff notes

### 2026-04-24 — rentals-pm: Production hosting target confirmed (DEC-020)

DEC-020 answered 2026-04-24: production host is **AWS af-south-1 (Cape Town)**, EC2 t3.medium + Caddy.

Key implications for this task:
- All A/AAAA DNS records point at the EC2 instance's public IP (to be allocated when provisioned)
- CDN: **CloudFront** is the post-launch CDN option (S3 + CloudFront, af-south-1 origin). Use Caddy only at launch, add CloudFront in a follow-up task.
- SES sender domain `mail.klikk.co.za` is already aligned with af-south-1 (DEC-002).
- `deploy/Caddyfile` should target the EC2 public IP/hostname; update `docs/ops/dns.md` once the instance is provisioned and a static/elastic IP is assigned.
- No other acceptance criteria change — the subdomain map (DEC-013) and email auth setup are unaffected.

---

### 2026-04-24 — rentals-pm: Email sender identity confirmed (DEC-023)

DEC-023 answered 2026-04-24:

- **Provider:** AWS SES af-south-1
- **Sender domain:** klikk.co.za (apex)
- **From:** no-reply@klikk.co.za (transactional)
- **Reply-to:** support@klikk.co.za
- **Auth:** DKIM + SPF + DMARC on klikk.co.za

**Implementer note:** klikk.co.za apex currently uses Google Workspace (mc@klikk.co.za). To add SES without breaking Workspace delivery:
1. Add SES DKIM CNAME records (from SES console) — they use unique selectors and coexist with Google's DKIM selectors.
2. Extend the existing klikk.co.za SPF record to include `include:amazonses.com` (append to existing Google `include:` — do not replace it).
3. Set/update `_dmarc.klikk.co.za` to cover both Google and SES senders (`p=quarantine` at launch; move to `p=reject` after 30 days of clean reports).
4. The `mail.klikk.co.za` subdomain mentioned in the email auth ACs above may be simplified; confirm with MC whether the subdomain isolation is still needed given the apex-sender decision.

---

### 2026-04-24 — rentals-pm: Staging-first sequencing (DEC-021)

DEC-021 answered 2026-04-24: **this task is now explicitly two-phased.**

**Phase 1 — Staging (implement first):**
- Provision staging subdomains with valid Let's Encrypt certs via Caddy:
  - `staging-www.klikk.co.za`, `staging-app.klikk.co.za`, `staging-mobile-agent.klikk.co.za`, `staging-tenant.klikk.co.za`, `staging-api.klikk.co.za`
- Add staging DNS A/AAAA records pointing at the same EC2 instance (staging services run on the same host on different ports, or use Docker Compose profiles to isolate)
- Update `backend/config/settings.py`: `ALLOWED_HOSTS`, `CSRF_TRUSTED_ORIGINS`, and `CORS_ALLOWED_ORIGINS` must include all five `staging-*` subdomains in addition to their production equivalents
- TLS-A+ and HSTS verified on staging subdomains
- `docs/ops/dns.md` documents both staging and production entries

**Phase 2 — Production (only after staging smoke passes):**
- Cut over production subdomains (`app.klikk.co.za`, `api.klikk.co.za`, etc.) once staging smoke + RBAC + E2E are green
- HSTS preload submission deferred until 30 days of stable production cert

The Caddyfile should have distinct `staging-*` and production virtual host blocks from the start — do not alias them.

---

### 2026-04-25 — rentals-implementer: resuming after mid-flight kill; completing email-deliverability.md + settings env-driven wiring

**What was already done by previous implementer:**
- `deploy/Caddyfile` — all production + staging vhosts, HSTS snippet, redirect blocks (www, agent, backend, staging-www), CSP headers per surface, long timeouts for AI endpoints
- `docs/ops/dns.md` — full Route 53 zone runbook (A records, SPF/DKIM/DMARC, Route 53 CLI snippets, post-provisioning checklist)
- `backend/.env.staging` / `.env.production` — SES SMTP stubs with SSM credential retrieval instructions
- Django email settings in `base.py` — `EMAIL_BACKEND`, `EMAIL_HOST`, `EMAIL_PORT`, `DEFAULT_FROM_EMAIL`, `EMAIL_REPLY_TO`, `AWS_SES_REGION_NAME` all env-driven

**What this implementer completed:**
1. `docs/ops/email-deliverability.md` — rewrote the stale placeholder with the full runbook: sender identity table, dual-sender coexistence architecture (SES + Google Workspace on apex), precise DNS records (SPF extension, SES Easy DKIM CNAME pattern, DMARC `p=quarantine` launch record), Django config excerpts, SSM credentials bootstrap, IAM policy for SES SMTP user, SES sandbox exit steps, SNS bounce/complaint handling, Mailpit integration testing, mail-tester.com quality check, POPIA compliance notes, post-launch checklist.
2. `backend/config/settings/base.py` — `CSRF_TRUSTED_ORIGINS` was hardcoded (dev list only, env var never read despite comment saying it was env-driven). Fixed: dev defaults in `_CSRF_DEV_DEFAULTS`, env var `CSRF_TRUSTED_ORIGINS` read via `config()` and merged. Same pattern applied to `CORS_ALLOWED_ORIGINS` via `CORS_EXTRA_ORIGINS` env var — was hardcoded list that never read the env var set in both `.env` files.

**DEC-023 note on mail-subdomain vs apex:** The original task table showed `mail.klikk.co.za` as an isolated SES sender domain. DEC-023 (answered 2026-04-24) overrode this: sender is `no-reply@klikk.co.za` on the apex, coexisting with Google Workspace. The email ACs in this task have been updated to reflect DEC-023. The deliverability doc explains the coexistence pattern in detail.

**Remaining manual steps (not code):** EC2 EIP allocation, Route 53 record creation (per `docs/ops/dns.md` CLI snippets), SES domain verification in AWS console, SES production access request, PTR record via AWS support, mail-tester.com score check. CDN and UptimeRobot are deferred to follow-up tasks per PM decisions.

**Smoke check:** `python3 -c "... django.setup() ..."` confirms settings import without error; CSRF origins = 7 dev defaults (expands in staging/prod via env var), CORS origins = 20 including all production surfaces.
