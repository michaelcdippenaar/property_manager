---
id: RNT-024
stream: rentals
title: "Maintenance badge shows 5 open issues but list is empty"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: review
assigned_to: reviewer
depends_on: []
asana_gid: "1214278133602287"
created: 2026-04-25
updated: 2026-04-25
---

## Goal

Eliminate the discrepancy where the maintenance badge counter reads 5 but the maintenance list shows zero issues for account mc.dippenaar.pr@gmail.com.

## Acceptance criteria

- [x] Reproduce: log in as mc.dippenaar.pr@gmail.com and confirm badge shows 5 while the list is empty.
- [x] Identify which endpoint is wrong: compare the SQL (or queryset) generated by `MaintenanceBadgesView` vs the maintenance list endpoint for the same user/agency scope.
- [x] Fix so badge count == list count for the same user/agency scope (status filters, soft-delete exclusions, and agency/tenant scoping must be identical between the two endpoints).
- [x] Add a backend test asserting `MaintenanceBadgesView.count == MaintenanceListView.count` for a fixture user with a mix of statuses (open, pending, escalated, closed, soft-deleted).
- [x] Document the badge-counting rule in a docstring on `MaintenanceBadgesView` — which statuses count, which do not, and why.

## Files likely touched

- `backend/apps/maintenance/monitor_views.py` (badge endpoint)
- `backend/apps/maintenance/views.py` (list endpoint)
- `backend/apps/maintenance/tests/test_badges.py` (new or extend)

## Test plan

**Manual:**
1. Log in as mc.dippenaar.pr@gmail.com in the admin app.
2. Note the badge count on the sidebar/dashboard maintenance icon.
3. Click into the Maintenance section and confirm the list count matches the badge.
4. Repeat after applying each available status filter to confirm totals add up.

**Automated:**
- `cd backend && pytest apps/maintenance/tests/test_badges.py -v`

## Handoff notes

(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-25 — rentals-pm: Task filed. Likely cause is badge endpoint counting differently (wrong scope, soft-deletes, or broader status set) than the list endpoint. Implementer should diff the two querysets first before touching any fix.

2026-04-25 — rentals-implementer: Root cause confirmed — `badges()` action in `MaintenanceRequestViewSet` used a global, unscoped `MaintenanceRequest.objects.exclude(...)` count completely bypassing the role-based queryset scoping in `get_queryset()`. An agent user whose portfolio had no requests would see badge=5 (global total) while the list showed 0.

Fix applied in `backend/apps/maintenance/views.py` — `badges()` now calls `self.get_queryset()` (reusing the same scope logic: tenant sees own, non-admin agents see accessible properties, admin sees all) then filters to `status__in=[OPEN, IN_PROGRESS]`. The `pending_questions` count was also unscoped; it now applies the same `get_accessible_property_ids` scoping for non-admin users, with tenants returning 0 (they are not agents).

Full docstring added to `badges()` explaining the counting rule, scoping rationale, and status semantics.

New test file `backend/apps/maintenance/tests/test_badges.py` — 8 tests, all passing:
- Admin badge matches global open+in_progress count
- Agent badge isolates own properties (no cross-agent bleed)
- badge count == filtered list count for same user auth
- Closed/resolved requests excluded
- Mixed status counts correct
- AgentQuestion scoping: admin sees all, tenant sees 0, answered not counted

No frontend changes needed — the API contract (`open_issues`, `pending_questions` keys) is unchanged; only the values are now correct.
