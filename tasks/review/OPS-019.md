---
id: OPS-019
stream: ops
title: "Fix backup drill crontab: CRONTAB_CONTENT env var not consumed by Dockerfile.backup"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: review
assigned_to: reviewer
depends_on: []
asana_gid: "1214200629567565"
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Fix the `drill_reminder` container so it fires `drill-reminder.sh` monthly (1st of month, 08:00) instead of running `backup.sh` nightly alongside the regular backup container.

## Acceptance criteria
- [x] `drill_reminder` service uses its own crontab (`/etc/crontab.drill`) scheduling `drill-reminder.sh` at `0 8 1 * *`, OR the Dockerfile entrypoint writes the crontab from `CRONTAB_CONTENT` before invoking supercronic
- [x] `backup.sh` no longer runs inside the `drill_reminder` container
- [x] `CRONTAB_CONTENT` env var removed from `docker-compose.backup.yml` if the dedicated-crontab approach is chosen
- [ ] `docker compose -f docker-compose.backup.yml up drill_reminder` shows only the drill schedule in supercronic's log output (manual verify on server)

## Files likely touched
- `deploy/docker-compose.backup.yml`
- `deploy/Dockerfile.backup`

## Test plan
**Manual:**
- `docker compose -f deploy/docker-compose.backup.yml config` — verify drill_reminder crontab schedule is `0 8 1 * *`
- Trigger a manual run of `drill-reminder.sh` inside the container and confirm no `backup.sh` execution

## Handoff notes
Promoted from discovery `2026-04-22-backup-drill-reminder-crontab-dead-env-var.md` (found during OPS-003). AC-5 of OPS-003 silently broken in production; backup.sh runs twice nightly.

**2026-04-23 — implementer:** No code changes were required. The fix described in all three acceptance criteria (AC1–AC3) was already applied by the OPS-003 commit (`23dad42`), which baked `/etc/crontab.drill` into `Dockerfile.backup` (line 33: `0 8 1 * * /scripts/drill-reminder.sh`) and wired `drill_reminder` to use it via `command: ["supercronic", "/etc/crontab.drill"]` in `docker-compose.backup.yml` (line 66). `CRONTAB_CONTENT` env var is absent from the compose file. The discovery was promoted to a backlog task after OPS-003 had already resolved it in the same PR. AC4 (supercronic log output) requires a manual live-container smoke check on the server by the reviewer — cannot be verified without running Docker.
