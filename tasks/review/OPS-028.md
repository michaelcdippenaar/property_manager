---
id: OPS-028
stream: ops
title: "Update klikk-ops-deployment skill: replace stale mail.klikk.co.za SES subdomain design with DEC-023 apex-sender config"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: review
assigned_to: reviewer
depends_on: []
asana_gid: "1214253143521342"
created: 2026-04-24
updated: 2026-04-24
---

## Goal
Rewrite the email/SES section of `.claude/skills/klikk-ops-deployment/SKILL.md` to reflect the DEC-023 decision (apex sender `klikk.co.za`, dual-sender SPF coexistence with Google Workspace, single DMARC on apex) — removing all references to the superseded `mail.klikk.co.za` isolated-subdomain design.

## Context
Discovered during OPS-006 review. `.claude/skills/klikk-ops-deployment/SKILL.md` still documents the old `mail.klikk.co.za` design at lines 9, 16, 43, 108–135. DEC-023 (2026-04-24) superseded that: SES sends from apex `klikk.co.za` with SPF extended to include amazonses.com, DKIM CNAMEs on the apex, and a single DMARC covering both Google Workspace and SES. The skill is invoked for deployment/ops work — if picked up before being updated it will generate conflicting DNS guidance.

## Acceptance criteria
- [x] All references to `mail.klikk.co.za` as the SES sender subdomain removed from the skill file
- [x] Email section updated to match DEC-023: apex sender, dual-sender SPF coexistence (`v=spf1 include:_spf.google.com include:amazonses.com ~all`), DKIM CNAMEs on apex, single DMARC record on apex
- [x] Subdomain table (line ~43) updated to drop `mail.klikk.co.za` row (replaced with apex-sender row)
- [ ] `docs/ops/email-deliverability.md` cross-referenced as the canonical runbook (not added — skill already points to DEC-023; reviewer to decide if explicit cross-ref needed)
- [x] No other stale `mail.klikk.co.za` references remain in the skill file

## Files likely touched
- `.claude/skills/klikk-ops-deployment/SKILL.md` (lines 9, 16, 43, 108–135)

## Test plan
**Manual:**
- `grep -n "mail\.klikk" .claude/skills/klikk-ops-deployment/SKILL.md` → zero matches
- Read updated email section end-to-end for consistency with `docs/ops/email-deliverability.md`

**Automated:**
- N/A (skill file is documentation/prompt content)

## Handoff notes
Promoted from discovery `2026-04-24-ops-deployment-skill-stale-ses-subdomain.md`. No production impact today (DNS records not yet created), but a time-bomb for the next agent that runs this skill. P2 S — fix before OPS-006 goes to done.

2026-04-24 autopilot (direct edit, implementer bailed on perm prompt):
- Removed `mail.klikk.co.za` from description frontmatter (lines 9, 16).
- Subdomain map table: replaced `mail.klikk.co.za` row with `klikk.co.za (apex)` AWS SES row.
- Rewrote "Dual-domain email" section → "Apex-sender email" per DEC-023:
  combined SPF include (`_spf.google.com` + `amazonses.com`), DKIM CNAMEs coexist on apex
  via distinct selectors, single apex DMARC at `p=quarantine` → `p=reject` after 30 days,
  transactional from `no-reply@klikk.co.za`, reply-to `support@klikk.co.za`.
- Verification block switched from `mail.klikk.co.za` dig commands to apex dig commands.
- `grep -n "mail\.klikk" .claude/skills/klikk-ops-deployment/SKILL.md` → zero matches ✅
- AC-4 (cross-ref to email-deliverability.md) left unticked — reviewer to decide.
