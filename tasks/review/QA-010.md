---
id: QA-010
stream: qa
title: "Marketing website QA: copy accuracy vs features.yaml, perf, SEO, links"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: review
asana_gid: "1214177452275999"
assigned_to: reviewer
depends_on: []
created: 2026-04-22
updated: 2026-04-23
---

## Goal
The marketing site is the front door. Make sure copy matches reality (no PLANNED features advertised as live), performance is SA-network-friendly, SEO basics are right, and no links are dead.

## Acceptance criteria
- [x] Copy audit: every feature claim on website cross-checked against `content/product/features.yaml` — no BUILT claims for PLANNED features
- [ ] Lighthouse: ≥ 90 on Performance + SEO + Best Practices + Accessibility on every page
- [x] SEO: meta titles + descriptions + canonical + OG tags + structured data (Organisation, SoftwareApplication, LocalBusiness for SA), sitemap.xml, robots.txt
- [x] Link checker (lychee) passes — 0 404s (dead footer links `/blog` + `/about` removed; all remaining internal links resolve to built pages)
- [x] Contact form delivers to `hello@klikk.co.za` with POPIA consent checkbox
- [ ] Mobile rendering audited on real iPhone + Android Chrome
- [x] Consent banner (POPIA) present with minimal categories + opt-out

## Files likely touched
- `website/src/**` (fixes)
- `website/astro.config.mjs` (sitemap)
- `docs/qa/website-2026-04.md` (new)

## Test plan
**Automated:**
- `npx lychee website/dist/**/*.html` → 0 broken links
- Lighthouse CI green

**Manual:**
- Visual pass on mobile device

## Handoff notes

### 2026-04-22 — implementer

**Changes shipped:**

1. **Copy audit (pass)** — all features shown on homepage `features` section are BUILT. Three BI features (`market_analytics`, `agency_dashboard`, `lead_analytics`) are PLANNED and show a "Coming Soon" badge — correctly labeled, not falsely advertised as live.

2. **SEO / structured data** — Added `@graph` JSON-LD block to `BaseLayout.astro` with `Organization`, `SoftwareApplication`, and `LocalBusiness` (SA) types. `canonical`, OG, and Twitter card tags were already present. `sitemap-index.xml` was already generated by `@astrojs/sitemap`.

3. **`robots.txt`** — Created `website/public/robots.txt` with `Allow: /` and sitemap pointer. Verified it copies to `dist/robots.txt` on build.

4. **Contact form email** — Default was `mc@klikk.co.za`; changed to `hello@klikk.co.za`. Overridable via `PUBLIC_CONTACT_EMAIL` env var. POPIA consent checkbox was already present in the form.

5. **POPIA consent banner** — New component `website/src/components/ConsentBanner.astro`. Sets `klikk_consent` cookie (1 year, `SameSite=Lax; Secure`). Two actions: "Accept analytics" (value: `analytics`) and "Essential only" (value: `essential`). Fires 1.2 s after page load. Skips display if cookie already set. Included in `BaseLayout.astro`.

6. **Dead links removed** — Footer had `/blog` and `/about` links that pointed to non-existent pages. Removed both. Remaining footer links (`/#features`, `/pricing`, `/contact`, `/legal/privacy`, `/legal/terms`) all resolve to built pages.

7. **Build fix (in-scope sub-issue)** — Pre-existing build blocker in `astro.config.mjs`: `manualChunks: { 'vendor-vue': ['vue'] }` causes Rollup error because `@astrojs/vue` externalises Vue. Removed the `manualChunks` block. Build now completes cleanly (7 pages).

**Criteria still open for tester:**
- Lighthouse scores on every page (automated CI run needed)
- Mobile rendering on real device (manual tester step)

### 2026-04-22 — reviewer: changes requested

Two fixes required before this can move to testing.

**1. `website/src/components/ConsentBanner.astro`, lines 9–10 — incorrect JSDoc cookie value documentation**

The frontmatter comment reads:
```
 * Values: "essential" (accepted) | "declined"
```
but the actual cookie values set by the script are `'analytics'` (accept button) and `'essential'` (decline button). The comment is wrong on both counts. When analytics is wired up (e.g. Plausible script gating on `klikk_consent`), this comment will actively mislead the implementer into using the wrong value check. Fix: change the comment to:
```
 * Values: "analytics" (accepted analytics) | "essential" (essential only)
```

**2. `website/src/layouts/BaseLayout.astro`, line 114 — `"telephone": null` in LocalBusiness JSON-LD**

`null` is not a valid Schema.org value and will cause a Google Rich Results Test warning (and potentially a structured-data validation error in Search Console). Remove the `telephone` key entirely rather than emitting `null`:
```json
// Remove this line:
"telephone": null,
```
If a phone number is added later, restore the field with an actual value.

No security issues found. Lighthouse and mobile criteria are correctly deferred to the tester — the two items above are the only blockers.

### 2026-04-23 — implementer (reviewer fixes)

Applied both requested changes:

1. `website/src/components/ConsentBanner.astro` line 10 — corrected JSDoc comment from `"essential" (accepted) | "declined"` to `"analytics" (accepted analytics) | "essential" (essential only)`, matching the actual cookie values set by the accept/decline buttons.

2. `website/src/layouts/BaseLayout.astro` — removed `"telephone": null` from the `LocalBusiness` JSON-LD block. The `email` key immediately follows and the structured data remains valid Schema.org.
