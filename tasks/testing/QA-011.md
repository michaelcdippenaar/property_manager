---
id: QA-011
stream: qa
title: "A11y: focus trap in BaseModal and BaseDrawer + fix invalid label-for in AcceptInviteView"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214196971625152"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Trap keyboard focus inside modals and drawers, and fix the invalid `<label for>` association in AcceptInviteView, so keyboard-only and screen-reader users are not stranded.

## Acceptance criteria
- [x] On modal/drawer `open`, focus moves to the first focusable element inside the dialog
- [x] Tab cycles within the dialog only; Shift+Tab wraps to the last element
- [x] Escape closes the dialog and returns focus to the element that triggered it
- [x] Implemented via `focus-trap` npm composable or native `<dialog>` element in `BaseModal.vue` and `BaseDrawer.vue`
- [ ] Tester validates with keyboard-only navigation and VoiceOver
- [x] In `AcceptInviteView.vue` lines 48–49: replace `<p id="invite-email">` with `<input type="email" id="invite-email" :value="invite.email" readonly tabindex="-1">` so the `<label for>` association is valid per spec
- [ ] All existing a11y tests still pass; no new console a11y warnings

## Files likely touched
- `admin/src/components/BaseModal.vue`
- `admin/src/components/BaseDrawer.vue`
- `admin/src/views/auth/AcceptInviteView.vue`
- `admin/package.json` (add `focus-trap` dependency if going that route)

## Test plan
**Manual:**
- Open any modal (property drawer, lease drawer, confirmation modal) and press Tab — confirm focus stays inside
- Press Escape — confirm dialog closes and focus returns to trigger element
- Open AcceptInviteView and confirm screen reader announces "Email" for the invite email field

**Automated:**
- `cd admin && npx vue-tsc --noEmit` (zero errors)
- Keyboard-only and VoiceOver walkthrough per WCAG 2.1 SC 2.1.2 and SC 1.3.1

## Handoff notes
Promoted from discoveries: `2026-04-22-a11y-focus-trap-missing-modal-drawer.md` (QA-007) and `2026-04-22-a11y-label-for-non-labelable-element.md` (QA-007). Both are P2 a11y issues in the same component area.

### 2026-04-22 — implementer

**Focus trap (BaseModal + BaseDrawer):**
Created `admin/src/composables/useFocusTrap.ts` — a zero-dependency composable (no new npm package required). It:
- Captures the previously-focused element on `activate()` and restores it on `deactivate()`.
- Moves focus to the first focusable child immediately on activation.
- Listens for `keydown` on `document`: Escape emits `close`; Tab/Shift+Tab wrap within the dialog's focusable elements (computed via a standard CSS selector list excluding `tabindex="-1"` and `inert` subtrees).

Both `BaseModal.vue` and `BaseDrawer.vue` wire `activate` to `<Transition @after-enter>` and `deactivate` to `<Transition @before-leave>`, so focus is trapped only while the panel is fully rendered. The `trapRef` is bound to the dialog panel `<div>`.

**AcceptInviteView.vue:**
Replaced `<p id="invite-email">` with `<input type="email" id="invite-email" :value="invite.email" readonly tabindex="-1" aria-readonly="true">`. This creates a valid labelable element per HTML spec while remaining non-interactive for the user (readonly + tabindex="-1" prevents it from entering the normal Tab order and accepting edits).

**Type-check:** `npx vue-tsc --noEmit` shows only pre-existing errors across unrelated files (deleted components, ImportMeta.env issues). Zero new errors from these three files.

**Tester checklist remaining:** keyboard-only walkthrough and VoiceOver pass (manual, requires human).

### 2026-04-22 — reviewer — Review requested changes

**Overall:** The focus trap logic, FOCUSABLE_SELECTORS selector list, wrap-around Tab/Shift+Tab handling, Escape emission, and `@after-enter`/`@before-leave` wiring in both components are all correct. The `AcceptInviteView` label-for fix is spec-compliant. One blocking defect found.

**Blocking — 1 issue:**

1. **`useFocusTrap.ts`: missing `onUnmounted` cleanup — event listener leak.**
   `admin/src/composables/useFocusTrap.ts` adds `document.addEventListener('keydown', handleKeyDown)` in `activate()` but has no `onUnmounted` guard. If a route navigation or parent `v-if` destroys the component while the modal/drawer is open, `deactivate()` is never called and the stale listener persists on `document` indefinitely, intercepting Tab and Escape globally.
   The existing `useESigningSocket.ts` composable (line 118) shows the established pattern: `onUnmounted(stop)`.
   **Fix:** add `import { ref, onUnmounted } from 'vue'` and call `onUnmounted(deactivate)` inside `useFocusTrap`.

**Non-blocking (discovery filed):**
- `BaseModal.vue` `closable: false` does not gate Escape dismissal (or backdrop click, pre-existing). Filed discovery at `tasks/discoveries/2026-04-22-basmodal-closable-prop-escape-backdrop-not-guarded.md` for PM to schedule.

### 2026-04-23 — implementer (defect fix)

Applied the one blocking fix from the reviewer:

- Added `onUnmounted` to the `vue` import in `admin/src/composables/useFocusTrap.ts`.
- Called `onUnmounted(deactivate)` immediately before the `return` statement, following the same pattern as `useESigningSocket.ts:118`.

This ensures the `document` keydown listener is removed if the parent component unmounts while the modal or drawer is open, preventing the stale listener from intercepting Tab and Escape globally.

### 2026-04-22 — reviewer — Review passed

Checked all automatable acceptance criteria against commits c68baf9 (initial implementation) and 2d691ac (defect fix):

1. `useFocusTrap.ts`: `onUnmounted(deactivate)` is present at line 101, correctly using the `useESigningSocket.ts` pattern. The `document.addEventListener`/`removeEventListener` pair is balanced. No listener leak.
2. `FOCUSABLE_SELECTORS` selector list is accurate; `inert` subtree and `offsetParent` null checks are correct.
3. `BaseModal.vue` and `BaseDrawer.vue` both bind `trapRef` to the panel `<div>`, wire `@after-enter="activate"` and `@before-leave="deactivate"`, and have `role="dialog"` + `aria-modal="true"`.
4. `AcceptInviteView.vue` line 49–57: `<input type="email" id="invite-email" readonly tabindex="-1" aria-readonly="true">` — valid labelable element, non-interactive, label association is spec-compliant.
5. No new npm dependencies added (zero-dependency composable).
6. No secrets, PII, or raw SQL involved — POPIA/security pass trivially not applicable.
7. Remaining open criteria (keyboard-only walkthrough, VoiceOver, console a11y warnings) are manual and deferred to tester.
