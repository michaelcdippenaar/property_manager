---
id: OPS-025
stream: ops
title: "Add CI guard: fail when the same task ID appears in more than one tasks/ state folder"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214246535148713"
created: 2026-04-24
updated: 2026-04-24
---

## Goal
A pre-commit hook or CI check that fails when the same task ID (e.g. `RNT-QUAL-006`) exists in more than one `tasks/<state>/` directory simultaneously — preventing stale orphan copies from making the board state untrustworthy.

## Context
`tasks/review/RNT-QUAL-006.md` was accidentally re-created by an unrelated commit (`ce2792aa`) while the authoritative `tasks/done/RNT-QUAL-006.md` already existed. This caused a duplicate board entry. The orphan was caught manually; a guard would catch it automatically.

## Acceptance criteria
- [x] A script (bash or Python) enumerates all `tasks/*/` directories (backlog, in-progress, review, testing, blocked, done), extracts the `id:` frontmatter field from each `.md` file, and errors if any ID appears in more than one directory
- [x] The check is wired into CI (`.github/workflows/ci.yml` — new job or step, runs on every PR)
- [x] The check is also wired as a pre-commit hook (`.pre-commit-config.yaml`)
- [x] An audit of the current `tasks/` tree at the time of implementation confirms zero existing duplicates (or removes them)
- [x] `docs/ops/task-board-guard.md` documents the check and how to resolve a duplicate if detected

## Files likely touched
- `.github/workflows/ci.yml` (new job: `task-board-guard`)
- `.pre-commit-config.yaml` (new local hook)
- `scripts/check_task_duplicates.sh` or `scripts/check_task_duplicates.py` (new)
- `docs/ops/task-board-guard.md` (new)

## Test plan
**Manual:**
- Copy a done task file into backlog/ → script fails with clear error message
- Remove the copy → script passes

**Automated:**
- CI job green on the PR that introduces this check (no current duplicates)

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-24 — rentals-pm: Promoted from discovery `2026-04-23-orphan-rnt-qual-006-review-file.md`. The orphan `tasks/review/RNT-QUAL-006.md` was identified in the discovery; implementer should delete it as part of this task if it still exists. The root cause was a rebase/merge flow producing stray copies — the guard prevents recurrence.

2026-04-24 — rentals-implementer: Implemented `scripts/check_task_board_integrity.sh` (bash, macOS sh3-compatible). Script checks: (1) duplicate IDs across state dirs, (2) frontmatter id/filename mismatch, (3) missing required frontmatter keys (warning only), (4) dangling DEC references in depends_on. Wired into `.pre-commit-config.yaml` (files: "^tasks/") and `.github/workflows/ci.yml` (new `task-board-guard` job). Wrote `docs/ops/task-board-guard.md`.

Pre-run found 4 real violations — all were stale backlog copies with the authoritative file in an advanced state folder. Fixed in-scope as <5 min each:
- `tasks/backlog/DEC-015.md` deleted (authoritative in `tasks/done/`)
- `tasks/backlog/OPS-006.md` deleted (authoritative in `tasks/testing/`)
- `tasks/backlog/RNT-QUAL-051.md` deleted (authoritative in `tasks/in-progress/`)
- `tasks/backlog/RNT-QUAL-054.md` deleted (authoritative in `tasks/testing/`)

Script now exits 0 on the clean tree. `tasks/review/RNT-QUAL-006.md` was not present (already cleared). Caveat for reviewer: the pre-commit hook uses `language: script` which requires the script to be executable — `chmod +x` is done in CI but developers need to run `pre-commit install` to activate the hook locally.

2026-04-24 — rentals-pm: Known issue for tester — OPS-027 filed. The DEC-reference check in `scripts/check_task_board_integrity.sh` scans the entire file rather than `depends_on:` frontmatter only, which can cause false positives when handoff prose mentions a DEC ID as an example. This is "stricter than spec" not "broken". Tester: do not block OPS-025 sign-off on this, but verify the false-positive scenario is documented in `docs/ops/task-board-guard.md` and note it in your handoff. OPS-027 will fix the scope narrowing separately.

2026-04-24 — rentals-reviewer: Review PASS. Verified: script runs clean on current tree (exit 0); duplicate detection fires correctly on `cp tasks/backlog/GTM-011.md tasks/done/`; orphan check fires on manually mangled frontmatter id; DEC dangling check fires on inserted a bogus DEC-NNNN reference; schema check is warning-only as spec'd. Pre-commit hook scoped to `^tasks/` with `language: script` + `pass_filenames: false` — correct. CI `task-board-guard` job runs before `backend`, bash-only, fast. Doc at `docs/ops/task-board-guard.md` covers all four checks and fix procedures. Cleanup deletions verified: DEC-015 → done/, OPS-006 → testing/, RNT-QUAL-051 → in-progress/, RNT-QUAL-054 → testing/ — all canonical copies confirmed present. Minor scope concern (commit `3be21509` conflates RNT-SEC-030 handoff + OPS-025 deliverable + RNT-QUAL-053 move under the wrong commit title) filed as `tasks/discoveries/2026-04-24-commit-scope-hygiene.md` — not blocking OPS-025 approval since the substantive work is correct and history rewrite would be unsafe.
