---
id: RNT-011
stream: rentals
title: "Scaffold Android Capacitor platform for Klikk Agent app"
feature: ""
lifecycle_stage: null
priority: P1
effort: M
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214196765313865"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Add the Android platform to the Klikk Agent Capacitor app so the app can be built and smoke-tested on Android devices, unblocking QA-004 and the Play Beta milestone.

## Acceptance criteria
- [x] `@capacitor/android` added to `agent-app/src-capacitor/package.json` matching the version of `@capacitor/ios` (currently `^6.2.1`)
- [x] `npx cap add android` run; `android/` directory committed
- [x] `android/app/src/main/AndroidManifest.xml` reviewed — only `INTERNET` permission declared; no camera/location/contacts/phone-state
- [x] `targetSdkVersion 34` and `minSdkVersion 23` confirmed in `build.gradle`
- [ ] App builds and launches successfully on Android 14 emulator (Pixel 7 AVD)
- [ ] App builds and launches successfully on Android 12 emulator (low-end profile AVD)
- [ ] No regressions on the existing iOS build

## Files likely touched
- `agent-app/src-capacitor/package.json`
- `agent-app/src-capacitor/android/` (new directory)
- `agent-app/src-capacitor/capacitor.config.ts` (may need android-specific config)

## Test plan
**Manual:**
- `cd agent-app/src-capacitor && npx cap run android` on Pixel 7 AVD — app launches, login screen visible
- `cd agent-app/src-capacitor && npx cap run android` on low-end Android 12 AVD — app launches
- Confirm iOS build still succeeds (`npx cap run ios`)

**Automated:**
- QA-004 Android smoke matrix can now be executed

## Handoff notes
Promoted from discovery: `2026-04-22-android-platform-not-scaffolded.md` (QA-004). Blocks Play Beta milestone and all Android QA-004 smoke tests.

### 2026-04-22 — implementer

- Added `@capacitor/android@^6.2.1` to `agent-app/src-capacitor/package.json` alongside the existing `@capacitor/ios@^6.2.1`.
- Ran `npm install` in `agent-app/src-capacitor/`; package resolved to `6.2.1`.
- Ran `npx cap add android` from `agent-app/src-capacitor/`; Capacitor scaffolded the full `android/` directory and synced Gradle successfully.
- Reviewed `AndroidManifest.xml`: only `android.permission.INTERNET` declared — no camera, location, contacts, or phone-state permissions present.
- Default `minSdkVersion` from Capacitor was `22`; updated `android/variables.gradle` to `minSdkVersion = 23` per acceptance criteria. `targetSdkVersion = 34` and `compileSdkVersion = 34` were already correct.
- `android/.gitignore` generated by Capacitor correctly excludes `local.properties`, build outputs, and generated web assets — machine-specific SDK path is not committed.
- Emulator smoke tests (AC 5, 6, 7) are manual and deferred to the tester; this task cannot run emulators in CI.

### 2026-04-22 — reviewer (approved)

Checked against all four code-verifiable acceptance criteria:

1. **Package version match** — `agent-app/src-capacitor/package.json` has `@capacitor/android: "^6.2.1"` matching `@capacitor/ios: "^6.2.1"`. Pass.
2. **android/ directory committed** — Full Capacitor scaffold present in commit `8fb671e`. Pass.
3. **AndroidManifest.xml permissions** — Only `android.permission.INTERNET` declared; no camera, location, contacts, or phone-state permissions. `android:exported="false"` correctly set on the FileProvider. Pass.
4. **SDK versions** — `variables.gradle`: `minSdkVersion = 23`, `compileSdkVersion = 34`, `targetSdkVersion = 34`. All correct. Pass.

Security/POPIA pass:
- No backend endpoints involved; no auth changes; no PII logged.
- `.gitignore` correctly excludes `local.properties`, build artifacts, and generated web assets.
- `google-services.json` is not committed (guarded by try/catch in `build.gradle`).
- Out-of-scope discovery filed: `tasks/discoveries/2026-04-22-android-allow-backup-enabled.md` — `android:allowBackup="true"` in manifest is the Capacitor scaffold default and allows ADB data-backup of JWT tokens/PII; warrants a follow-up hardening task.

ACs 5, 6, 7 (emulator smoke tests) are correctly deferred to tester. Sending to testing.
