---
id: QA-017
stream: qa
title: "Fix esigning_native_signing_test MCP tool: update public-link call to send signer_role"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214200406334153"
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Fix the `esigning_native_signing_test` MCP E2E tool so it can complete a full native signing round-trip by sending `{ signer_role: ... }` instead of `{ submitter_id: ... }` to the public-link endpoint.

## Acceptance criteria
- [x] `esigning_native_signing_test` sends `{ signer_role: submission.signers[0].role }` to `esigning/submissions/<id>/public-link/`
- [ ] Tool passes all steps end-to-end against a local lease with a native signing template (no HTTP 400)
- [x] `esigning_public_link` standalone tool (line 74-76) also updated to send `signer_role` instead of `submitter_id`
- [ ] All other esigning MCP tool steps continue to pass

## Files likely touched
- `services/tremly-mcp/lib/tools/esigning.mjs`

## Test plan
**Automated:**
- `mcp__tremly-e2e__esigning_native_signing_test` with a valid lease ID — all steps pass, no HTTP 400

## Handoff notes
Promoted from discovery `2026-04-22-esigning-native-signing-test-mcp-tool-stale-api-contract.md` (found during RNT-001). Tool always fails at `create_public_link` step; backend expects `signer_role` not `submitter_id`.

**2026-04-23 — implementer**
Three call-sites in `services/tremly-mcp/lib/tools/esigning.mjs` updated:
1. `esigning_public_link` standalone tool: schema parameter renamed from `submitter_id: z.coerce.number()` to `signer_role: z.string()`, POST body updated to match.
2. `esigning_native_signing_test` Step 3: changed `{ submitter_id: submission.signers?.[0]?.id || 1 }` to `{ signer_role: submission.signers?.[0]?.role }`.
3. `esigning_form_submit_test` inner signer loop (in-scope sub-issue — same endpoint, same bug): changed `{ submitter_id: signer.id }` to `{ signer_role: signer.role }`.

Smoke-check: `node --check lib/tools/esigning.mjs` passes cleanly. The two remaining acceptance criteria (end-to-end HTTP 400 absence, all steps pass) require a live backend run — left for tester.


**2026-04-23 — reviewer**
Review passed. Verified:
1. All three call-sites in `services/tremly-mcp/lib/tools/esigning.mjs` now send `signer_role` (standalone tool line 70-78, native signing test step 3 line ~161, form submit test inner loop line ~278).
2. Backend contract confirmed at `backend/apps/esigning/views.py:749-768` (`ESigningCreatePublicLinkView`): requires `signer_role` string, matched against `submission.signers[].role`, returns 400 otherwise.
3. Standalone tool schema updated consistently (zod `z.string()`, description added).
4. No remaining `submitter_id` references in MCP tool code (`services/tremly-mcp/lib/tools/*.mjs`). Only a stale row in `PLAN.md` — filed discovery `2026-04-23-tremly-mcp-plan-md-stale-submitter-id-reference.md`.
5. Security: public-link endpoint already enforces `IsAgentOrAdmin` + `can_manage_esigning` + submission-scoped queryset, unchanged. No auth/PII regression.
6. Remaining two acceptance boxes (end-to-end + all other steps) require a live backend + lease — tester to execute.
