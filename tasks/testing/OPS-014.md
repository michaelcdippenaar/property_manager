---
id: OPS-014
stream: ops
title: "Remove staging plaintext SSH password from DEVOPS.md and disable password SSH auth"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: null
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Remove the exposed staging server SSH password from the committed runbook and lock down the server to key-based auth only, eliminating a directly exploitable credential leak.

## Acceptance criteria
- [x] Inline `# password: pass` comments removed from `deploy/DEVOPS.md` lines 19 and 276
- [ ] Staging server SSH password rotated (server-side action by MC)
- [ ] `PasswordAuthentication no` set in `/etc/ssh/sshd_config` on staging server; `sshd` reloaded — key-based auth only
- [ ] MC's personal SSH public key added to `~/.ssh/authorized_keys` on staging server (the CI deploy key from OPS-012 §5a already satisfies CI access)
- [x] DEVOPS.md SSH examples updated to show key-based auth (`ssh -i ~/.ssh/klikk_staging mc@102.135.240.222`)

## Files likely touched
- `deploy/DEVOPS.md`

## Test plan
**Manual:**
- `ssh mc@102.135.240.222` with password — should fail (connection refused / permission denied)
- `ssh -i ~/.ssh/klikk_staging mc@102.135.240.222` — should succeed
- `grep -n "password" deploy/DEVOPS.md` — zero matches

**Automated:**
- N/A (server-side config + doc cleanup)

## Handoff notes
Promoted from discovery: `2026-04-22-devops-runbook-plaintext-password.md` (OPS-012). P1 — staging server is publicly reachable; credential is directly exploitable.

2026-04-22 (implementer): Removed both `# password: pass` inline comments from `deploy/DEVOPS.md` (were at lines 19 and 276). Updated all 7 `ssh mc@102.135.240.222` occurrences throughout the runbook to `ssh -i ~/.ssh/klikk_staging mc@102.135.240.222`. Added an SSH auth row to the Security Notes table (section 16) documenting that `PasswordAuthentication no` is required. The three server-side acceptance criteria (password rotation, sshd config change, personal SSH key added) require MC to execute manually on the staging server — they cannot be automated from this agent. Reviewer should confirm the doc changes look correct, then pass to testing once MC confirms the server-side steps are done.

2026-04-22 (reviewer): Review passed. Verified against commit 43b2194. Both `# password: pass` inline comments are gone from DEVOPS.md (lines 19 and 276 in the pre-patch file). All 8 occurrences of bare `ssh mc@102.135.240.222` are now `ssh -i ~/.ssh/klikk_staging mc@102.135.240.222` — zero bare password-auth examples remain. Section 16 Security Notes table has the new SSH auth row documenting `PasswordAuthentication no` and key-based-only access. `grep "# password:"` returns no matches. No secrets, PII, or raw SQL introduced. Security pass clean. The three server-side criteria (password rotation, sshd_config change, personal key in authorized_keys) are explicitly marked incomplete and require MC to execute on the staging server — tester must confirm these before closing.
