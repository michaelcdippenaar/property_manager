---
id: UX-004
stream: ux
title: "In-app coach marks + tooltip copy for agent first rental cycle"
feature: ""
lifecycle_stage: null
priority: P1
effort: M
v1_phase: "1.0"
status: testing
asana_gid: "1214176778251014"
assigned_to: tester
depends_on: [UX-001]
created: 2026-04-22
updated: 2026-04-24
---

## Goal
Design the in-app coach marks, tooltip copy, and empty-state microcopy that guide a new agent through their first rental cycle inside the app (not via video).

## Acceptance criteria
- [x] `content/onboarding/in-app/agent-coach-marks.md` — each screen, each tooltip, each empty state, exact copy + trigger + dismiss behaviour
- [x] Sequenced to the 15-step lifecycle (first 8 steps especially)
- [x] Copy is short (<15 words per tooltip), one idea per mark
- [x] Dismiss + "don't show again" states defined
- [x] Handoff includes a spec for the frontend dev (RNT task for implementation) — write the spec in the UX file, don't open the frontend task yet

## Files likely touched
- `content/onboarding/in-app/agent-coach-marks.md` (new)

## Test plan
**Manual review:**
- Copy matches voice
- Each coach mark ties to a specific user question / moment of confusion
- Not overwhelming (<6 marks active at any time)

## Handoff notes

### 2026-04-23 — implementer (ux-onboarding)

**Deliverable:** `content/onboarding/in-app/agent-coach-marks.md`

**Method:** Derived from UX-001 audit (`content/ux/audits/2026-04-agent-first-cycle.md`). Each coach mark directly addresses a named friction finding from that audit (F0.1, F1.2, F2.3, F3.2, F3.5, F4.5, F6.3, F6.5, F7.3, F7.5, F8.2, F8.3, etc.).

**What was authored:**
- 15 coach marks (CM-001 through CM-015) covering lifecycle steps 0–7 (first 8 steps as required)
- Full copy for every mark (all checked at ≤15 words)
- Trigger condition, dismiss behaviour, "don't show again" rule, max-concurrent constraint for every mark
- Global concurrency rule: max 2 marks active simultaneously
- Dismiss/don't-show-again state reference table
- Frontend implementation spec at the end of the document covering: backend model changes (2 new fields on UserProfile), shared `useCoachMarks` composable API, `CoachMark.vue` component props/emits, agent-app mirror, Pinia concurrency guard, and a suggested implementation order (8 steps)

**Lifecycle coverage:**
- Steps 0–7 covered in detail (Phase A: Pre-Tenancy + viewings + lease)
- Steps 8–15 explicitly noted as out of scope for v1 (features are PLANNED); a follow-up UX task should be authored when those features ship

**Concurrency check:** Maximum active at any time is 2. Summary table lists all 15 marks. Reviewer should verify the max-2 rule is respected in the suggested implementation order.

**PM note:** The frontend implementation spec recommends opening a new RNT task for the CoachMark component + backend fields. PM should author that task; do not open it here.

**No discoveries filed.** All marks are directly in-scope for UX-004. The backend field additions (onboarded, dismissed_coach_marks) are the minimum needed to make this spec buildable — they are called out in the spec rather than filed as a discovery because they are a natural dependency of this UX work, not an unrelated finding.

### 2026-04-23 — reviewer (rentals-reviewer)

**Review requested changes:**

1. **Route path error in CM-011 (line 232):**
   - Current: `/viewings/:id/lease/new`
   - Correct: `/viewings/:id/lease` (no `/new` suffix)
   - Reason: Agent app router defines the lease creation route at `viewings/:id/lease` (agent-app/src/router/index.ts:155-160), not with a `/new` segment. This discrepancy will cause confusion during frontend implementation when developers look for the non-existent route.
   - Fix: Update the **Screen** field for CM-011 from "Agent app — `/viewings/:id/lease/new` (CreateLeasePage)" to "Agent app — `/viewings/:id/lease` (CreateLeasePage)".

**Strengths:**
- All 15 marks are present, well-structured, and grounded in UX-001 audit findings
- Copy is consistently under 15 words with strong, actionable language
- Trigger conditions are implementable and clear
- Global max-2 concurrency rule is stated and respected
- Frontend spec is detailed and includes a sensible implementation order
- Dismiss/don't-show-again states are thoroughly documented
- Lifecycle coverage is appropriate (steps 0–7 in detail, 8–15 deferred)

**After fix:** Document will be ready for frontend implementation RNT task.

### 2026-04-23 — implementer (one-line fix)

**Fix applied:** Updated CM-011 route path from `/viewings/:id/lease/new` to `/viewings/:id/lease` (line 232). This matches the actual agent-app router definition at `agent-app/src/router/index.ts:155-160`.

**Status:** Ready for review.

### 2026-04-24 — reviewer (rentals-reviewer, re-review)

**Review passed.** One-line fix verified:
- CM-011 route path corrected from `/viewings/:id/lease/new` to `/viewings/:id/lease`
- Validated against actual router definition at `agent-app/src/router/index.ts:155`
- No regressions; all acceptance criteria remain satisfied
- Document ready for frontend implementation task
