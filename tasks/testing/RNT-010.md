---
id: RNT-010
stream: rentals
title: "Add agent-app .env example files for developer onboarding"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214181227868477"
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Add `.env.development.example`, `.env.staging.example`, and `.env.production.example` files to `agent-app/` documenting all required env vars (matching the pattern already in `admin/`), so a fresh checkout has a clear checklist of what to populate.

## Acceptance criteria
- [x] `agent-app/.env.development.example` exists with `API_URL` and `GOOGLE_CLIENT_ID` as placeholder keys with a comment pointing to `.env.secrets`
- [x] `agent-app/.env.staging.example` and `agent-app/.env.production.example` (or a single `agent-app/.env.example`) created with the same vars
- [x] Files committed and tracked in git (no sensitive values)
- [x] `agent-app/README.md` or equivalent onboarding doc references the example files

## Files likely touched
- `agent-app/.env.development.example` (new)
- `agent-app/.env.staging.example` (new)
- `agent-app/.env.production.example` (new)

## Test plan
**Manual:**
- Fresh checkout: follow the example file to populate real values and confirm `quasar dev` starts without env errors

## Handoff notes
(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

2026-04-22 — Promoted from discovery `2026-04-22-agent-app-missing-env-example.md` found during RNT-SEC-001. DX hardening follow-up after secrets were removed in that task.

2026-04-23 — Implementation complete. Created three `.env.*.example` files following admin pattern: `.env.development.example` (localhost dev), `.env.staging.example` (CI-injected), `.env.production.example` (CI-injected). All files use placeholder values only (API_URL, GOOGLE_CLIENT_ID, SENTRY_DSN, etc.), no real secrets committed. Created `agent-app/README.md` with dev setup instructions that reference the example files. Files match quasar.config.js env var schema (API_URL, GOOGLE_CLIENT_ID, SENTRY_DSN/ENVIRONMENT/RELEASE/TRACES_SAMPLE_RATE).

2026-04-23 — Review passed. Verified all four ACs: three `.env.*.example` files exist with API_URL + GOOGLE_CLIENT_ID placeholders, Sentry vars commented out, no real secrets. Grepped `process.env` / `import.meta.env` across agent-app src — all build-time vars referenced in quasar.config.js are covered (API_URL, GOOGLE_CLIENT_ID, SENTRY_DSN, SENTRY_ENVIRONMENT, GIT_COMMIT→SENTRY_RELEASE, SENTRY_TRACES_SAMPLE_RATE). README.md created with explicit `cp` setup instructions referencing each example file. Placeholder values only (your-client-id.apps.googleusercontent.com, backend.klikk.co.za is a public hostname). Dropped two discoveries: `2026-04-23-agent-app-hardcoded-dev-credentials.md` (P1 — real-looking creds hard-coded in LoginPage dev default) and `2026-04-23-agent-app-capacitor-server-url-example.md` (P2 — missing CAPACITOR_SERVER_URL in example file for live-reload DX).
