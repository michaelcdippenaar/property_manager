---
id: RNT-028
stream: rentals
title: "Typography pass round 2 — fix half-bump leaks, restore H1 hierarchy, WCAG AA"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214280574996340"
created: 2026-04-26
updated: 2026-04-26
---

## Goal

Complete the typography pass started in RNT-025 by closing the half-bump leaks, restoring H1 hierarchy (33% gap compressed), and eliminating all sub-WCAG-AA font sizes across the admin app.

## Background

RNT-025 bumped the Tailwind small tier (12/13/15px) but missed body default, `.page-title`, hard-coded px values in CSS files, and 10–11px WCAG violations in AppLayout.vue. MC reports the app still feels both small AND empty. Audit by Opus identified the root cause: hierarchy compression — H1 is only 33% bigger than body, with no visual anchor.

## Acceptance criteria

- [x] **1. Tailwind config** (`admin/tailwind.config.js`) — extend fontSize tier:
  ```js
  fontSize: {
    micro: ['12px', { lineHeight: '17px' }],
    xs:   ['13px', { lineHeight: '18px' }],
    sm:   ['15px', { lineHeight: '22px' }],
    base: ['16px', { lineHeight: '25px' }],
    lg:   ['18px', { lineHeight: '28px' }],
    xl:   ['22px', { lineHeight: '30px' }],   // was 20 → H1 anchor
    '2xl':['28px', { lineHeight: '34px' }],   // was 24 → stat values
    '3xl':['34px', { lineHeight: '40px' }],
  }
  ```
- [x] **2. `admin/src/assets/main.css`**:
  - body `font-size: 16px` (was 15px)
  - `.page-title { @apply text-2xl font-bold ... }` (was text-xl)
  - Fix `.text-micro` alias: `@apply text-[12px] text-gray-500` (currently does `@apply text-xs` — token drift)
- [x] **3. `admin/src/styles/klikk-components.css`**: replace every hard-coded `font-size: 13px` with `14px`; bump `12px → 13px`; lift `.klikk-page-header h1` to 30px.
- [x] **4. `admin/src/assets/agent-shell.css`**: apply 12→13 / 13→14 / 14→15 sweep on `.tab`, `.btn`, `.ev-row`, `.search input`, `.badge`, `.ledger td`. Scoped — safe.
- [x] **5. WCAG AA fixes** — search & replace:
  - All `text-[10px]` → `text-[11px]` minimum (5 occurrences) — under WCAG floor
  - In `admin/src/components/layout/AppLayout.vue`: bump 11 occurrences of `text-[11px]` → `text-xs` (13px) for sidebar labels
  - `tiptap-editor.css:43,148` 10px → 12px
  - `klikk-badge.size-sm` 11px → 12px
- [x] **6. Contrast sweep** — replace hot `text-gray-400` on white → `text-gray-500` (currently 4.4:1, fails AA 4.5:1 body) at:
  - `OnboardingItem.vue:23`
  - `MoveInChecklist.vue:7,86`
  - `OverdueMaintenanceWidget.vue:35`
  - `PropertyInformationEditor.vue:25`
  - `LeaseTimelineGantt.vue:8`
- [ ] **7.** `cd admin && npm test`  *(deferred to tester — implementer ran node-mode tests only)* — no new regressions vs RNT-025 baseline (8 pre-existing failures in `signature-block-signing.browser.test.ts` are acceptable).
- [x] **8.** `cd admin && npx vite build` — must succeed.
- [x] **9.** Added `admin/src/__tests__/typography-tokens.node.test.ts` asserting Tailwind `xl: 22px`, `2xl: 28px`, and body `16px` in main.css (node-mode test — no DOM mount needed).

## Out of scope

- DocumentPage.vue 14px lease body — print fidelity, needs legal review (file as separate discovery)
- TipTap editor chrome below 12px — editor-only, separate ticket
- The two competing stat-tile components (24px sans vs 28px serif) — UX decision required

## Files likely touched

- `admin/tailwind.config.js`
- `admin/src/assets/main.css`
- `admin/src/styles/klikk-components.css`
- `admin/src/assets/agent-shell.css`
- `admin/src/components/layout/AppLayout.vue`
- `admin/src/assets/tiptap-editor.css`
- `admin/src/components/onboarding/OnboardingItem.vue`
- `admin/src/components/move-in/MoveInChecklist.vue`
- `admin/src/components/maintenance/OverdueMaintenanceWidget.vue`
- `admin/src/components/property/PropertyInformationEditor.vue`
- `admin/src/components/leases/LeaseTimelineGantt.vue`

## Test plan

**Manual:**
- Open admin app dev server (`cd admin && npm run dev`)
- Check dashboard page: body text should read at 16px, H1/page-title visibly larger than body
- Check sidebar in AppLayout: all labels should be legible (13px minimum)
- Check badge sizes: `size-sm` badge text should be 12px minimum
- Verify no text appears extremely small or crowded after the bump

**Automated:**
- `cd admin && npm test` — full Vitest suite, expect no new failures beyond the 8 pre-existing in `signature-block-signing.browser.test.ts`
- `cd admin && npx vite build` — clean production build
- New Vitest snapshot test: assert Tailwind token values for `xl` (22px), `2xl` (28px), and body default (16px)

## Handoff notes

(Each agent appends a dated entry here on handoff. Do not edit prior entries.)

### 2026-04-26 — implementer → reviewer

Applied all six edit groups. Notes / deviations:

- **Tailwind tokens (AC1):** extended `fontSize` with `base`, `lg`, `xl: 22px`, `2xl: 28px`, `3xl: 34px` plus comment explaining the H1/stat anchor lift.
- **AppLayout location:** task referenced `admin/src/components/layout/AppLayout.vue` but the actual file is `admin/src/components/AppLayout.vue` (no `layout/` subdir). Worked on the real file.
- **AppLayout 11px count:** task expected "11 occurrences" of `text-[11px]`; the real file had **3 pre-existing** + **5 from the 10→11 sweep** = 8 total. Of those, I bumped the four genuine sidebar/text labels to `text-xs` (lines that were section headers, dropdown sublabel/intro, and item-description) and left the four tight UI badges/avatars at `text-[11px]` (notification count circles inside `min-w-[16px] h-4` chips and the avatar initials inside a 28px circle) — bumping those to 13px would break the fixed-size pills/avatar. All remaining 11px values are at the WCAG-AA floor already.
- **`text-gray-400` → `text-gray-500` contrast sweep (AC6):** applied at exactly the six call sites listed. Other `text-gray-400` usages on the same files (icons, helper accents) were left alone — task explicitly listed lines.
- **tiptap-editor.css line numbers (AC5):** task said lines 43 + 148. Line 43 was already `11px` (likely from RNT-025's small-tier bump) and line 148 was `10px`; per AC5 intent ("editor 10px under WCAG floor → 12px") I lifted both to 12px. The `font-size: 9px` block at line 126 is part of the page-break ribbon UI and is editor-only chrome — left alone per "Out of scope > TipTap editor chrome below 12px".
- **klikk-components.css (AC3):** swept every 12→13 / 13→14, lifted `.klikk-page-header h1` to 30px and bumped contiguous body sizes (alert-strip, page-header sub, ev-act, ev-addr) so the +1 step is consistent rather than half-leaking.
- **Test (AC9):** went with a node-mode test (`*.node.test.ts`) reading the tailwind config + main.css with regex rather than mounting a Vue component. Browser-mode mounting + `getComputedStyle` would have required postcss/tailwind compilation pipeline inside the test, which is heavier and slower. The node-mode test runs in <30 ms via `npm run test:drift` and is sufficient to lock in the token values.
- **Verification:** `npx vite build` clean (7.46 s); `npm run test:drift` 3 files / 10 tests passing. Full browser Vitest suite NOT run by implementer — left for tester per workflow.

No discoveries dropped — out-of-scope items called out in the task body remain valid candidates (DocumentPage 14px, dual stat-tile typography, TipTap chrome below 12px).

### 2026-04-26 — reviewer → tester

Review passed. Verified all 9 ACs against commit a82845e6:

- **AC1 Tailwind config:** confirmed extended fontSize tier with micro/xs/sm/base/lg/xl(22)/2xl(28)/3xl(34) and inline comment explaining the H1+stat anchor lift.
- **AC2 main.css:** body 16px, .page-title now text-2xl, .text-micro alias correctly rewritten to text-[12px] text-gray-500 (drift fixed).
- **AC3 klikk-components.css:** every 12→13 / 13→14 swap applied; .klikk-page-header h1 lifted to 30px; alert-strip / ev-row body lines bumped consistently.
- **AC4 agent-shell.css:** scoped .tab/.btn/.ev-when/.search input/.badge/.ledger td all bumped per task. Remaining 11px (.tl-month tile) is at WCAG floor and acceptable.
- **AC5 WCAG floor:** zero remaining text-[10px] in src UI (only test-assertion strings). AppLayout count deviation justified — 4 sidebar/section labels lifted to text-xs; 4 fixed-size chip/avatar containers held at 11px (12px would break min-w-[16px] h-4 pills + 28px avatar). Tiptap 10→12 done; .klikk-badge.size-sm 11→12 done. font-size:9px page-break ribbon explicitly out of scope.
- **AC6 Contrast sweep:** all 6 listed call sites flipped text-gray-400 → text-gray-500. Other gray-400 usages on same files (icons / planned-pill chips) correctly left alone.
- **AC7 npm test:** deferred to tester per task.
- **AC8 vite build:** ran locally, clean (8.82s).
- **AC9 Token guard test:** ran typography-tokens.node.test.ts → 2 passing in 173ms; asserts xl=22, 2xl=28, body=16px and sanity-checks RNT-025 small tier.

No security / POPIA surface — pure CSS + Tailwind config + one DOM-less node test. Tight chip/avatar 11px exemptions are documented in handoff and justified. Handing to tester for full Vitest battery + manual smoke.
