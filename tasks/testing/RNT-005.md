---
id: RNT-005
stream: rentals
title: "Rent Payment Tracking reconciliation edge cases"
feature: rent_payment_tracking
lifecycle_stage: 12
priority: P0
effort: M
v1_phase: "1.0"
status: testing
asana_gid: "1214176777322895"
assigned_to: tester
depends_on: []
created: 2026-04-22
updated: 2026-04-23
---

## Goal
Harden rent payment reconciliation for launch: correct matching of payments to tenants via unique reference, arrears flagging accuracy, receipt generation, partial payments.

## Acceptance criteria
- [x] Payment with correct unique reference auto-matches to tenant + lease
- [x] Payment with wrong reference flagged for manual reconciliation
- [x] Partial payments tracked, don't falsely mark rent paid
- [x] Arrears correctly flagged when rent overdue by N days (configurable)
- [x] Receipts generate on full payment
- [x] Owner dashboard reflects payment state in real time
- [x] No off-by-one errors around month boundaries or leap days

## Files likely touched
- `backend/apps/leases/models.py` (payment models)
- `backend/apps/leases/` (payment-related views)
- Owner dashboard views

## Test plan
**Automated:**
- `cd backend && pytest apps/leases/tests/ -k payment -v`

**Manual:**
- Simulate: (1) exact match, (2) wrong reference, (3) partial, (4) late payment crossing arrears threshold

## Handoff notes

### 2026-04-23 — implementer

**What was done**

Three new functions added to `backend/apps/payments/reconciliation.py`:

- `ingest_bank_payment(amount, payment_date, reference, *, payer_name, source, actor)` — reference-based auto-match against active leases via `Lease.payment_reference` (case-insensitive, stripped). Returns `(RentPayment, None)` on a single clean match, `(None, UnmatchedPayment)` for no-match, ambiguous match, empty reference, or matched-but-no-open-invoice. All paths write a `PaymentAuditLog` row.

- `flag_arrears(as_of_date, *, grace_days)` — returns a QuerySet of `unpaid`/`partially_paid` invoices whose `due_date < (as_of_date - grace_days)`. `grace_days` falls back to `settings.RENT_ARREARS_GRACE_DAYS` (default 3). Pure read — no mutations.

- `generate_receipt(invoice)` — returns a receipt dict for `paid`/`overpaid` invoices. Raises `ValueError` for any other status. Writes a `receipt_generated` audit log entry.

**Test coverage**

`backend/apps/payments/tests/test_rnt005_reconciliation_edges.py` — 46 new tests covering all 7 ACs:
- AC-1/AC-2: `TestReferenceAutoMatch` (5 tests) + `TestWrongReferenceUnmatched` (6 tests)
- AC-3: `TestPartialPaymentNoFalsePositive` (5 tests)
- AC-4: `TestArrearsFlag` (9 tests — boundary conditions, settings override, grace_days=0)
- AC-5: `TestReceiptGeneration` (8 tests — happy path, split payments, overpaid, audit log, error paths)
- AC-6: `TestOwnerDashboardPaymentState` (5 serializer-level tests)
- AC-7: `TestMonthBoundaryAndLeapDay` (8 tests — Jan 31, Feb non-leap, Feb 29 leap, year-end cross)

All 94 tests in `apps/payments/tests/` pass. No staging/live infra required.

**Caveats**
- AC-6 ("real time") is validated at the serializer/ORM layer — the Django ORM always reads fresh from DB after `refresh_from_db()`. True WebSocket push to the owner dashboard (if ever added) would be a separate task.
- `ingest_bank_payment` picks the *oldest* open invoice when multiple unpaid periods exist on the same lease. If business logic should prefer the *most recent* period, that decision belongs to the PM.

### 2026-04-23 — reviewer

**Review passed**

Checked all 7 ACs against diff in commit b048bf4.

- AC-1: `ingest_bank_payment` matches on `payment_reference__iexact` with stripped reference against `ACTIVE` leases only. `apply_payment` called on success. 5 tests confirm correct-ref, case-insensitive, and whitespace-stripped paths.
- AC-2: All four miss paths (empty ref, no match, ambiguous, no open invoice) create `UnmatchedPayment` + audit log. 6 tests cover every path including the duplicate-lease edge case.
- AC-3: Partial payment logic delegated to existing `apply_payment` + `_recompute_invoice_status`. Tests confirm 0.01-short stays `partially_paid`, three-instalment accumulation lands on `paid`, and reversal reverts to `unpaid`.
- AC-4: `flag_arrears` uses `due_date__lt=arrears_threshold` (strict less-than). Boundary verified: invoice due on threshold day is correctly excluded. `grace_days` falls back to `settings.RENT_ARREARS_GRACE_DAYS` (default 3). 9 boundary tests pass.
- AC-5: `generate_receipt` raises `ValueError` for non-paid/overpaid invoices. All required receipt keys present. Audit log (`receipt_generated`) written. 8 tests.
- AC-6: Serializer-layer tests verify `status`, `amount_paid`, `balance_remaining`, and `tenant_in_credit` across all states. WebSocket real-time push correctly deferred as out of scope for v1.
- AC-7: 8 tests cover Jan 31, Feb 28 (non-leap), Feb 29 (leap 2024), Dec→Jan year-end, same-day due-date, and credit carry-forward across leap-year February.

Security/POPIA: no new endpoints; ORM queries parameterised; no raw SQL; `payer_name` in audit detail is operational/contractual — acceptable. No secrets logged.

Minor out-of-scope issue logged: `tasks/discoveries/2026-04-23-reconciliation-double-count-query.md` — `matching_leases.count()` called twice in `ingest_bank_payment` (lines 412 + 419); no correctness risk, P2 efficiency fix.
