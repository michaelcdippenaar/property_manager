---
id: UX-014
stream: ux
title: "Tenant chat empty state — add suggested prompts and capability description"
feature: ""
lifecycle_stage: null
priority: P2
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214229937588659"
created: 2026-04-23
updated: 2026-04-23

---

## Goal
Replace the generic chat empty state with suggested starter prompts and a description of the AI assistant's scope so tenants understand and engage with the feature from day one.

## Acceptance criteria
- [x] Empty state includes 2–3 tappable suggested prompts (e.g. "Report a repair", "When is my rent due?", "What does my lease say about pets?")
- [x] Tapping a prompt opens a new conversation with that text pre-filled in the input
- [x] Subtitle copy updated to describe scope: "Ask about your lease, maintenance requests, or anything about your property"
- [x] FAB has a visible label or tooltip: "New conversation" (currently unlabelled icon only)
- [ ] Prompts reviewed and approved by UX/GTM before merge (choice of prompts is content, not just code)
- [x] No regression on existing conversations list (prompts only shown when list is empty)

## Files likely touched
- `tenant/src/views/chat/ChatListView.vue` — line ~11, empty state section

## Test plan
**Manual:**
- Open chat tab as a tenant with no prior conversations; confirm prompts and updated subtitle visible
- Tap a suggested prompt; confirm new conversation opens with text pre-filled
- Create one conversation; confirm prompts disappear and normal conversation list renders
- Confirm FAB label visible on iOS and Android

**Automated:**
- Vitest snapshot or component test for `ChatListView` empty state branch

## Handoff notes
2026-04-23 rentals-pm: Promoted from discovery `2026-04-23-tenant-chat-no-empty-state-prompt.md` (found during UX-002 audit). P2 — engagement/onboarding polish. Prompt copy needs GTM/UX sign-off before implementing.

2026-04-23 implementer: RNT-SEC-044 was checked first — it added an empty state inside `ChatDetailView.vue` (zero-message state within an open conversation). UX-014 targets `ChatListView.vue` (zero-conversations list), so there is no overlap; both changes coexist cleanly.

Changes made:
- `tenant/src/views/chat/ChatListView.vue`: replaced generic subtitle with scope copy ("Ask about your lease, maintenance requests, or anything about your property"); added 3 tappable prompt chips below empty-state heading; added `startWithPrompt()` which calls `createConversation(prompt)` and navigates to the new conversation with `?prefill=<prompt>` query param; changed FAB to `fab--labeled` pill with "New conversation" label.
- `tenant/src/views/chat/ChatDetailView.vue`: reads `route.query.prefill` in `onMounted` and sets `messageText` so the text is pre-filled when the view opens.
- `tenant/src/assets/main.css`: added `.fab.fab--labeled` and `.fab-label` utility classes (extended FAB, pill shape).

Prompt copy (3 prompts: "Report a repair", "When is my rent due?", "What does my lease say about pets?") is placeholder copy matching the task examples — this AC item is flagged for GTM/UX sign-off before merge.

2026-04-23 reviewer: Review passed. All code-quality, convention, security, and POPIA checks passed.

Checked:
1. AC1 — suggestedPrompts array + v-for chip buttons in v-else-if="conversations.length === 0" block. Correct guard; prompts only render on empty state.
2. AC2 — startWithPrompt() calls createConversation(prompt) which pushes to chat-detail with ?prefill=<prompt>. ChatDetailView.vue reads route.query.prefill in onMounted and assigns to messageText via String() coerce. Route name chat-detail confirmed in tenant/src/router/index.ts line 103. No XSS risk — value goes into a v-model textarea, never rendered as HTML.
3. AC3 — Subtitle copy updated verbatim in template line 15-17.
4. AC4 — FAB changed to fab fab--labeled pill with <span class="fab-label">New conversation</span>. CSS utility added in main.css and follows existing @apply pattern.
5. AC5 (open) — GTM/UX copy sign-off not obtained. Prompt copy is placeholder. This AC item is intentionally unchecked. DO NOT merge to main until GTM confirms. Test gate can proceed.
6. AC6 — v-else-if / v-else structure ensures prompts are hidden once conversations.length > 0. No regression path.
7. RNT-SEC-044 coexistence confirmed — POPIA notice lives in ChatDetailView.vue inside v-else-if="messages.length === 0" (within-conversation scope). Entirely separate branch from ChatListView.vue zero-conversations empty state.
8. No backend changes, no new endpoints, no PII logged, no raw SQL.
9. prefill value is user-visible text only; it is never persisted or transmitted by the prefill logic itself — only submitted if the tenant taps Send.
10. No Vitest component test shipped for ChatListView empty state branch. The test plan lists this as an automated item — tester should chase this before sign-off.
11. createConversation error handler silently swallows errors (/* ignore */) — pre-existing pattern, not introduced by this change. Flagged as a discovery item: tasks/discoveries/2026-04-23-chat-create-conversation-silent-error.md.
