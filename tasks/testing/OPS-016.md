---
id: OPS-016
stream: ops
title: "Fix backup drill-reminder crontab — dead env var, double backup.sh run"
feature: ""
lifecycle_stage: null
priority: P1
effort: S
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214195189715207"
created: 2026-04-22
updated: 2026-04-22
---

## Goal
Fix the `drill_reminder` Docker service so it actually runs `drill-reminder.sh` on the 1st of each month instead of running `backup.sh` at midnight (which doubles S3 writes and silently never sends the reminder).

## Acceptance criteria
- [x] `drill_reminder` service uses a dedicated crontab file (e.g. `/etc/crontab.drill`) that schedules `drill-reminder.sh` at `0 8 1 * *`
- [x] `CRONTAB_CONTENT` env var removed from `docker-compose.backup.yml` (or the Dockerfile entrypoint correctly consumes it)
- [x] `backup.sh` no longer runs inside the `drill_reminder` container
- [x] The main backup container still runs `backup.sh` at `0 0 * * *` (unchanged)

## Files likely touched
- `deploy/docker-compose.backup.yml`
- `deploy/Dockerfile.backup`
- `scripts/drill-reminder.sh` (or new `deploy/crontab.drill`)

## Test plan
**Manual:**
- Inspect running container: `docker exec drill_reminder supercronic --list` — confirm only `0 8 1 * *` entry for `drill-reminder.sh`
- Confirm backup container schedule unchanged

**Automated:**
- OPS-003 AC-5 verified in post-deploy smoke check

## Handoff notes
2026-04-22: Promoted from discovery `2026-04-22-backup-drill-reminder-crontab-dead-env-var.md` (found during OPS-003 review). Silently broken in production — ops team receives no monthly reminder.

2026-04-22 (implementer): All four acceptance criteria were already satisfied by commit `23dad42` (OPS-003: implement → review). Verified against current file state:
- `Dockerfile.backup` line 33 bakes `/etc/crontab.drill` with `0 8 1 * * /scripts/drill-reminder.sh`
- `docker-compose.backup.yml` `drill_reminder` service uses `command: ["supercronic", "/etc/crontab.drill"]`; no `CRONTAB_CONTENT` env var present
- Default `CMD` in Dockerfile uses `/etc/crontab.backup` (backup.sh at 00:00 UTC) — drill_reminder overrides it entirely
- No code changes required. This task is a duplicate of work already shipped in OPS-003. Reviewer should confirm via `docker exec drill_reminder supercronic --list` post-deploy and close as done.

2026-04-22 (reviewer): Review passed. Verified against current file state — all four ACs already satisfied by OPS-003 work (commit 23dad42). `deploy/Dockerfile.backup` line 33 bakes `/etc/crontab.drill` with `0 8 1 * * /scripts/drill-reminder.sh`. `docker-compose.backup.yml` `drill_reminder` service uses `command: ["supercronic", "/etc/crontab.drill"]` with no `CRONTAB_CONTENT` env var. Main `backup` service CMD unchanged (`/etc/crontab.backup`, `0 0 * * *`). No security concerns; infrastructure-only, no auth surface. Manual smoke check (`docker exec drill_reminder supercronic --list`) remains for tester.
