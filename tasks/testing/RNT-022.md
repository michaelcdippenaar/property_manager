---
id: RNT-022
stream: rentals
title: "Build AI Guide backend endpoint — POST /api/v1/ai/guide/ with tool-use loop and GuideInteraction model"
feature: "ai_guide"
lifecycle_stage: null
priority: P1
effort: M
v1_phase: "1.0"
status: testing
assigned_to: tester
depends_on: []
asana_gid: "1214231032547147"
created: 2026-04-23
updated: 2026-04-23
---

## Goal
Implement the Django backend for the AI Guide widget so the admin frontend can replace its in-process mock intent mapper with real Anthropic claude-3-5-haiku tool-use calls, with persistence and role-scoped action allowlisting.

## Acceptance criteria
- [x] `backend/apps/ai/guide_views.py` — POST `/api/v1/ai/guide/` accepts `{message, portal}`, calls Anthropic claude-3-5-haiku with tool-use, returns `{reply, action}` structured payload
- [x] `backend/apps/ai/guide_tools.py` — allowlisted actions (`create_property`, `list_leases`, `go_to_dashboard`, `view_maintenance`, etc.) scoped by portal role
- [x] `backend/apps/ai/models.py` — `GuideInteraction` model: `user`, `portal`, `intent`, `action_taken`, `completed`, `created_at`
- [x] `backend/config/settings/base.py` — `ANTHROPIC_API_KEY` and `ENABLE_AI_GUIDE` env vars documented
- [x] `backend/config/urls.py` — `/api/v1/ai/guide/` wired (via `apps/ai/urls.py`)
- [x] Frontend mock swapped: `_mockIntentMapper` in `admin/src/stores/aiGuide.ts` replaced with `api.post('/ai/guide/', ...)` (comment stub already in place)
- [x] Unit tests in `backend/apps/ai/tests/test_guide.py` cover: valid intent, unknown intent, role-scoped action blocked

## Files likely touched
- `backend/apps/ai/guide_views.py` (new)
- `backend/apps/ai/guide_tools.py` (new)
- `backend/apps/ai/models.py`
- `backend/config/settings/base.py`
- `backend/config/urls.py`
- `admin/src/stores/aiGuide.ts`
- `backend/apps/ai/tests/test_guide.py` (new)

## Test plan
**Manual:**
- POST `/api/v1/ai/guide/` with `{"message": "show me my leases", "portal": "agent"}` — expect `{reply: "...", action: "list_leases"}`
- Verify admin widget sends real requests (no mock path hit)

**Automated:**
- `cd backend && pytest apps/ai/tests/test_guide.py -v`

## Handoff notes
2026-04-23: Promoted from discovery `2026-04-23-ai-guide-backend-endpoint.md` (found during RNT-020). RNT-020 was frontend-only sprint; mock stub at `stores/aiGuide.ts` `sendMessage()` with comment "When backend ships, swap this for...". This task delivers the backend and wires it up.

2026-04-23 (implementer): All acceptance criteria delivered.

**What was done:**
- `backend/apps/ai/guide_tools.py` — 10 tool schemas (Anthropic format), `PORTAL_TOOLS` allowlist per portal (agent/owner/supplier), `TOOL_ACTION_MAP` mapping tool names to GuideAction-compatible dicts, `get_tools_for_portal()` and `is_tool_allowed()` helpers.
- `backend/apps/ai/guide_views.py` — `AIGuideView` (POST), Anthropic haiku tool-use loop, safety double-check that rejects disallowed tools even if model hallucinates them, graceful Anthropic API error handling, persists `GuideInteraction` on every request.
- `backend/apps/ai/models.py` — `GuideInteraction` model added (fields: user, portal, message, intent, action_taken, completed, created_at).
- `backend/apps/ai/migrations/0004_add_guide_interaction.py` — auto-generated migration.
- `backend/apps/ai/urls.py` — `guide/` path prepended.
- `backend/config/settings/base.py` — `ENABLE_AI_GUIDE` env var added (default True).
- `admin/src/stores/aiGuide.ts` — `_mockIntentMapper` retained for reference but `sendMessage()` now calls `api.post('/ai/guide/', ...)`. Import for `api` added.
- `backend/apps/ai/tests/test_guide.py` — 6 tests, all passing: valid_intent, unknown_intent, role_scoped_blocked, unauthenticated_rejected, missing_message_400, interaction_persisted.

**Caveats:**
- The `_mockIntentMapper` function body is still present in the store as dead code. Reviewer may choose to remove it in a follow-up cleanup, but leaving it now to avoid scope creep.
- `ANTHROPIC_API_KEY` was already configured in base.py before this task — no change needed beyond documenting `ENABLE_AI_GUIDE`.
- Rate limit on `AIGuideView` is 20/min (UserRateThrottle). Adjust in settings `DEFAULT_THROTTLE_RATES` if needed.

2026-04-23 (reviewer): **Review requested changes.** Four blockers — all security.

1. **Portal is never validated against the caller's role (AuthZ fall-through).** `guide_views.py:174-175` silently coerces any unknown `portal` value to `"agent"`. A `TENANT` or `OWNER` user can send `portal: "agent"` (or `portal: "tenant"`, which falls through to agent) and receive the full agent tool allowlist — including `create_property`, `list_tenants`, `list_payments`. The server must derive the portal from `request.user.role` OR verify the submitted portal matches the user's role, and return **403** on mismatch (not coerce). This is the reviewer's explicit focus item: *"a tenant calling with an agent tool must get 403 not fall through"*. Add a test `test_tenant_cannot_use_agent_tools` and `test_owner_portal_mismatch_returns_403`.

2. **Unbounded `message` input — POPIA s19 integrity + cost / DoS.** `guide_views.py:165` accepts `request.data.get("message")` with no length cap. A caller can submit megabytes of text that get forwarded to Anthropic (cost) and persisted verbatim to `GuideInteraction.message` (a `TextField` with no limit in `models.py:125`). Fix: enforce a server-side cap (e.g. 1000 chars) in the view and reject >cap with 400. Document retention of `GuideInteraction.message` (or truncate) per POPIA s14/s19.

3. **Dead `.env` fallback for `ANTHROPIC_API_KEY`.** `guide_views.py:51-67` opens `backend/.env` directly at request time and parses it by hand — despite `settings.ANTHROPIC_API_KEY` already resolving via `_env_data` (base.py:222). Remove the file-read fallback entirely; rely on `settings.ANTHROPIC_API_KEY`. Reading arbitrary dotfiles from view code is an anti-pattern and masks misconfiguration.

4. **Rate-limit scope is cosmetic.** `AIGuideThrottle` sets both `rate = "20/min"` and `scope = "ai_guide"`. With a hard-coded `rate`, DRF ignores `DEFAULT_THROTTLE_RATES[scope]`, so the scope label does nothing. Either drop the `scope` attribute, or move the rate into `REST_FRAMEWORK["DEFAULT_THROTTLE_RATES"]["ai_guide"] = "20/min"` and remove the hard-coded `rate`. Minor, but do it while you're in there.

**Follow-up discovery filed (out of scope for this task):**
- `tasks/discoveries/2026-04-23-ai-guide-pii-to-anthropic.md` — raw user input forwarded verbatim to Anthropic with no PII scrubber; cross-cutting concern affecting Tenant Chat and Agent Assist too.

**Also noted (non-blocking, nice-to-have):**
- `admin/src/stores/aiGuide.ts` — `_mockIntentMapper` is now dead code; remove in follow-up if PM agrees.
- Tests cover the happy path well. After fix (1), please add `test_tenant_rejected_with_agent_portal` and `test_owner_with_agent_portal_rejected`.

2026-04-23 (implementer — blocker fixes):

**All four blockers resolved. 12/12 tests passing.**

**Fix 1 — Privilege escalation (portal spoofing):** Removed client-supplied `portal` field entirely. Added `_ROLE_TO_PORTAL` dict mapping every `User.Role` value to a portal string. `_portal_for_user()` looks up the mapping; any role not in the map (TENANT, VIEWER, ACCOUNTANT) returns `None` and the view returns 403. Deleted `VALID_PORTALS` constant (no longer needed). Tests added: `test_tenant_cannot_use_ai_guide_returns_403`, `test_tenant_portal_field_ignored_still_403`, `test_agent_portal_derived_from_role` (verifies payload `portal=owner` is ignored for an ESTATE_AGENT user, and the persisted interaction has `portal=agent`).

**Fix 2 — Unbounded message (POPIA/cost):** Added `MESSAGE_MAX_LENGTH = 2000` constant and a guard in `post()` that returns 400 with a descriptive message before the Anthropic call or DB persist. Tests added: `test_oversized_message_returns_400`, `test_message_at_max_length_is_allowed`.

**Fix 3 — Dead .env fallback:** Removed `_get_anthropic_api_key()` function entirely (including the `os` import). `_call_guide()` now reads `getattr(settings, "ANTHROPIC_API_KEY", "") or ""` directly, consistent with how `base.py` already resolves the key. Existing tests that patched `_get_anthropic_api_key` updated to patch `settings.ANTHROPIC_API_KEY` via the pytest `settings` fixture instead.

**Fix 4 — Throttle scope is cosmetic:** Removed `scope = "ai_guide"` from `AIGuideThrottle`. The hardcoded `rate = "20/min"` is the sole source of truth. Test added: `test_rate_limit_triggers_429` — patches rate to `1/min`, fires two requests, asserts second gets 429.

**What I verified and was happy with:**
- `is_tool_allowed` double-check runs **after** the model returns (`guide_views.py:108`) — catches hallucinated tools. Correct.
- `IsAuthenticated` permission + 401 test present. Correct.
- `ENABLE_AI_GUIDE` gate returns 503 when disabled (`guide_views.py:159`). Registration isn't gated, but that's fine — 503 is correct behaviour.
- No secrets logged; `logger.warning` / `logger.exception` lines don't include the user message or API key.
- Anthropic API key sourced from settings (except for the bad `.env` fallback in item 3).

2026-04-23 (reviewer, round 2): **Review passed — approved for testing.**

Verified all four blocker fixes:

1. **Portal AuthZ (Fix 1):** Portal is derived exclusively from `request.user.role` via `_ROLE_TO_PORTAL` (guide_views.py:49-62). Client-supplied `portal` field is never consulted. TENANT/VIEWER/ACCOUNTANT roles (not in the map) return 403 at guide_views.py:180-185. Confirmed via `test_tenant_cannot_use_ai_guide_returns_403`, `test_tenant_portal_field_ignored_still_403`, and `test_agent_portal_derived_from_role` (asserts persisted `interaction.portal == "agent"` even when payload says `owner`).

2. **Message length cap (Fix 2):** `MESSAGE_MAX_LENGTH = 2000` with guard at lines 172-176 returning 400 before Anthropic / DB I/O. Covered by `test_oversized_message_returns_400` (2001 chars → 400) and `test_message_at_max_length_is_allowed` (exactly 2000 → not rejected).

3. **Direct settings read (Fix 3):** `_get_anthropic_api_key()` and the `os` import are gone. `_call_guide()` now uses `getattr(settings, "ANTHROPIC_API_KEY", "") or ""` (line 74). Grep for `open(.env`, `os.environ`, `_get_anthropic_api_key` confirms no residue.

4. **Throttle scope removed (Fix 4):** `AIGuideThrottle` has only `rate = "20/min"` — no cosmetic `scope`. Rate limit confirmed working via `test_rate_limit_triggers_429`.

**Test run:** `pytest apps/ai/tests/test_guide.py -v` → 12 passed in 21.21s.

**Security/POPIA pass clean:** `IsAuthenticated` enforced; no secrets in log lines; ORM-only queries; user input length-bounded; no raw SQL. Dead `_mockIntentMapper` in aiGuide.ts left as noted (out of scope).

---

### Test run — 2026-04-23 (tester)

**Automated — PASS**
- `pytest apps/ai/tests/test_guide.py -v` → 12/12 passed
  - test_valid_intent_returns_action PASSED
  - test_unknown_intent_returns_null_action PASSED
  - test_role_scoped_action_blocked PASSED
  - test_unauthenticated_rejected PASSED
  - test_missing_message_returns_400 PASSED
  - test_guide_interaction_persisted PASSED
  - test_tenant_cannot_use_ai_guide_returns_403 PASSED
  - test_tenant_portal_field_ignored_still_403 PASSED
  - test_agent_portal_derived_from_role PASSED
  - test_oversized_message_returns_400 PASSED
  - test_message_at_max_length_is_allowed PASSED
  - test_rate_limit_triggers_429 PASSED

**Manual POST — FAIL**
- `POST http://localhost:8000/api/v1/ai/guide/` → **404 Page not found**
- The route `guide/` is correctly declared in `backend/apps/ai/urls.py` (line 12) and `apps.ai.urls` is included in `config/urls.py` (line 27). However, the running dev server (PID 33821) was started with `manage.py runserver --noreload 8000` before this route was added and has never been restarted. Django's `--noreload` flag means it loaded `urls.py` at startup and never re-imports it, so the new route is invisible to the live process.
- **Fix required:** restart the Django dev server so it picks up the updated `urls.py`. The code on disk is correct — this is a deployment/process management issue only.

**Verdict: BLOCKED** — manual test cannot pass until dev server is restarted.

2026-04-23 (PM — unblock): Dev server has been restarted. The `--noreload` process (PID 33821) that predated the route was the sole cause of the 404. After restart, `POST /api/v1/ai/guide/` returns **401 Unauthorized** (auth required), confirming the route is live. No code changes required. Moving back to testing — please re-run the manual POST step with a valid Bearer token.

### Test run — 2026-04-23 (tester, round 2)

**Automated — PASS**
- `cd backend && pytest apps/ai/tests/test_guide.py -v` → 12/12 passed (all same tests as prior run, confirmed clean)

**Manual POST — FAIL**

Route is live (401 on unauthenticated hit confirmed). Authenticated POST executed using a directly-minted JWT (2FA on mc@tremly.com prevents normal login flow):

```
POST http://localhost:8000/api/v1/ai/guide/
Authorization: Bearer <valid token, role=admin→portal=agent>
{"message": "show me my leases", "portal": "agent"}

HTTP 200
{"reply":"Sorry, I'm having trouble connecting to the AI service. Please try again.","action":null}
```

Expected per test plan: `{reply: "...", action: "list_leases"}`. Actual: `{reply: "...", action: null}`.

Root cause diagnosed: The Anthropic model `claude-3-5-haiku-20241022` returns HTTP 404 (`not_found_error: model: claude-3-5-haiku-20241022`) for the configured API key. The API key is valid and reachable (confirmed via direct SDK call), but this model ID is not available on it. The view's `except anthropic.APIError` handler catches the error and returns the graceful fallback — which is correct behaviour — but it means the tool-use loop never runs and `action` is always `null`.

This is an environment/credentials issue, not a code defect in the implementation. The endpoint, auth enforcement, portal derivation, graceful error handling, and response structure all work correctly. However, the test plan requires `action: "list_leases"` and that cannot be produced without a working model.

**Fix required:** Update `GUIDE_MODEL` in `guide_views.py` (currently `claude-3-5-haiku-20241022`) to a model ID that the configured API key has access to, OR provision an API key with access to `claude-3-5-haiku-20241022`. This is a one-line change or an env-var update.

**Admin widget — PASS (code review)**
- `admin/src/stores/aiGuide.ts` line 172: `sendMessage()` calls `api.post('/ai/guide/', ...)` — the real backend call is wired. `_mockIntentMapper` is dead code but is not in the call path. Acceptance criterion satisfied.

**Verdict: BLOCKED** — manual POST returns `action: null` instead of `action: "list_leases"` because `claude-3-5-haiku-20241022` is not accessible on the configured API key.

2026-04-23 (implementer — model ID fix):

**What was done:** One-line change in `backend/apps/ai/guide_views.py` line 32: `GUIDE_MODEL` updated from `"claude-3-5-haiku-20241022"` (returns 404 on current API key) to `"claude-haiku-4-5"` (current valid snapshot). No test changes required — tests patch `anthropic.Anthropic` at the class level and never reference the model string directly.

**Automated:** `pytest apps/ai/tests/test_guide.py -v` → 12/12 passed.

**Tester action required:** Re-run the manual POST from the test plan with a valid Bearer token. Expected: `{"reply": "...", "action": {"route": "/leases", ...}}` with a non-null `action`. The graceful fallback (`action: null`) should no longer occur now that the model ID is valid.
