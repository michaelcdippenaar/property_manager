# content/legal CHANGELOG

Append one entry per corpus version bump (i.e. per release of
`apps.legal_rag` against this directory). Each entry follows the format
below.

```
## <corpus_version> — <yyyy-mm-dd>

### Added
- `<concept_id>` — short description

### Changed
- `<concept_id>` — from <old> to <new>, reason

### Attestations
- `<attestation_id>` — N facts confirmed, M corrected
```

---

<!-- entries appended here, newest first -->
